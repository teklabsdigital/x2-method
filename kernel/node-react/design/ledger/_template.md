---
kind: ledger
status: working
slice: _template
---

# Fidelity ledger template (slice NNN)

Copy this file to `slice-NNN.md` at each lock (gate 2b). The ledger enumerates the locked portion's visual atoms;
the per-screen fidelity tests assert both directions against it (UI-4): exhaustiveness (every atom renders) and
de-fabrication (nothing renders beyond it). Derived from the LOCKED prototype only, never authored ahead of it.

| Screen | Atom | Notes |
|--------|------|-------|
| <screen> | <data-atom id> | <state/theme coverage notes> |
