# kernel/node-react

The Node and TypeScript edition of the X2 kernel, and the catalog's second witness. It exists to answer a
question the first edition could not: whether the 69 claims in `../claims/` are portable invariants, or a
description of one stack's mechanisms wearing portable language. `BUILD-BRIEF.md` states that purpose and the
delta protocol the build follows.

**This edition is under construction, and `conformance.json` declares 65 of 69 claims `owed`.** What exists is
the platform seam every route-scanning claim stands on, proven red-green; the endpoint spine realizing SEC-1,
SEC-2 and SEC-3 over it; and the edition records the shared gates need. TEN-1 is deliberately still `owed`
despite three of its four surfaces being guarded and proven, for the reason S-8 in the findings register gives.

To be precise about what that record means, because an audit was right to press on it: `owed` here is a
statement about **pass order**, not a claim that no mechanism in this tree does anything. Several composed
mechanisms plainly run and plainly bind, and DOC-1, DEP-1 and HUM-1 in particular go red under injection against
this tree. They stay `owed` because the delta protocol in `BUILD-BRIEF.md` requires each claim's Node mechanism
to be designed from the claim text before the sibling edition's realization is opened, and a status written
ahead of that pass would be a status copied rather than earned. Every row is promoted by its own pass and by
nothing else. Reading a green build here as conformance would be the failure of aspirational enforcement that
the catalog was extracted to prevent.

## What the scaffold settled, and why it is structural

Four claims (SEC-1, SEC-2, SEC-3, TEN-1) name the same surface in the same words: a scan over "the composed route
table". In .NET that phrase is free, because `EndpointDataSource` is not a report about the router, it is the
router. Fastify hands out no equivalent: `printRoutes()` renders a tree for humans, `findRoute()` answers about
one URL. Nothing here is complete by construction, and a scan is only as good as its enumeration.

So completeness is bought, and the first attempt to buy it was audited and broken in thirteen ways. What it
costs, after the repair:

1. **The recorder is on the registration path.** `createApp` (`server/src/app.ts`) installs an `onRoute` hook in
   the same expression that creates the instance. `onRoute` fires inside `fastify.route()`, which every
   registration form funnels through, so no route can be registered before the recorder exists.
2. **The enumeration is materialized late, not early.** `onRoute` records references; the table is built at
   `onReady`. Snapshotting at `onRoute` was the audit's first refutation: Fastify runs hooks in registration
   order and registers what the LAST one leaves behind, so a later hook rewrote a route's url after the recorder
   had recorded it, and the table said `/decoy` while the server served `/admin/impersonate`.
3. **The table is reconciled against a second view.** At `onReady` it is compared with the router's own
   `printRoutes()` rendering, and any disagreement in either direction refuses the boot. Without this, the
   mechanism only ever agrees with itself.
4. **The evidence is immutable.** A getter-only decorator, frozen entries and a frozen array, so
   `app.routeTable = []` throws instead of emptying the table while every route keeps serving.
5. **The framework import is banned outside the composition root.** `server/eslint.config.js` covers every
   source extension, the subpath specifiers, dynamic `import()`, computed specifiers and `createRequire`.
6. **Only the entrypoint opens a socket.** A lint cannot see every dynamic evasion, so the second constraint is
   that no module but `src/main.ts` may call `listen`, and no module reaches the raw server modules. An instance
   the lint misses still serves nothing.

The recorder also refuses, before the server can serve, any route that exposes a surface it does not declare: a
body-bearing method with no `schema.body` (and the body-bearing set is defined by exclusion, so DELETE, OPTIONS
and methods added through `addHttpMethod` are all covered), a path parameter with no `schema.params`, any route
with no `schema.querystring`, and any wildcard. A declaration also has to be readable rather than merely
present: `{}`, `true`, `null` and open objects are refused, and `additionalProperties: false` is required.
That last clause is load-bearing rather than stylistic, because Fastify validates with `removeAdditional`: a
closed schema strips every field it does not name before the handler runs, so the declaration a scan reads and
the object a handler receives are the same thing.

The hole this cannot close is named rather than hidden: a hook that answers a request itself serves a URL with no
route behind it. `src/platform/__tests__/routeSurface.test.ts` asserts that hole as a passing test, so the day a
mechanism closes it, the test fails and gets rewritten instead of the limitation being quietly forgotten.

## What is here

```
BUILD-BRIEF.md   what this edition is for, the delta protocol, and the cut line
VERIFICATION.md  the dated verification history and the red-green proof table
VERSIONS.md      the DEP-1 ledger: kernel pin, packages, publish dates; the 30-day window header
conformance.json the per-claim status record: one row per catalog claim, the table below generated from it
edition.json     this edition's dependency manifests (DEP-1) and irreversible-surface homes (HUM-1)
.github/workflows/ci.yml   the TEST-3 loop (template; moves to repo root at instantiation)
.github/CODEOWNERS         the HUM-1 gate on migrations and published contracts (armed at instantiation)
.gitattributes   eol=lf everywhere (INV-03: no CRLF churn across the Windows/Mac/Docker boundary)
server/          Fastify 5 on Node 24: the composition root, the route recorder, and its completeness proofs
docs/            the DOC-1 tree (claims, decisions, contracts, runbooks, work); the templates are shared
```

Composed from the shared tier (`kernel/shared/`, one home for what is not stack-specific; see its README):

```
client-web/      Vite + React 19: tokens, primitives, notes module, scans, e2e harness + composed smoke
design/          the INV-01 design home: prototype/ (provenance README + imported artifact + _ds) and ledger/
docs/*/_template.md        the DOC-1 templates, one per registry kind
tools/docs-lint.mjs        the DOC-1 / TEN-5 / DEP-1 / DEC-1 / HUM-1 / conformance / MET-08 gate (plain node)
tools/conformance.mjs      validates conformance.json and generates the table below
```

**Never hand-edit a composed path.** Edit `kernel/shared/` and run `node kernel/tools/compose.mjs`; `--check`
fails the build on any divergence, so a local edit to a composed copy cannot merge.

## Running it

```
cd server && npm ci && npm run verify   # tsc, vitest, eslint
cd server && npm start                  # serves /health on the port declared in server/config/settings.json
cd client-web && npm ci && npm run verify
node tools/docs-lint.mjs                # the documentation, ledger, and conformance gates
node tools/conformance.mjs --check      # the record and its generated table
```

The server runs TypeScript directly on Node 24 (native type stripping), the same way the shared client's e2e
harness does. `tsc --noEmit` is the type gate; there is no build step to keep in sync with the run step.

## Instantiation manifest

Same obligation and same shape as the sibling edition's, because it is the same kernel: a concrete file set, then
ordered setup steps, then a verification set verified AS A SET rather than one bullet at a time from memory.
x2:seed runs it and does not restate it. This edition shipped without one until 2026-07-27, so a seed would have
reached step 2 of that skill and found no definition to run (E-65).

**A. The file set** (copy into the new repo root): `server/`, `client-web/`, `docs/`, `tools/`, `design/`,
`scripts/`, `.github/` (workflow AND CODEOWNERS), `.gitignore`, `.gitattributes`, `.nvmrc`, `VERSIONS.md`,
`conformance.json`, `edition.json`, `secret-scan.allow.json`.

This enumeration IS the file set, and it is the TRACKED contents of each item, not a directory copy. Measured
2026-07-27 by executing this step: `cp -R` over the enumeration produces a 233M tree, because `node_modules/`
and `.data/kernel.db` are nested INSIDE `server/` and `client-web/` rather than beside them, so enumerating the
top level does not exclude them. The enumeration and the exclusion are different axes and this paragraph used to
claim the first did the work of the second. Copy what git tracks (`git ls-files` scoped to each item, from the
kernel checkout the seed reads): 96 files, 1.2M. In the sibling the same literal copy takes a `.env` holding a
live development password, which is the same defect with a worse consequence (E-25, E-109). BUILD-BRIEF.md and VERIFICATION.md stay behind (kernel provenance, not project material), and so does
THIS README, because the seeded project writes its own. `scripts/` and `.nvmrc` joined this list on 2026-07-27 and both
were overdue. `scripts/e2e.ts` landed four commits earlier and `ci.yml`'s `e2e` job runs it by that path, so a
seed run in between would have produced a repository whose third CI job fails with ENOENT on its first push.
`.nvmrc` had never been named by either edition's manifest, so the runtime pin DEP-1 spreads across six surfaces
travelled as five and a seeded project's developers got whichever Node they happened to have. Both were found by
the check this paragraph is now compared against, on its first run, which is E-108. Two things the sibling ships that this edition does not, listed here so their absence is a
decision rather than an oversight: there is no `.vscode/`, and there is no `.claude/` prompt-submit hook, which
the sibling carries as the mechanical defence against standing constraints decaying under a long session. If
either is added it joins this list in the same commit, and so does any other directory a job learns to name.

**B. Setup steps, in order:**

1. **Rename the product.** `name` in `server/package.json` and `client-web/package.json`; the paths under
   `dependencySurfaces` and `irreversibleSurfaces` in `edition.json`; the matching `.github/CODEOWNERS` paths,
   which docs-lint fails on if the two disagree; and the `file` paths in `secret-scan.allow.json`, whose header
   says an entry matching nothing fails the scan. This edition has no `.sln`, no `UserSecretsId` and no
   `ConnectionStrings` key, so the sibling's config-value list does not apply; what does apply is
   `server/config/settings.json`, whose `auth.issuer` and `auth.audience` are kernel placeholders and rename with
   the product, and the database file name, which is written in TWO places and must agree: `database.file` in the
   same settings file and `engine.file` in `edition.json`. Both were missing from this list until 2026-07-27, so
   a correctly seeded project ran against `kernel.db` (E-109). Renaming the audience is not cosmetic: a test
   asserted against the kernel's placeholder value and failed on the day the rename was done right, which is the
   finding that added this sentence.
2. **Git setup.** `git init`; the canonical `.gitignore` and `.gitattributes` (`eol=lf`) are in the file set;
   first commit only when directed.
3. **Write the kernel pin (DEP-1).** Fill `VERSIONS.md`'s Kernel provenance section mechanically from the kernel
   checkout the file set was copied from: remote (`git remote get-url origin`), commit (`git rev-parse HEAD`) and
   the catalog pass date. Mechanical means the seed runs the commands; a human is never asked to transcribe a
   hash.
4. **Arm the gates (TEST-3 + HUM-1, the step that gets skipped).** Move `.github/workflows/ci.yml` to the repo
   root; branch protection requires EVERY job the workflow declares plus code-owner review; replace `@OWNER` in
   CODEOWNERS with the product owner. Until this step the loop runs and blocks nothing. Arming stays a human
   step, which is how both claims word it; what the kernel owes is the readback, so finish with
   `node tools/gate-check.mjs` and do not treat this step as done until it exits ok. It derives the required job
   names from the workflow rather than listing them, which is why this step says "every job it declares" and not
   a number. Both editions' workflows declare five jobs today; this one declared four when the step was written,
   which is exactly why the step carries no number and the aside naming one went stale instead (E-108).
5. **Dev environment.** `cd server && npm ci`, then `cd client-web && npm ci`. Node 24 or newer, because the
   server and the harness run TypeScript directly through native type stripping and there is no build step. The
   port and host are declared configuration, not a default and not an environment read: `server/config/settings.json`
   carries `http.port` and `http.host`, validated at startup and overridable per environment through a name
   derived from the key. Client 5173.
6. **Design home (INV-01), carried forward deliberately.** As the sibling edition, and for the same reason: the
   export is two skills downstream of the seed, so the placeholder ships and the debt is recorded in
   `design/prototype/README.md` rather than pretended away.

**C. Verify as a set** (the seed is not done until all pass):

- `npm run verify` in `server/` (tsc, vitest, eslint)
- `npm ci && npm run verify` in `client-web/`
- `node tools/docs-lint.mjs --self-test`, then `node tools/docs-lint.mjs`
- `node tools/conformance.mjs --check`
- `node tools/secret-scan.mjs --self-test`, then `node tools/secret-scan.mjs`
- `node tools/gate-check.mjs --self-test`, then `node tools/gate-check.mjs`
- `node scripts/e2e.ts`, which migrates a throwaway database, boots the real entrypoint on a port nobody wrote
  down, drives the real client services against it and then the composed entrypoint. It needs no engine service
  container and no developer setup, because the store is `node:sqlite` and that is the runtime, so it belongs in
  a set verified on a fresh machine where the sibling's `scripts/e2e.sh` does not.
- The dash check standalone, with the literal-byte pattern (BSD grep false-negatives on a BRE class):
  `grep -rn "$(printf '\342\200\224')" <authored paths>` and `grep -rn "$(printf '\342\200\223')" <authored paths>`

One gap this list does not paper over: step 5 points at no runbook, because `docs/runbooks/` here holds only
`_template.md`. It belongs to this edition's next build pass and is named rather than left for a seeder to
discover. The e2e bullet above was the second gap until 2026-07-27, and it was closed by the orchestrator four
commits before this list said so (E-108).

## Conformance

Every claim in the catalog owes a row in `conformance.json`, and docs-lint fails if one is missing, so a claim
minted by a future catalog pass cannot land while this edition stays silent about it. The table below is
generated from that record: edit the JSON, never the table.

The four statuses mean what they mean in the catalog: `proven` (a mechanism exists and a test proves it binds),
`patterned` (the mechanism exists and is followed, with a named gap), `latent` (the mechanism exists but has had
no real subject yet), `owed` (not built, with a named trigger).

**A row's status is what a guard does, never what a process has reached.** Until 2026-07-27 this section said
every row was `owed` because the delta protocol required each Node mechanism to be designed from the claim text
before the sibling's realization was opened. That sentence was false twice over. It was false as arithmetic, four
rows already read `proven`. It was false in kind, and that is the part worth naming: it made an `owed` status
mean "this claim has not had its delta pass" when the vocabulary defines it as "not built, with a named trigger".
Those are different facts, and a reader seeding from this record cannot tell them apart. The delta pass is a
research procedure this edition ran to make portability findings falsifiable; the owner closed its research phase
on 2026-07-27 (see `record/delta-log.md`), so it can no longer gate anything, and it should not have gated a
status while it was open.

The measured consequence is that this record UNDERSTATES the tree, which is the rarer and less dangerous
direction but is still a record that cannot be honestly seeded from. Several claims are realized in the shared
tier, composed into this edition, and passing here while their rows read "not built": the token, lint, fidelity
and smoke mechanisms behind the UI family, the redactor behind SEC-6, the naming and placement test behind MOD-2,
and docs-lint itself behind DOC-1. Correcting them is planting work, one claim at a time, not a bulk flip: a row
moves when a violation planted against that claim's own obligation turns that claim's own guard red, and not
before. Rows whose sibling is also `owed` carry that claim's product trigger too, since a claim waiting on the
first message broker waits in every edition.

<!-- conformance:begin -->
Generated from `conformance.json` by `tools/conformance.mjs`; edit the JSON, not the table.
69 claims at catalog pass 2026-07-26: 4 `proven`, 5 `patterned`, 3 `latent`, 57 `owed`.

29 of those rows state a status per obligation; the row's own status is the weakest of them, so the tally above reads a split row at its weakest half and never at its strongest.

| Claim | Edition mechanism | Status |
|-------|-------------------|--------|
| TEN-1 tenant comes from the credential only | the prohibition half IS built and proven on all four surfaces: `scanEndpointSpine` rejects tenant-shaped route, query and body-contract names by tokenized run, and a runtime `onRequest` strip in `createApp` removes tenant-shaped request headers before any handler, which is the only reachable mechanism for a surface that cannot be closed. The strip's registry carries the concatenated spellings (`tenantid`, `orgid`, `organisationid`) explicitly, because Node lowercases header names before any hook runs, so `X-TenantId` arrives as `x-tenantid` and the tokenizer's camel-case half is unavailable on this surface by construction | patterned (the weakest of 2 obligations); **tenant identity never travels as a request parameter** `proven`: `scanEndpointSpine` rejects tenant-shaped route, query and body-contract names by tokenized run, and a runtime `onRequest` strip in `createApp` removes tenant-shaped headers before any handler, carrying the concatenated spellings explicitly because Node lowercases header names before any hook runs; **the tenant is resolved solely from the validated credential** `patterned`: Built 2026-07-27 at 53b1eec, and it is the S-8 gap closing. The trigger, the credential mint, has half fired: this edition now VERIFIES a credential even though it still does not mint one, and verification is the half this obligation needed. `decideCredential` reads the Authorization header, verifies the signature, and puts the token's `tenant_id` on a frozen credential; it reads no route parameter, no query value, no body and no other header, and `app.ts` has already deleted every tenant-shaped header before it runs. So a tenant reaching a handler has come through a verified signature and through nothing else. Planted at 53b1eec: a token carrying no `tenant_id`, or an array in its place, is refused rather than defaulted. `patterned` and not `proven` for a reason this row should say plainly: NOTHING DOWNSTREAM READS `request.credential` YET, so the resolution rule is proven at the point of resolution and unexercised at every point of use. The first store query that scopes by tenant is the first seam that has to take it from here rather than from an argument, and each such seam owes its own test; Lifted off `owed` 2026-07-27 at 53b1eec, and the roll-up rule is what lifted it: the mint half of SEC-4 and TEN-6 is still owed, but the VERIFY half landed 2026-07-27 at 53b1eec and moved the second obligation off `owed`. TEN-1's statement is a prohibition plus a resolution rule; the prohibition is realized and red-green proven, and the resolution rule had no mechanism for as long as no credential existed to resolve from, which is S-8. It has one now. Round 4 audit: the prohibition half was itself overstated, because `X-TenantId` and `X-OrgId` walked past the strip over real HTTP until the concatenated entries landed, which is evidence for the conservative status rather than against it. Findings: A-2, E-9, E-10, S-8. |
| TEN-2 ambient scope, fail-closed | `TenantId` is a branded type produced only by `tenantOf(credential)`, with the testing hatch pinned out of production by lint; every `NoteStore` method takes it as its first parameter; `architecture/statementSurface.ts` asserts every statement in every `STATEMENTS` set constrains `tenant_id` to a parameter; `persistence/notes/__tests__` and `routes/__tests__` assert two tenants are separated at the engine and on the wire | patterned (the weakest of 4 obligations); **no store operation can run without a tenant** `proven`: Every method of `NoteStore` takes `TenantId` first, and `TenantId` is a branded type whose only producer is `tenantOf(credential)`. There is no value of that type that did not come from a verified signature, and no call that omits it, because omitting it is a type error. Strengthened 2026-07-27 at the one end the type could not hold: `tenantForTesting` is the hatch that produces a `TenantId` from an arbitrary string, its own comment said a lint could pin it, and nothing did. It is now refused outside `__tests__`, as the named import and as the call, in both spellings so a namespace import does not walk past. Red-proven against the real route module; **cross-tenant access is uniformly not-found** `proven`: Read, get and delete all carry `tenant_id` in their own predicate, so a cross-tenant request is an absence at the store rather than an error to be translated at the route. E-50 is why this is stated as uniformity rather than as correctness: in the sibling a cross-tenant read answered 404 while a cross-tenant delete with a forgotten filter answered 500, and a caller could tell the difference, which is an existence oracle. Proven end to end by the harness's `cross-tenant-404` scenario, green at 31278db. Strengthened twice on 2026-07-27. Statically: the scan reads every statement's predicate and refuses one that does not constrain `tenant_id` to a parameter, so uniformity is a property of the statement set rather than of three methods. Behaviourally: read, list and delete are each asked as a second tenant, in process and over the wire, and the delete is asserted to leave the row where it was, because a delete that reported not-found and removed the row anyway passes a status-code assertion; **every execution path entry establishes a tenant** `patterned`: Every HTTP path does: the gate hook sets `request.credential` before any handler, and each handler calls `tenantFor(request)`. There are no non-HTTP paths in this edition, so the register of `Begin` sites the claim asks for has nothing to list. Per-seam: the first background job, scheduled task or CLI owes its own answer, and the only answer available is a credential; **a guard refuses rather than defaulting** `patterned`: Lifted from `owed`. The hole this obligation names is a handler that obtains a REAL `TenantId` and passes the wrong one, which no type can close, and it is now asserted per request: three tests drive the composed app with a credential seam and assert against the DATABASE rather than the response, because a response only shows what the handler chose to send back. Planted exactly, `tenantOf({ ...request.credential, tenantId: 'someone-else' })`, and eslint is clean on it while two of the three tests go red. `patterned` and not `proven` because the assertion is per route: the fifth handler owes its own, and nothing enumerates handlers to check that each has one. trigger for `proven`: a seam recording the tenant each store call received, compared against the credential on the request that caused it, so the obligation is discharged once for the surface rather than once per route; Re-measured 2026-07-27. Three of the four obligations moved and the row stays `owed` on the fourth. The cross-tenant scan E-77 says the sibling cannot have is built: every statement is a literal in one object, so 'every query is tenant-scoped' is a statement about the store rather than about the methods somebody remembered to check. It reads the PREDICATE rather than the statement, because a SELECT can name `tenant_id` in its column list and read every tenant, which is the shape a substring check passes. Two findings came out of planting against it: E-100, that E-50's own divergence had no test on the delete side in either edition, and E-102, that a uniformly wrong tenant is self-consistent so no single-tenant test can see it. Findings: E-99, E-100, E-101, E-102. |
| TEN-3 tenant-leading composite keys | `0001_initial.sql` declares `PRIMARY KEY (tenant_id, id)`, so the tenant leads row identity | owed (the weakest of 3 obligations); **the primary key leads with the tenant id** `proven`: `PRIMARY KEY (tenant_id, id)` in the shipped migration, applied by the real migration file in every test fixture rather than by a hand-written CREATE beside it. A row cannot be addressed without naming its tenant, and a query that forgets the tenant cannot use the key at all; **the assertion covers every tenant-owned entity** `owed`: There is exactly one table and no marker, no registry and no scan. The sibling's `TenantKeyTests` is the shape this takes, and its E-43 is the warning: narrowing the registry and stripping the marker there left 113 tests green, both halves going vacuous together. trigger: the second table, which is the first moment an enumeration has more than one thing to enumerate; the scan is over the migration SQL rather than over an ORM model, because this edition has no model to reflect over; **a tenant-shaped column implies the tenant-owned marker** `owed`: The comparison the sibling makes between its marked set and the set of entities carrying a tenant column. Neither side exists here yet: no marker, one table. trigger: the same second table; Built 2026-07-27 at 31278db. One tenant-owned table exists, so the key shape is right and nothing enumerates it. trigger: the second tenant-owned table, which is the first moment an enumeration has more than one thing to enumerate. |
| TEN-4 save-time guard and write provenance | not built | owed; trigger: the first tenant-owned write path. |
| TEN-5 sanctioned-bypass ledger | `docs/claims/tenant-bypass-ledger.md` (empty, v1 by design) plus docs-lint's TEN-5 checks: the ledger must exist, every row must name a sole-reader test, that name must resolve to an identifier in the tree, and it must be the only bypass ledger | owed (the weakest of 5 obligations); **a committed ledger exists and is where the claim lives** `proven`: Removing it fails docs-lint with a sentence naming TEN-5, not a stack trace; **a ledger row that names no sole-reader test fails the build** `proven`: Red-correct in this tree. The parse is keyed on the column NAME rather than position, so an inserted column cannot move the cell out of reach, and a ledger the parser cannot read as a table is a failure rather than an empty pass (E-35 repaired); **the named sole-reader test exists, and is the remedy** `patterned`: Red-correct in this tree for a row naming an identifier that appears in no source file (E-34 repaired). `patterned` on the obligation's second clause: it proves the name resolves to something, not that the something is a test, and nothing checks that the test is the sole reader; **one ledger, not several** `patterned`: Red-correct in this tree for a planted `docs/claims/billing-bypass-ledger.md`. `patterned` because the rule is on the file NAME, so a second ledger under another spelling escapes; **every sanctioned cross-tenant path appears in the ledger** `owed`: unbuilt. Half of the trigger this obligation carried has fired: it read `a persistence layer, then the first sanctioned bypass`, and `server/src/persistence/` now exists. What arrived with the layer is a partial substitute rather than the mechanism. `scanStatementSurface` refuses any statement whose WHERE does not constrain `tenant_id` to a parameter, so a cross-tenant read can no longer be written into a store quietly; the only way past it is the file-level exemption list, and a stale exemption is itself reported. What is missing is the JOIN: the exemption list is a register of files permitted to hand the engine SQL outside the statement set, the ledger is a register of sanctioned cross-tenant ACCESS, and nothing relates the two, so an exemption added for a file that does read across tenants would satisfy the scan and never reach the ledger. Both shipped exemptions are argued as non-tenant-bearing (migration DDL, and one PRAGMA), and the claim rules only on the TEN-3 key-shape case belonging elsewhere, not on this one. trigger: the first sanctioned bypass, which is now one exemption away rather than one layer away; Measured in this edition's own tree 2026-07-27 (node Phase A, F3). Lifted out of a bare `trigger:` line, which recorded no mechanism while four checks were binding in this tree. All four planted here and all four red-correct: an absent ledger, a row with an empty test cell, a row naming `NightlyNodeSweepIsSoleReader` which exists nowhere, and a second `billing-bypass-ledger.md`. The row stays `owed` on one obligation, gated on the first sanctioned bypass. It read `trigger-gated twice over` until 2026-07-27, the second gate being that this edition had no persistence layer for a cross-tenant read to live in; the store landed one, and nothing re-read this row when it did (E-106). Findings: E-34, E-35, E-77, E-106. |
| TEN-6 tenant is minted from membership, never asserted | `tokenVerification.ts` refuses a token that does not carry exactly one non-empty `tenant_id`; `bearerCredential.ts` puts it on the credential and reads no other surface; `app.ts` strips every tenant-shaped header before the seam runs | owed (the weakest of 4 obligations); **the credential carries exactly one active tenant** `proven`: Planted 2026-07-27 at 53b1eec: a token with no `tenant_id`, or with an array in its place, is refused by reason rather than defaulted, and a verified token's `tenantId` is asserted to be the value the token carried. `Credential` has a single `tenantId` field, so 'exactly one' is the type rather than a convention; **a tenant never travels as a header, a parameter, or mutable session state** `proven`: Three mechanisms, none of them new here and all of them now load-bearing because there is finally a credential for them to be the alternative to: `app.ts` deletes every tenant-shaped header in the first onRequest hook, before the credential seam; `scanEndpointSpine` refuses a tenant-shaped name on the route, query and body surfaces of the composed table; and `decideCredential` reads the Authorization header and nothing else, so there is no code path by which a tenant reaches a handler except through a verified signature. The credential is frozen, so it is not mutable session state either; **entering or switching a tenant mints a new credential through the real sign-in surface** `owed`: Unbuilt, and this is the clause the claim is named for. There is no sign-in surface, no tenant selector, and no membership data to validate a selection against, so nothing here mints anything: tokens are minted out of process by the shared harness minter, which is legitimate for a kernel with no identity module and is exactly the premise `mintToken.mjs` records as invalidated by the first identity slice. trigger: a membership store. This read `which arrives with the persistence layer (G)`, and the layer arrived on 2026-07-27 carrying a notes table and nothing else, so the prediction was wrong and the trigger is independent of the layer (E-106); the mint surface is then TEN-1's one sanctioned carve-out and `CARVE_OUTS` in endpointSpine.ts is the empty list waiting for it; **revoking a membership bumps the session version, so credentials for that tenant die immediately** `owed`: Composes with SEC-4, and SEC-4's half of it is built: `sessionVersions.bump` exists and a bumped principal's token is refused end to end. What is missing is the thing that would CALL it, because there is no membership to revoke. trigger: the same membership store; this obligation is a one-line call at the point where a membership is removed, and it is recorded now so that point is not written without it; Half built 2026-07-27 at 53b1eec. The verifier half of this claim is real: a credential now carries exactly one tenant and it comes from a signed token. The MINT half is what the claim is named for and it needs membership data, which this edition has no store for, so it stays owed and named rather than borrowing credit from the half that landed. Findings: E-84. |
| SEC-1 every endpoint gated | `POLICIES` registry (a policy naming no permission, or named `anonymous`, is refused at construction, so the bare authenticated-only gate is unrepresentable rather than rejected, and both refusals are exercised by test rather than asserted over the shipped registry's contents); every route declares `config.policy`, checked by `scanEndpointSpine` over the composed table; `ANONYMOUS_ROUTES` carries a justification per entry, is keyed by method and url, covers a synthesized HEAD only under reference identity on `config`, and a stale entry fails; deny-by-default `onRequest` hook installed in the expression that creates the instance, denying an ungated route to anonymous AND authenticated callers alike; `composeApp` runs the scan after `ready()` and refuses to compose a violating surface, which is itself red-green proven through the `surfaces` parameter | proven (the weakest of 5 obligations); **every route names a policy, or is an allowlisted anonymous route** `proven`: `scanEndpointSpine` reads `config.policy` off every entry in the composed table. Four refusals run as permanent tests: no policy at all, an anonymous route absent from the allowlist, a look-alike policy object (policies compare by identity, not shape), and an independently registered HEAD trying to ride in on its GET's allowlist entry. The last has its own non-vacuity partner asserting the SYNTHESIZED head is accepted, so the guard is not simply refusing all HEADs; **the enumeration reaches every route the server serves** `proven`: Two independent mechanisms, and the second is why this is `proven` and not merely believed. The table records REFERENCES at `onRoute` and materializes at `onReady`, after every hook has had its turn, and `onReady` reconciles it against Fastify's own `printRoutes()` and refuses to boot on a disagreement in either direction. Measured 2026-07-27: narrowing the scan's own loop to match nothing fails 23 tests across SEC-1, SEC-2, SEC-3 and the wiring proof, each naming its claim. The sibling's equivalent narrowing left five assertions across two claims green (E-72), and the difference is structural rather than diligence (E-79); **the anonymous allowlist is reviewed rather than accumulated** `proven`: Each entry carries a justification, is keyed by method AND url, and a stale entry that matches no route fails, so the list cannot rot. A synthesized HEAD is covered under its GET by REFERENCE identity on `config`, which is a proof of synthesis no comparison of values can give, and is what stops a separately written HEAD from inheriting the carve-out; **the fallback denies by default, to authenticated callers too** `proven`: The deny-by-default `onRequest` hook is installed in the expression that creates the instance. Proven against both an anonymous caller and an AUTHENTICATED one with no permission, which is the distinction that makes it deny-by-default rather than require-authentication (the sibling shipped the weaker one, E-7). Seven hook bypass attempts and a credential-leak probe were run and none evaded; **the scan is wired into composition, not merely written** `proven`: `composeApp` runs `assertEndpointSpine` after `ready()`, so a violating surface refuses to BOOT rather than merely failing CI. The wiring is itself red-green proven through the `surfaces` parameter, which exists precisely because the composed app is by construction the one with no violations in it, so a violating surface has to be passable in. An earlier round claimed this refusal on the evidence of a manual injection, and an audit then deleted the call and its import and watched the suite stay green with a clean typecheck and lint; Re-measured 2026-07-27 (node Phase A) and the flat `proven` is now five obligations, because a flat status is how several separate things come to be carried by one word (the lesson E-72 recorded in the sibling). Every obligation below is evidenced by a permanent test that runs on every suite execution, not by a hand-run pass. The credential mint is still owed (SEC-4, TEN-6), so every gated route in this exemplar answers 401 and the permission evaluation is proven through an injected `authenticate` seam rather than a real mint; that is a limit on what has been exercised end to end, not on the gate. Findings: A-3, A-4, E-7, E-8, E-10, E-79. |
| SEC-2 anti mass-assignment | `scanEndpointSpine` walks every route's `schema.body` recursively through `properties`, `items` and `allOf`/`anyOf`/`oneOf`, matching `SERVER_CONTROLLED_FIELDS` by tokenized run (`id` whole-name only, so a foreign key does not trip it); `createApp` refuses at registration any schema keyword the walk does not descend (`$ref`, `patternProperties`, tuple-form `items`, `not`, `if`/`then`/`else`, `dependentSchemas`, `prefixItems`, `contains`, `propertyNames`, `unevaluatedProperties`, `definitions`) and requires EVERY object schema inside a scanned surface to set `additionalProperties: false`, so the closed declaration is a runtime filter at every level and not only at the top | proven (the weakest of 4 obligations); **the predicate rejects a registry member at any depth of a declared body** `proven`: The walk descends `properties`, `items` and `allOf`/`anyOf`/`oneOf`. Refusals run as permanent tests at the top level and nested, with a non-vacuity partner accepting a body carrying only caller-owned fields. Depth was the round 4 audit's finding: the closure was depth-0 only, so an open nested object delivered `author.tenantId` and `author.createdBy` to the handler with the scan silent; **the enumeration reaches every body-bearing route** `proven`: Same composed table and same boot-time reconciliation as SEC-1's enumeration obligation, and the same measurement covers it: narrowing the scan's loop fails this claim's tests by name (E-79). Only GET and HEAD are bodyless; the obligation set is inverted rather than enumerated, so a method this code has never heard of owes a body contract rather than escaping; **the declaration cannot hide a surface the walk does not descend** `proven`: `createApp` refuses at REGISTRATION any schema keyword the walk cannot descend (`$ref`, `patternProperties`, tuple-form `items`, `not`, `if`/`then`/`else`, `dependentSchemas`, `prefixItems`, `contains`, `propertyNames`, `unevaluatedProperties`, `definitions`) and requires every object schema inside a scanned surface to set `additionalProperties: false`. This is the obligation the sibling has no equivalent of: the closed declaration is a runtime filter at every level, not only at the top, so eight evasions found by the round 4 audit are refused at boot rather than reported later; **the comparison resolves compounds, concatenations and decompositions of a listed word** `proven`: Matched by tokenized run, with `id` whole-name only so a foreign key does not trip it, asserted in both directions: `id` and `Id` match, `parentId` and `noteId` do not. The registry's heuristic residue is the claim's own declared weakening note (a field named `newState` slips past a registry listing `status`), so it is the claim's cost rather than this mechanism's gap; Re-measured 2026-07-27 (node Phase A); the flat `proven` is now four obligations. The contract is a value on the route, so there is no assembly heuristic, no name-suffix filter and no route-without-contract (B-2). The registration-time refusal of unwalkable schema keywords is this edition's strongest single mechanism and the sibling has no equivalent of it. Findings: B-2, B-4, E-6, E-9, E-10, E-79. |
| SEC-3 no PII in URLs | `scanEndpointSpine` over the composed route table: path parameters reconciled between the URL text and `schema.params` (a disagreement in either direction fails), query parameters from the mandatory closed `schema.querystring`, matched against `PII_PARAMETERS` by tokenized contiguous run so `emailAddress` and `user_email` match `email`, plus explicit entries for the three morphological classes a run over a tokenized entry cannot reach: concatenations (`firstname`, `dateofbirth`), decompositions (`mail`, so `e_mail` and `EMailAddress` match) and plurals (`emails`), with the tokenizer splitting letters from digits so `email1` matches; carve-outs are keyed by claim, route, SURFACE and field, carry a justification, cover a synthesized HEAD under their GET, and a stale carve-out fails | proven (the weakest of 5 obligations); **the predicate rejects a registry match on a parameter it can see** `proven`: Permanent refusal tests for a path parameter named for the value itself, a compound one, a snake-cased one and a query parameter; **the enumeration reaches every declared path and query parameter** `proven`: Path parameters are reconciled between the URL text and `schema.params` and a disagreement in EITHER direction fails, so a parameter in the URL but not the schema is caught and so is the reverse. Query parameters come from a mandatory closed `schema.querystring`. Covered by the same narrowing measurement as SEC-1 (E-79); **a parameter that binds without being declared is inside the surface** `proven`: This is the obligation the sibling carries as `owed`, and this edition closes it rather than inheriting it. `additionalProperties: false` plus Fastify's `removeAdditional` means an undeclared query parameter is STRIPPED before the handler runs, so an ad-hoc read of an undeclared parameter reads nothing. It does not close SEC-3's underlying harm, since a URL carrying PII still reaches logs and proxies whether or not the handler reads it; it closes the enumeration hole, which is what this obligation is; **the comparison resolves compounds, concatenations, decompositions and plurals of a listed word** `proven`: Tokenized contiguous run, plus explicit entries for the three morphological classes a run over tokens cannot reach: concatenations (`firstname`, `dateofbirth`), decompositions (`mail`, so `e_mail` and `EMailAddress` match) and plurals (`emails`), with letters split from digits so `email1` matches. Its own false positives are ASSERTED rather than described: `fileName` and `tenantName` match and are accepted as the cost, `filename`, `namespace`, `mailbox` and `voicemail` do not; **carve-outs are reviewed and cannot rot** `proven`: Keyed by claim, route, SURFACE and field, each carrying a justification, covering a synthesized HEAD under its GET, and a stale carve-out fails. The shipped list is empty, so both the suppression path and the staleness path were dead code an audit found shipped unexecuted; they are reachable now through the `carveOuts` parameter and both branches are exercised; Re-measured 2026-07-27 (node Phase A); the flat `proven` is now five obligations. The undeclared-parameter obligation is genuinely closed here and is `owed` in the sibling, which is each edition measured on its own mechanism rather than against the other. Findings: E-9, E-10, E-79. |
| SEC-4 JWT hardening and revocation | `server/src/platform/tokenVerification.ts` verifies a bearer token as a pure function over a token, a key set and an instant; `bearerCredential.ts` turns an Authorization header into a credential and applies the session-version check; `sessionVersions.ts` holds the server-side version; `composeApp` wires all three, and `tokenVerification.test.ts` proves them by input | owed (the weakest of 6 obligations); **the signing algorithm is pinned to exactly one** `proven`: `ADMITTED_ALGORITHMS` is an exported frozen value asserted whole, not probed one exclusion at a time. E-54 is why: the sibling's pin was proven by minting one HS384 token, which says nothing about what the set ADMITS, and adding RS256 to it left its whole suite green. Planted 2026-07-27 at 53b1eec: HS384, HS512, RS256, ES256 and none are each refused with a reason naming the declared alg, and a token signed correctly with HS256 while DECLARING HS384 is refused for declaring it rather than for failing to verify, which is what makes this a comparison and not a negotiation; **tokens must be signed, on a branch the algorithm pin cannot shadow** `proven`: Checked before the header is read at all, with its own reason. E-55 measured why the two must be independent: with the sibling's pin still `[HS256]` and `RequireSignedTokens` false, an alg:none token AUTHENTICATED, because an unsigned token never reaches signature validation so the pin never runs. Planted at 53b1eec: a token that is BOTH unsigned AND declares an inadmissible algorithm is refused for carrying no signature, which is the ordering the independence rests on; **the premise the claim rests on: tokens expire, and come from the configured issuer and audience** `proven`: Recorded as its own obligation rather than folded into the ones above, because SEC-4's sentences name none of the three and this row should not pretend they do. E-88 is the argument: the revocation half says revocation takes effect `rather than at token expiry`, which PRESUPPOSES that tokens expire, and a guard set built from a claim's words alone does not cover a premise the claim states as background. Planted at 53b1eec: a wrong issuer, a wrong audience, an absent exp, a non-integer exp, a past exp and a future nbf are each refused by reason, and the exp boundary is asserted at one second so it is not off by one. Issuer and audience are read from resolved settings and never written twice (CFG-1, E-75); **a token minted before the current session version is refused** `proven`: `decideCredential` compares the token's `sv` against the server-side version on every request that presents a token, and it is the only path by which `request.credential` is ever set, so there is no request that reaches a handler without passing it. Equality and not `>=`: a token minted AHEAD of the server's version is refused too, because it is one the server cannot account for. Planted at 53b1eec by disabling the comparison, which turned three tests red including the end-to-end one; and proven positively rather than only negatively, which E-90 says is the half that distinguishes a working gate from an absent one: the same token, the same server, 200 before the bump and 401 after it; **every principal carries a server-side version, and it is the server that carries it** `proven`: The version lives in `sessionVersions.ts` on the server; the token carries only the version it was minted at, so no token can argue with a revocation. An unknown principal reads as `INITIAL_SESSION_VERSION`, which is asserted as a property rather than left to be discovered: a store with no user table behind it cannot distinguish a principal at version 1 from one it has never heard of, so any validly signed token bearing sv=1 for an unbumped subject is accepted. That is not a hole in revocation, which only has to invalidate what it has seen, and revoking requires having seen it; it is the absence of a principal store, which is a different claim and arrives with G; **revocation reaches every process serving the principal** `owed`: The store is a Map inside one process. For the shape this edition ships, one process, revocation is immediate and is proven above. For any deployment that runs more than one, a bump reaches the process that received it and no other, so a revoked token keeps working everywhere else and the claim's word `immediately` is false of the system even though it is true of each process. Split out rather than argued away inside the obligation above, because someone seeding from this record will choose a replica count without reading a paragraph. trigger: the first deployment running more than one process, or the persistence layer, whichever lands first; the upgrade is a shared store behind the same `SessionVersions` interface. SEC-4's weakening note about a short-TTL cache attaches HERE and not before: there is no cache in front of a Map, so the revocation delay is currently zero and the note's named-and-tested-bound obligation has no surface yet; Built 2026-07-27 at 53b1eec, and proved by INPUT rather than by configuration, because this stack has no validation object to read back off a host. That difference is not a handicap here: E-88 planted five of the sibling's seven validation properties permissive and all 261 of its tests stayed green, for want of a violating token to send. Every rejection below is asserted by REASON and not by status, for the reason E-90 gives: a refusal cannot distinguish a mechanism that works from one that is absent, because absence refuses everything, so only a success separates them. Findings: E-54, E-55, E-88, E-89, E-90. |
| SEC-5 no secrets in config | the committed-config half IS built and proven: a secret-shaped key (token-matched against `SECRET_SHAPED`) may hold only the empty placeholder in a committed file, refused at resolution AND by `scanConfigurationSurface`; the dev store is at a path asserted to be outside `EDITION_ROOT`, which is a property of the path rather than of a gitignore; a lint confines filesystem reads to the settings seam; secret-shaped literals in source are reported, with an exemption list carrying a reason per entry and a staleness check | owed (the weakest of 4 obligations); **no secret-shaped value in committed configuration** `proven`: a secret-shaped key (token-matched against `SECRET_SHAPED`) may hold only the empty placeholder in a committed file, refused both at resolution and by `scanConfigurationSurface`; **development secrets live outside the repository tree** `proven`: the dev store sits at a path asserted to be outside `EDITION_ROOT`, which is a property of the path rather than of a gitignore, and a lint confines filesystem reads to the settings seam; **a CI secret-scan gate runs over the repository** `proven`: `tools/secret-scan.mjs` runs in the CI loop with `--self-test` executed first, so the scan's extent is asserted in the same artifact as the scan (E-16). This obligation read `owed` in prose until 2026-07-26 and the E-16 repair had already built it (E-19); **runtime credentials sit behind a vault port** `owed`: no runtime credential exists in this edition, so the port has nothing to hold and the stored-handle idiom has nothing to store (trigger: the first runtime credential); `owed` on the S-8 reasoning, deliberately and conservatively: two of the claim's obligations have no mechanism here. The vault port for runtime credentials is owed in both editions (trigger: first runtime credential). A credential assigned to a non-secret-shaped name is still invisible to both built halves, which is the name predicate's stated reach and E-9's shape in a fourth registry. The sibling's realization of the same claim is measurably weaker on the built half: see E-11 and E-12. |
| SEC-6 log safety | `redact.ts` scrubs Authorization values (JSON and header forms) and JWT-shaped strings from harness output, unit-tested in `__tests__/redact.test.ts` | owed (the weakest of 2 obligations); **secret-shaped values are scrubbed by one redactor** `patterned`: One redactor, three arms (JSON authorization, header authorization, JWT shape), red-correct when an arm is narrowed. Heuristic by shape, so a secret in a form none of the three arms matches escapes; each new secret shape owes its own arm; **each logging surface asserts its own redaction** `owed`: The harness output surface is covered. The SERVER emits nothing: `createApp` passes `logger: false`. Trigger: the first server log statement, which creates the surface this obligation is about; Measured in this edition's own tree 2026-07-27 (node Phase A, F2), not inferred from the sibling. Red-correct: narrowing the JWT arm to a pattern that matches nothing turns `scrubs a JWT-shaped token` red. The row is `owed` on its server half, and that half is trigger-gated rather than merely unbuilt: `createApp` constructs Fastify with `logger: false`, so this server emits no log at all. There is no logging surface to assert redaction over yet, which is a materially different situation from the sibling's and is why the trigger names the first log statement rather than a missing test. |
| SEC-7 opaque public identifiers | not built | owed; trigger: the first anonymous resource surface in an edition project (the kernel exemplar has none). |
| SEC-8 abuse posture | not built | owed; trigger: the first publicly exposed edition host (the launch pre-flight names it). |
| SEC-9 deployment-edge hardening | not built | owed; trigger: the first deployed edition host (arrives with the launch pre-flight). |
| SEC-10 authentication strength asserted at mint, verified at the gate | not built | owed; trigger: first identity slice (shared with SEC-4's version store and TEN-6). |
| SEC-11 the runtime credential cannot touch the schema | not built | owed; trigger: the next edition build pass (rides DATA-6: the migrate mode and the role split are one provisioning story; the integration tier's provisioning path is where both roles are created). |
| TIME-1 UTC, offset-aware, and nothing else | the contract half IS built and proven: `scanEndpointSpine` walks every route's body, params, querystring, headers and response schemas and rejects a time-named property (token-matched against `TIME_SHAPED`) declared as a bare string, since `format: 'date-time'` is RFC3339 and RFC3339 requires an offset; `platform/clock.ts` is the one clock seam, with `new Date`, `Date.now`, `Date.parse`, `Date.UTC` and `performance.now` banned everywhere else by lint, which is the monotonic-durations extension's confinement | owed (the weakest of 3 obligations); **no naive instant on the contract surface** `proven`: `scanEndpointSpine` walks every route's body, params, querystring, headers and response schemas and rejects a time-named property declared as a bare string, since `format: 'date-time'` is RFC3339 and RFC3339 requires an offset; **durations come from the monotonic source through one clock seam** `proven`: `platform/clock.ts` is the one seam; `new Date`, `Date.now`, `Date.parse`, `Date.UTC` and `performance.now` are banned everywhere else by lint, which is the monotonic-durations extension's confinement; **no naive instant in domain types or persistence** `owed`: no persistence surface exists here, so a third of the statement has nothing to scan (trigger: the first store); trigger: the first store, for the persistence surface, and the first scheduling slice, for the zone-carrying shape the weakening note describes. `owed` on the S-8 reasoning, and the claim itself does not port as written (A-5, B-5). A JS `Date` is an instant: it lacks the ambiguity the harm paragraph is about AND lacks the offset the statement requires, so it satisfies neither side of the ban, because "UTC-anchored offset-aware" fuses two properties `DateTimeOffset` supplies together. `Temporal` is absent from Node 24.13.1, asserted as a test. No persistence surface exists, so that third of the claim has nothing to scan; the response half of the contract scan is a report rather than a proof, because response schemas carry no closure obligation. The two editions check disjoint surfaces for this claim (E-15). |
| DATA-1 stores, records, downward deps | `architecture/importGraph.ts` scans every module under `server/src` and holds each layer to a declared allowlist of the layers it may import; `app/notes/noteStore.ts` declares the store interface and the record; `persistence/notes/sqliteNoteStore.ts` is the only implementation; `app/notes/noteService.ts` sits between; `compose.ts` is the only module that names the implementation, which the scan asserts rather than describes | patterned (the weakest of 6 obligations); **data access lives behind a store interface** `proven`: `NoteStore` is an interface in `app/`, `sqliteNoteStore` is its only implementation and lives in `persistence/`, and no module under `routes/` or `app/` may import `persistence/` at all. Enforced rather than described: the rule is a scan over every module in the tree, and planting the import into the real `routes/notes.ts` turns two tests red. A type-only import is counted, with its own red proof, because a layer that needs another's types to compile cannot be changed without it; **records are pure data holders with zero behaviour** `proven`: `Note` is a `Readonly<{...}>` type alias of five fields. It is a TYPE and not a class, so it cannot carry a method: there is no object to hang one on. Unrepresentable rather than refused, which is this edition's answer wherever the platform allows it; **an endpoint never touches a store, and calls one service method** `patterned`: Two halves, and only one of them is now enforced. **A handler cannot touch a store**: `routes/` may import `app/` and `platform/` and nothing else, asserted by the import-graph scan and red-proven against the real route module. **A handler calls ONE service method** is still held by reading: each of the four does, and nothing refuses a fifth that calls two, or that calls a service method and then reaches for another. trigger for `proven`: an AST assertion over the handler bodies in `routes/`, which is the shape that would close it and which was deliberately not attempted in this pass, because a positional check over call expressions is the kind of brittle predicate E-2 records the cost of; **dependencies flow downward only** `proven`: Declared per layer and enforced over every module: `app` may import `app` and `platform`; `routes` and `persistence` may import `app` and `platform`; `platform` may import only itself; `architecture` may not import the layers it scans; `main.ts` may reach only `compose.ts`. Four upward edges are red-proven, including a scan importing the store it scans and the entrypoint reaching past the composition. A layer no rule describes is a violation rather than a default, and a rule whose layer imports nothing is reported as the residue of a rename; **cross-boundary collaborators are interfaces registered at the composition root** `proven`: `composeApp` is the only module in the server that names `sqliteNoteStore`, `openDatabase` or `noteService`, and it hands the service to the route surface as a parameter. That was true by construction and is now a fact the suite asserts: the set of modules outside `persistence/` with an edge into it is compared against `{compose.ts}` exactly, so the composition root's privilege is measured rather than assumed; **the enumeration reaches every endpoint, record and store** `proven`: Built. `scanImportGraph` walks `server/src` and reads 51 edges in the tree as it stands. It reports finding no imports at all as a violation, so it cannot go silently vacuous the way E-11 and E-92 describe, and `importEdges` is a second view a test names its expectations against: eleven specific layer pairs are asserted present, because a green scan is the same output whether it read fifty edges or none. Tests are excluded from the walk with a stated reason: a test imports across every layer by design, and holding test files to the production graph makes the rule unusable and the pressure is to delete it; Re-measured 2026-07-27 and lifted from `owed` to `patterned`. The import-graph scan the row's own trigger named is built, and it closed four of the six obligations. **The rule set is ports and adapters, not a stack**, and the graph was read before the rules were written for that reason: `persistence/` imports `app/` because `app/` owns the store interface, which under a tower reading is an upward edge and forbidden. A rule set derived from the claim's words rather than from the design would have failed this tree on its correct edges. The row's sixth obligation also said the scan was 'this row's whole distance from proven', and that was wrong: the third obligation's second half, an endpoint calling ONE service method, is a second distance and no scan covers it. Findings: E-99. |
| DATA-2 bounded reads | `architecture/statementSurface.ts` reads every `STATEMENTS` set in the tree statically and asserts each SELECT carries a LIMIT and no statement pages by OFFSET; `sqliteNoteStore.list` pages by keyset over `(created_at_utc, id)` and clamps to `MAX_PAGE_SIZE` at the store; the cursor crosses the wire opaque | proven (the weakest of 4 obligations); **list reads are paginated by keyset, not offset** `proven`: The SQL carries no OFFSET and cannot: `STATEMENTS.listAfter` is a row comparison `(created_at_utc, id) > (?, ?)`, and the whole statement set is one frozen object a test reads. The cursor carries both ordering columns, so equal timestamps neither drop a row nor serve one twice; the sibling's index history is the reason the tiebreak is not optional. Now enforced as well as true: the scan refuses any statement containing OFFSET, with a red proof, so the keyset form is the only paging this store can express; **the bound is explicit and lives at the store** `proven`: `MAX_PAGE_SIZE` is a constant on the interface and `boundedLimit` clamps there, not at the route. A bound applied only at a handler is a bound on that handler, and the next caller of the store gets none, which is what E-48 measured when the sibling's clamp was removed and nothing noticed. Proven behaviourally as of 2026-07-27 rather than by reading: asking the store for more than `MAX_PAGE_SIZE` returns exactly the maximum, and asking for 0, -1 or 1.5 returns the default rather than nothing; **reads run untracked** `proven`: Unrepresentable rather than enforced: this engine has no change tracker to disable. `node:sqlite` returns plain row objects, so there is no identity map, no tracked graph and no tracked-read-only-query to forbid. Recorded as satisfied by the platform rather than by a mechanism, so nobody builds a guard whose subject cannot occur; **every list read is bounded, not merely every list endpoint** `proven`: Built, and it covers the whole surface rather than one method. `scanStatementSurface` reads every `STATEMENTS` object declared anywhere in `server/src`, statically, and refuses a SELECT with no LIMIT. A second list method added without a clamp is caught the moment it is written. Three things make the enumeration real rather than nominal: a statement whose value is not a plain literal is reported by name rather than skipped, because a query assembled at runtime defeats the chokepoint whatever it turns out to say; SQL handed to `prepare` or `exec` outside the set is a violation with two named exemptions carrying their reasons; and the scan reports finding NO statements as a violation of its own, so a renamed directory cannot make it silently vacuous. Red proofs for every branch, plus a live plant against the shipped store; Re-measured 2026-07-27 and lifted from `owed` to `proven`. The assertion over `STATEMENTS` that the row's own trigger named is built, and it is stronger than the trigger asked for: it also refuses OFFSET, refuses a statement it cannot read statically, and refuses SQL that reaches the engine outside the declared set at all, so the set is the whole surface rather than the queries whose author chose to put them there. Its first run reported a real defect in this edition: `STATEMENTS.get` carried no LIMIT. The key already bounded that read to one row, so the read was never unbounded; what was missing is that the bound lived in the SCHEMA and the scan reads statements. `LIMIT 1` was added rather than teaching the scan the primary key, which would be a second copy of the migration living in an architecture test. Findings: E-99. |
| DATA-3 two-layer at-most-once | not built | owed; trigger: first idempotent external side effect. |
| DATA-4 cross-store reconcile | not built | owed; trigger: first cross-store sequence. |
| DATA-5 fail-fast mandatory config | the startup half IS built and proven: `resolveSettings` runs as `composeApp`'s default parameter, before `createApp`, which cannot be constructed without the frozen result, so the ordering is a compile error rather than a convention; every mandatory key absent, mistyped, empty or undeclared is refused by name and ALL problems are reported at once; the runtime-refusal idiom is `requireZone`, tested | owed (the weakest of 2 obligations); **mandatory configuration halts startup, naming the missing key** `proven`: `resolveSettings` runs as `composeApp`'s default parameter, before `createApp`, which cannot be constructed without the frozen result, so the ordering is a compile error rather than a convention; every mandatory key absent, mistyped, empty or undeclared is refused by name, and all problems are reported at once; **an operation missing runtime context refuses rather than guessing** `owed`: `requireZone` is the idiom and it is tested, but there is no principal here and therefore no stored timezone, so it ships with a test and no caller (trigger: the first seam that needs runtime context, which is the credential mint); trigger: the first seam that needs runtime context, which is the credential mint (SEC-4, TEN-6), since a principal is what carries a timezone. `owed` on the S-8 reasoning: the runtime-refusal half has no seam in this edition to refuse at, because there is no principal and therefore no stored timezone, so `requireZone` ships with a test and no caller. The sibling has a live one. The claim says "an error naming the missing key", singular; this reports all of them, because a validator that stops at the first turns a three-key misconfiguration into three failed deploys, and the sibling takes the singular reading (E-14). The undeclared-key refusal is the closure obligation with the opposite remedy to the request surface's, which is S-10. |
| DATA-6 migrate and exit | not built | owed; trigger: the next edition build pass (a small host change; the mechanism is two facts on the composed host). |
| DATA-7 ruled retention and disposal | not built | owed; trigger: the first PII-bearing entity in an edition project, or the first contractual deletion clause, whichever lands first. |
| DATA-8 a state change and its event commit together or not at all | not built | owed; trigger: the first event publish in an edition project (arrives with the first broker, alongside RES-5). |
| DATA-9 a stale write is refused, never silently applied | not built | owed; trigger: the next edition build pass (rides TEN-4's interceptor work; the token and the refusal live in the same save pipeline the tenancy guard already owns). |
| DATA-10 everything that grows has a registered sweep | not built | owed; trigger: the first append-only operational table in an edition project (DATA-8's outbox is the likely first, with RES-5's dead-letter quarantine close behind). |
| DATA-11 a migration never breaks the release running beside it | not built | owed; trigger: the first production deployment with rolling replacement; the lint half can land at the next edition build pass, ahead of the compatibility job. |
| CFG-1 operational settings are configuration, not code | `SETTINGS_SPEC` in `platform/settings.ts` is the declared configuration surface and the only reader of a config file, the secret store or `process.env`; `scanConfigurationSurface` parses `server/src`, `tools/`, `.github/workflows/`, `client-web/tools/` and `scripts/` and reports operational-setting literals (provider endpoints, model ids), any `process.env` read outside the seam, and any script literal that duplicates a committed configuration value; a lint confines `process.env` to the seam | patterned (the weakest of 3 obligations); **the declared configuration surface is the only reader of config, secrets or the environment** `proven`: `SETTINGS_SPEC` in `platform/settings.ts` is the one reader, and a lint confines `process.env` to that seam. The ambient environment is closed by being made a DECLARED channel rather than banned: an env var may only override a key the spec declares, under a name derived from that key (A-6). The source checks run over the parsed AST rather than over text, because a text scan reported the comments explaining its own bans (E-5's second instance). Widened 2026-07-27, and the widening is the point rather than a detail: the closure ran over the two sources that are parsed JSON objects and never over the environment itself, so a variable aimed at this channel that matched no declared key resolved in silence (E-97). A name derived from a key stops an undeclared key being READ; it says nothing about what the environment carries. Now refused by name at startup, with a red proof and a non-vacuity test that `PATH` and `HOME` are ignored. The environment confinement check still applies to `source` surfaces only, which is deliberate: a script's job is to SET the environment for a process it starts, and `scripts/e2e.ts` is the live instance; **no operational-setting literal appears in host source or a shipped script** `proven`: `scanConfigurationSurface` parses `server/src`, `tools/`, `.github/workflows/`, `client-web/tools/` and `scripts/` and reports provider endpoints and model ids. The surface list is now asserted to REACH real files rather than assumed to: `scannedFiles()` is a second view over the same walk and a test names one real file per surface, because a green scan is the same output whether the walk reached five surfaces or none (E-92). Red-correct: removing the `scripts` surface fails that test alone, while the green scan goes on reporting ok; **a shipped script reads a committed configuration value rather than duplicating it** `patterned`: Red-correct: a literal `'kernel-api'` planted into `client-web/tools/harness/main.ts` is reported by name, naming the key it duplicates. It is `patterned` and not `proven` because the candidate set is filtered twice and BOTH filters have live instances under them. Values shorter than eight characters are excluded by a stated argument (a check that fires on `info` is a check somebody deletes), and non-string JSON values are never candidates at all, which is not a stated argument but a consequence of the type test. `http.port` is 5080, a number, so it is excluded twice over, and both the harness and the smoke tool carry `?? 'http://localhost:5080'` (E-78). That is a duplicated committed value AND the silent default CFG-1's own text calls out one claim over. The row read `proven` while its own note admitted the length floor, which is the overstatement this re-measurement was for. First live catch outside a fixture, 2026-07-27: adding `scripts/` to the surfaces reported `scripts/e2e.ts` duplicating the committed value of `http.host` on the first run, naming the key. The duplication was real and was removed rather than exempted; Re-measured 2026-07-27 (node Phase A) and LOWERED from `proven`. The row's own note already admitted the duplication check's length floor while the status said `proven`, and the re-measurement found two live instances under it: the shared harness and smoke tools both default to a hardcoded port that duplicates `config/settings.json` (E-78). Not repaired here, because both files are COMPOSED SHARED files and the fix is the same cross-edition decision the sibling's `mintToken.mjs` needs (E-75); `edition.json` is now the obvious home for it, since DB2 established that pattern and it sits at the same relative path in both editions. Findings: A-6, E-5, E-13, E-78. Re-measured 2026-07-27 with the e2e orchestrator: `scripts/` was added to the scanned surfaces in the same change that created the first file in it, and the scan reported a real duplication in that file on its first run. Two further findings came out of using the channel rather than reading it: E-96 on a default that equals the value it defaults for, and E-97 on the environment channel being closed at one end only. |
| CON-1 one wire dialect | none built. Route response shapes are declared per route in `src/routes/`, and nothing compares them against each other or against a dialect. | owed (the weakest of 4 obligations); **one host-level serialization and error configuration point** `owed`: unbuilt AND unmet. `app.ts` registers no `setErrorHandler` and no shared error serializer, so each route decides its own error shape and there is nothing central for a scan to assert against. Trigger: a host-level error handler, which is the artifact this obligation is about; **no endpoint invents its own error shape** `owed`: unbuilt, and VIOLATED in shipped code: the notes route declares `404: { type: 'object', properties: { error: { type: 'string' } } }` and sends `{ error: 'not found' }`, which is not RFC 9457 problem details (E-83). Trigger: the error handler above, then a scan over the composed route table's declared response shapes, which is cheap here because Fastify keeps the schema as a value on the route; **enums cross the wire as closed string sets** `owed`: unbuilt, and this is the one clause whose subject genuinely does not exist yet: no route declares an enum. Trigger: the first enum on the wire. This trigger was the WHOLE row's trigger until this round, which is how the two violated clauses above stayed invisible; **identifiers are opaque strings** `owed`: unbuilt, and no longer violated: `sqliteNoteStore.ts` mints `randomUUID()`, so the substance of this obligation is met in shipped code while nothing at all holds it there. It read `VIOLATED in shipped code` for four commits after the counter was deleted (E-106). Half the stated trigger fired and it was the half that does not decide the status: the opaque id at the store boundary arrived, the scan did not, so a later store returning a counter, or a second store minting its own ids, is invisible here and would be reported by nothing. Adjacent to but not covered by SEC-7, which binds on anonymous surfaces and this route is gated. trigger: a scan over the composed route table's declared response shapes, which is the same instrument the first two obligations wait on; Measured in this edition's own tree 2026-07-27 (node Phase A, F4), and the row was NOT correctly waiting. Its trigger read `the first enum on the wire`, which names the third of the claim's four clauses; the other three do not wait for an enum, and TWO OF THEM WERE VIOLATED IN SHIPPED CODE (E-83): `src/routes/notes.ts` declared and sent a bespoke `{ error: 'not found' }` 404 rather than problem details, with no host-level error handler for a route to conform to, and minted identifiers as `String(notes.size + 1)`, a dense sequential counter. A live violation is worse than an unbuilt guard, and this record showed neither. One of the two is now repaired: the store mints `randomUUID()` at its boundary, and this row went on describing the counter for four commits after it was deleted (E-106). The 404 shape is still live and still unguarded, which is why the note keeps saying so. Findings: E-83, E-106. |
| CON-2 contract parity fixture | `client-web/src/data/__fixtures__/note-contract.fixture.json` is the one physical fixture; `noteContract.test.ts` asserts the client's `CreateNote`, `Note` and `NoteList` types against it, and `server/src/architecture/contractParity.ts` plus its test assert the server's declared route schemas and its emitted responses against the same file | owed (the weakest of 5 obligations); **exactly one corpus, physically one file** `proven`: One fixture file inside the edition, read by the client test natively and by the server test through a relative path derived from `import.meta.dirname`. No copy exists inside the edition, so the claim's primary mechanism class holds here and the sanctioned materializer alternative is not needed: the precondition (both consumers under one root that travels together) is satisfied, because a seeded project's root holds both. Planted 2026-07-27 at 5dedb47 by removing the fixture: the server test file fails to load with ENOENT, so the read cannot go vacuous. Between EDITIONS the file is shared-tier and `compose.mjs --check` is the drift check, which is the alternative and is a different question from this one; **the consumer side asserts its types against the committed fixture** `proven`: `noteContract.test.ts` reads the fixture off disk and checks every property name of the three client contract types against it, so a rename on the client fails the client build; **the producer side asserts its declared contracts against the same fixture** `patterned`: `contractParityFindings` is a pure function over the fixture and the composed route table, so eleven refusal tests assert that a defect is REPORTED rather than that the shipped tree is clean (the E-79 shape). Planted 2026-07-27 at 5dedb47: renaming `items` to `notes` in the list response turns exactly one test red naming both field sets, and nothing else in the suite notices. Every contract in the fixture must be bound by some route, so a corpus entry the server never answers for is red; that is half of what the sibling's equivalent obligation records as owed under E-60, and it flows back. Per-seam and not proven, measured the same day: because `noteResponse` is bound by two routes, DELETING the binding from `GET /notes/:noteId` leaves all 172 tests green, so a route can stop being pinned without anything reporting it. Trigger for the last of it: a rule deciding which response schemas MUST carry a binding, which needs a way to say that `/health` and the 404 shape are not hand-mirrored contracts; **what the server emits matches what it declares** `patterned`: Five request-level tests drive the composed app through `CreateAppSeams.authenticate` and assert the emitted field sets against the same fixture, plus the cursor walk's totality and the 403 for an insufficient credential. Built because the scan above pins the declaration only, and here a declaration and an emission are two objects: planted 2026-07-27 at 5dedb47, changing the handler's `return { items, nextCursor }` to `return { notes: items, nextCursor }` left the parity scan, tsc and eslint all clean while the server sent `{"nextCursor":null}` with the list gone from the wire (E-85). Per-seam: each new route carrying a mirrored contract owes its own emission test. The sibling has no equivalent obligation and does not need one, because its handler returns the typed record the contract IS; **every hand-mirrored contract is pinned by the corpus** `owed`: The fixture-to-route direction binds (a corpus entry nothing answers for is reported), and the route-to-fixture direction does not: a fourth hand-mirrored contract added to both sides and never written into the fixture is invisible to every test in both trees, because a register cannot report what was never written into it. Trigger: a rule that decides membership of `hand-mirrored contract` from the code rather than from the register, which is the same trigger the sibling's row carries and is unbuilt in both editions; Producer half built 2026-07-27. E-82 recorded that nothing in `server/src` read the fixture, so the client was asserting against a second copy of its own beliefs; asking the server was the whole of the work and the answer was a live violation. `GET /notes` declared NO response schema at all and returned `{notes: [...]}` while the fixture and the client both say `{items, nextCursor}`, so the drift was not merely undetected, it was unreadable: a route that declares no response shape has nothing for a fixture to pin. Repaired in the same pass, with `cursor` added to the query schema because `removeAdditional` had been silently stripping the one the client sends, so every paged read answered page one. Findings: E-82, E-85, E-86. |
| CON-3 read completeness for action-bearing surfaces | not built | owed; trigger: the first edition project with a restorable session (the kernel exemplar has none). |
| CON-4 a breaking change is detected by machine, approved by human | not built | owed; trigger: the first published external contract (a versioned API consumed outside the repo, a partner surface, or the first schema registry; the composed client in the same repo is CON-2's territory, not a published contract). |
| RT-1 realtime discipline | not built | owed; trigger: first realtime slice; the promotion set is the hub arch test, MaximumReceiveMessageSize, the createHub client seam, and the socket harness scenario. |
| SRV-1 one deployable unit | not built | owed; trigger: the next edition build pass. Applies to web products; an API-only project re-rules it in its deltas file at adoption. |
| RES-1 every outbound call is bounded, and retries are earned | not built | owed; trigger: the first external service dependency in an edition project. Database session bounds are RES-6's, carved out so this claim stays about the chokepoint shape. |
| RES-2 no unbounded queue, pool, or backlog in the process | not built | owed; trigger: the first background worker or in-process queue in an edition project. |
| RES-3 the host dies cleanly, and there is a test that kills it | not built | owed; trigger: the first deployed edition host (rides SEC-9's deployment-edge work; the drain budget is a CFG-1 operational setting ruled against the platform's grace period). |
| RES-4 liveness answers alone; readiness answers for its dependencies | not built | owed; trigger: the first deployed edition host (lands with RES-3 and SEC-9; the health root SEC-1 already allowlists is the surface this claim splits in two). |
| RES-5 poison is quarantined at a counted limit, never retried forever, never dropped | not built | owed; trigger: the first message broker in an edition project (the transactional outbox relay, DATA-8, is the likely first consumer). |
| RES-6 every database session carries four finite bounds, each proven where it lives | not built | owed; trigger: the next edition build pass. Everything here runs on the existing integration tier against the real engine, so proven is reachable immediately; nothing waits on a new dependency. |
| PERF-1 a hot operation's IO is counted, budgeted, and gated | not built | owed; trigger: the next edition build pass (the v1 exemplar's list endpoint is the first designated seam; the interceptor rides the same seam TEN-4's stamps use). |
| PERF-2 a designated hot path's allocations are budgeted per operation | not built | owed; trigger: the first designated hot path in an edition project; designation before need fails the YAGNI gate, so the family expects most projects to hold zero designations for a long time, honestly. |
| PERF-3 a designated algorithmic seam proves its scaling with a counted ratio | not built | owed; trigger: the first hand-written algorithm over unbounded input in an edition project; library calls and database queries are not designations (the library's complexity is its documentation's promise, and query shape is PERF-1's and DATA-2's). |
| PERF-4 no sync IO and no whole-payload buffering on the serving path | not built | owed; trigger: the next edition build pass (a BannedApiAnalyzers configuration plus one host assertion; the client side already carries the idiom under UI-2's lint discipline). |
| PERF-5 every cache is bounded and every entry expires | not built | owed; trigger: the first cache in an edition project; the claim deliberately does not mandate caching anywhere, it prices caching where chosen. |
| PERF-6 wall time is never asserted raw | not built | owed; trigger: the first wall-time benchmark in an edition project; the lint half can land at the next edition build pass, ahead of any benchmark existing, since its job is to keep the first timing assert out. |
| MOD-1 module co-location | not built | owed; trigger: the second module. |
| MOD-2 deterministic naming and placement | `namingPlacement.test.ts` walks the client source tree and asserts component files are PascalCase under `components/` or a module; eslint `no-restricted-imports` confines the token import | owed (the weakest of 3 obligations); **client component files are named and placed deterministically** `proven`: Red-correct for a kebab-cased component file. The walk is recursive over the client source root and exempts the entry point by exact path rather than by suffix, so a `Domain.tsx` is not accidentally exempted; **the server tree has its own naming and placement rule** `owed`: unbuilt, and not measured by this round. The client walk stops at `client-web/src`; nothing asserts placement for `server/src`. Trigger: a walk over the server tree, which is F4's missing-server-half question for this claim; **artifact kinds form a closed registry, and a new kind is a deliberate kernel edit** `owed`: unbuilt. Nothing enumerates the kinds of artifact a module may contain, so a new kind arrives without a decision. Trigger: an artifact-kind registry the placement walk reads; Measured in this edition's own tree 2026-07-27 (node Phase A, F2), not inferred from the sibling. Red-correct: a `notes-panel.tsx` beside `NotesScreen.tsx` turns `component files are PascalCase under components/ or a module` red. Two obligations are unmeasured or unbuilt, and they are named rather than folded into the client half's status. |
| DOC-1 documentation lifecycle | `tools/docs-lint.mjs`: front matter, placement, work-slice ids, the DEC-1 provenance field, the TEN-5 and DEP-1 couplings, the archived-citation resolver and the MET-08 dash check | owed (the weakest of 6 obligations); **every markdown declares a kind, and the kind matches the single legal root it lives in** `proven`: Red-correct here for a `kind: notes` document in docs/claims/. The walk reaches every markdown in the tree. `bin`, `obj` and `dist` are skipped only where a project manifest sits beside them, so they are build OUTPUT rather than a name (E-32 repaired): authored markdown under a `docs/bin/` is now caught, and the identical file under a real project's `bin/` is still ignored. Both directions are controls. `proven` rather than `patterned` since 2026-07-27: E-32 was this obligation's only recorded residue, and the walk is now total over authored files. `node_modules` and `.git` stay unconditional because neither is ever authored; **no markdown outside the legal roots, closure over the edition tree** `proven`: Red-correct here for a `server/handover.md`. The walk reaches every markdown in the tree. `bin`, `obj` and `dist` are skipped only where a project manifest sits beside them, so they are build OUTPUT rather than a name (E-32 repaired): authored markdown under a `docs/bin/` is now caught, and the identical file under a real project's `bin/` is still ignored. Both directions are controls. `proven` rather than `patterned` since 2026-07-27: E-32 was this obligation's only recorded residue, and the walk is now total over authored files. `node_modules` and `.git` stay unconditional because neither is ever authored; **working documents carry a slice id** `patterned`: Red-correct here for a work doc with no slice key. The value is never validated (`slice: whenever` passes) and the check keys on the folder name, so a document declaring itself working in another root owes nothing (E-33); **status transitions move only forward** `owed`: unbuilt. The guard is a snapshot lint holding no prior value, so forward and backward transitions on the same file produce byte-identical clean output (E-33). Trigger: a reader comparing a declared status against its previous committed value, which makes this tool depend on git in a tree that may not have it; **archived documents are not cited as authority** `latent`: The resolver is built: it walks every markdown link, resolves it relative or root-anchored, and fails a LIVE document citing an ARCHIVED one. It has never run against a real citation HERE, because this tree contains no inter-document markdown link at all. Built and unexercised is `latent`, and it is a different situation from the sibling's, where the resolver has one real link to read; **the docs-lint gate runs in the CI loop** `owed`: The edition's own `ci.yml` has a docs-lint job, and that file is a TEMPLATE: it is not executed in this repository, so deleting its only invocation of the guard is reported by nothing. What actually runs docs-lint here is the repository's `kernel.yml`, over both editions in a matrix, which this row does not ship (E-39). Trigger: instantiation arming the loop; Measured in this edition's own tree 2026-07-27 (node Phase A, F5). The row's trigger said the mechanism was composed from the shared tier and what was owed was the MEASUREMENT. Taken: three plants, all red-correct here (a wrong kind in docs/claims/, a work doc with no slice id, and markdown outside the legal roots). Two obligations differ from the sibling's for reasons that are this edition's own: this tree contains ZERO inter-document markdown links, so the archived-citation resolver has no subject and is `latent` rather than `patterned`. Findings: E-32 (repaired), E-33. |
| DEC-1 a module's behaviour traces to a decision | `docs-lint.mjs` requires a `provenance` field on every document under `docs/decisions/`, naming the story, epic or claim served | owed (the weakest of 3 obligations); **a decision traces upward to what it serves** `proven`: Red-correct here: a `docs/decisions/d-999-probe.md` with no `provenance` field fails docs-lint with a sentence naming DEC-1. Every document in the folder is walked, so the check is total over the decision set; **behaviour traces downward to the decision that produced it** `owed`: unbuilt. Nothing links a module or a guard back to the decision it realizes, so a decision can be superseded with the code it produced left in place. Trigger: the first regeneration of a module from its decisions, which is what gives the downward link a consumer; **artifacts are produced in decision order** `owed`: unbuilt. Trigger: the next acceptance test, which is where the ordering becomes observable; Measured in this edition's own tree 2026-07-27 (node Phase A, F5). Lifted out of a single trigger line whose own text already admitted the upward lint was built and proven, while the row carried no mechanism at all. Red-correct here for a decision with no provenance field. The row stays `owed` on the downward link and the artifact-order check, both genuinely unbuilt, and both now stated as obligations so the built half is visible. |
| UI-1 token source lockstep | `src/theme/tokens.ts` is the sole token source, transcribed from `design/prototype/_ds/colors_and_type.css`; `tokenCoverage.test.ts` asserts every CSS variable in the export has a matching token and that the export defines more than 50 distinct variables; eslint `no-restricted-imports` plus `namingPlacement.test.ts` confine the import to `src/components` | latent; Measured in this edition's own tree 2026-07-27 (node Phase A, F2), not inferred from the sibling. Red-correct: deleting one token turns `every design-system variable has a matching token` red. `latent` rather than `proven` because the artifact it locks against is a KERNEL PLACEHOLDER: the export's own header says to replace its values with a real design export at instantiation. The lockstep mechanism is built and runs; it has never run against a real locked prototype, which is what this claim is about. Trigger: the first design lock in a seeded project. |
| UI-2 literal visual values are lint errors | eslint `no-restricted-syntax` error-tier rules banning literal visual values in the client, plus `ui2LintExtent.test.ts` asserting what those rules reach | patterned (the weakest of 2 obligations); **error-tier rules ban literal visual values and gate the client verification chain** `proven`: Red-correct with two distinct rule messages (hex colour, raw dimension), and `verify` runs `tsc`, `vitest` and `eslint` so the ban is a gate rather than advice; **coverage of every axis and every syntactic form a literal can take** `patterned`: 49 extent cases, of which fourteen are holes recorded as PASSING controls: negative literals and several ungated axes. Each new axis or syntactic form owes its own entry, and closing one breaks its control by design (E-31); Measured in this edition's own tree 2026-07-27 (node Phase A, F2), not inferred from the sibling. Red-correct: a `style={{ color: '#ff0000', padding: '12px' }}` on a screen produces two eslint errors, both naming UI-2, and the lint gates `npm run verify`. The extent test carries 49 cases including FOURTEEN holes held as PASSING controls, so closing one turns that test red and has to be argued rather than discovered silently (E-31). Findings: E-31. |
| UI-3 screens compose primitives only | eslint `no-restricted-imports` bans the whole theme layer outside `src/components`, plus `namingPlacement.test.ts`'s zero-dependency source scan of the same rule | owed (the weakest of 3 obligations); **the primitive component layer is the sole consumer of theme tokens** `proven`: Red-correct here twice over, by a source scan and by a lint rule, both naming UI-3. The lint bans the whole theme layer including a re-export barrel, not just the tokens file, which is the hole a narrower rule would leave; **screens create no style sheets and use no raw platform text elements** `owed`: unbuilt. The ban is on the token IMPORT; a screen writing raw markup or its own stylesheet imports nothing and is invisible to both mechanisms. Trigger: a scan over screen markup for raw platform elements, which needs a primitive registry to compare against; **client business logic lives in separately testable services, hooks and reducers** `latent`: The structure exists (`src/data/notesRepo.ts` is a separately testable service with its own tests) but nothing asserts that logic STAYS there, so the arrangement is a convention the tree currently follows rather than a mechanism; Measured in this edition's own tree 2026-07-27 (node Phase A, F5). Trigger fired: red-correct here for a screen importing `../../theme/tokens.ts`, caught TWICE and by two independent mechanisms, the test naming UI-3 and the lint naming UI-3. Two mechanisms for one obligation is worth recording rather than deduplicating: the lint gates `verify` and the test gates a bare `npm test`, so neither alone covers both ways a developer runs the tree. |
| UI-4 fidelity ledger | `NotesScreen.fidelity.test.tsx`: a hand-written LEDGER of atoms, rendered across the populated and empty branches, asserted for exhaustiveness (every ledger atom renders) and de-fabrication (nothing renders outside the ledger) | owed (the weakest of 3 obligations); **every ledger atom renders (exhaustiveness)** `patterned`: Both branches are rendered, populated and empty, so a conditional atom cannot hide. The ledger is hand-written, so each new atom owes its own entry; **nothing renders beyond the ledger (de-fabrication)** `owed`: Red-correct only for a fabrication that MARKS ITSELF with `data-atom`. An unmarked element is invisible to the collector and therefore to the claim (E-68). Trigger: a subject drawn from the rendered tree rather than from the marker the code applies to itself; **the ledger is exported from the locked prototype, not hand-maintained** `owed`: unbuilt. The LEDGER constant is written by hand in the test, so it records what somebody believed the prototype contained. Trigger: the first design lock, which gives the export something to derive from; Measured in this edition's own tree 2026-07-27 (node Phase A, F2), not inferred from the sibling. Red-correct for a fabricated `<div data-atom="promo-banner" />`. The row stays `owed` on its weakest obligation and the reason is E-68, inherited byte-for-byte because the guard is a composed shared file: the de-fabrication assertion collects `querySelectorAll('[data-atom]')`, so its SUBJECT is the set of elements that already declare themselves part of the ledger's vocabulary. A fabricated `<p>` carrying no marker renders green, and passes tsc and eslint too. This is the guard whose entire stated purpose is catching what an AI invented, and invented markup is the least likely to volunteer a marker. Findings: E-68. |
| UI-5 thin UI over tested services, composed entrypoint exercised | eslint `no-restricted-imports` confines construction of the transport and the data services to `src/main.tsx`; `tools/smoke/composedApp.smoke.ts` boots the composed entrypoint | owed (the weakest of 4 obligations); **modules outside the composition root import neither the transport nor the data services** `proven`: Red-correct here, by lint, naming UI-5 and naming `src/main.tsx` as the one permitted constructor. Caught by `npm run verify` and not by `npm test`; **the client UI holds no business or transport logic of its own** `owed`: unbuilt. The import ban stops a screen CONSTRUCTING a client; it does nothing about a screen that reimplements the logic inline with `fetch`, which imports nothing restricted. This is E-70's shape: an import ban cannot see transport logic that imports nothing. Trigger: a scan for transport primitives in module code; **the composed entrypoint is exercised against a running server** `patterned`: Lifted from `latent`, four commits late. The text read `there is no orchestrator to boot one and no CI job that calls it`, and `scripts/e2e.ts` had been booting one and running `npm run smoke` against it since 3ac6f25, green on every run, with a `node-e2e` job calling it (E-106). Non-vacuity measured rather than assumed, at 3087e57: with the stack down, the smoke fails rather than passing. Re-measured 2026-07-28 after E-107: the fallback base URL is gone, so an unset `VITE_API_BASE_URL` now throws a sentence naming the orchestrator instead of dialling the committed port, and the orchestrator hands this tier an OS-assigned port, so neither an absent environment nor a stray process on the default can satisfy it. That is E-96's repair reaching this tier as well as the harness. `patterned` and not `proven` by the claim's own weakening note, which makes the smoke per-seam at one line per primary flow: two flows are covered, the list load and the create-then-refresh, against the four methods the harness completeness audit enumerates; **the smoke asserts the BUILT form, not the source form** `proven`: Lifted from `owed` on 2026-07-28, planted and proven. The smoke no longer imports `src/main.tsx`: it reads the BUILT `dist/index.html`, takes the module script it names and the body it ships, imports that bundle and mounts into that DOM, so every build-time substitution the assertions see is the one the artifact carries. Both e2e orchestrators now run `npm run build` and the smoke from ONE environment, in that order, inside the timed run rather than as a prerequisite, because a stale `dist/` is otherwise indistinguishable from a fresh one. Planted 2026-07-28 at 834b1de by handing the BUILD a different base URL than the smoke asserts against: red-correct, `Timed out waiting for the list request`, because the built client called the address it was built for. **Under the source-form smoke that same plant was green**, which is the whole of E-107 in one measurement. The default base URL is gone too (E-96): an absent `VITE_API_BASE_URL` now throws a sentence naming the orchestrator rather than falling back onto the committed port. Green in both editions after the change, against a real server each; Measured in this edition's own tree 2026-07-27 (node Phase A, F5). Trigger fired: red-correct here for a screen importing `../../api/client.ts`. Scored as a THIRD-OUTCOME result rather than a clean red, and the distinction is the finding: `npm test` stayed at 65 passed. Only the LINT caught it, so this obligation binds under `npm run verify` and in CI and does not bind for a developer running the test suite alone. That is not a defect in the rule, it is a fact about where the rule lives, and it was worth measuring rather than assuming the suite would report it. |
| DEP-1 dependency quarantine | npm exact pins with committed lockfiles restored by `npm ci` + `VERSIONS.md` (cooling-off window header, container-images section, kernel-provenance section, advisory-rule section) + docs-lint's ledger, override, manifest-completeness, image, cross-surface agreement, window-number and kernel-provenance checks, with the dependency surfaces declared in `edition.json` | owed (the weakest of 7 obligations); **every direct dependency is pinned exactly, dated, and restored in locked mode** `patterned`: Three plants, all red-correct here: a caret range on `fastify` (exactness), an unledgered `left-pad` at an exact pin (keyed on name AND version, so a row for another version vouches for nothing), and a `design/scratch/package.json` nobody declared, caught by the completeness sweep that runs over the whole tree rather than over the governed set. Residues, unchanged: source mapping is deletable unreported, and publish dates are shape-tested rather than parsed; **a transitive pin taken under the advisory rule carries its own ledger row** `proven`: The npm reader reads `overrides` alongside dependencies and devDependencies. Red-correct here: moving the client tier's `postcss` override off its ledgered version reports it by name and version. This edition carries a real advisory-rule pin, so the obligation has a live subject rather than a control; **the cooling-off window number lives in exactly one place** `proven`: The number is parsed from the VERSIONS.md header and every other assertion of it is compared against it. Proven on the strongest evidence a guard can have: on its first run it caught a live defect in THIS edition's ledger, which named the superseded window as a live one under a header declaring the current number (E-76, repaired); **every container image is pinned tag-plus-digest and ledgered** `latent`: The predicate is built and control-tested (a floating tag, a missing tag, a missing digest, a missing ledger row, and Docker Hub shorthand on a Dockerfile FROM line are all caught in `--self-test`). It has never run against a real reference here, because this edition has no container image, and since 2026-07-27 that is a property of the engine rather than of not having one. The engine is chosen and declared: `node:sqlite`, in process, shipping no image and opening no port, so its version IS the runtime version and the Runtime row is where it is pinned. This text and `VERSIONS.md` both read "no persistence engine has been chosen" for four commits after the store landed (E-106). trigger: an engine that ships an image, which this one does not, so the honest reading is that this obligation may stay latent in this edition permanently; **one image value across every surface naming an image** `latent`: Same mechanism and same reason. The agreement check runs over an empty sighting set here, so it is built and unexercised rather than failing to reach anything. Trigger: the second surface naming an image, which is the first moment the question exists; **kernel provenance is ledgered and moves only with the mechanisms** `latent`: The section's absence fails docs-lint. No real pin exists until the next instantiation fills one, so the comparison half has never run; **publish date is checked against the cooling-off window** `owed`: unbuilt in both editions, and the only obligation here that is. Dates are recorded by hand and shape-tested. Trigger: a publish-date check that reaches the registry, which no tool in this repository does; Measured in this edition's own tree 2026-07-27 (node Phase A, F4). The row's own note already said the right thing, that the mechanism is realized in the shared tier and composed here so what is owed is the MEASUREMENT and not the build. The measurement is taken: four plants, all red-correct in this tree. Two obligations differ from the sibling's for a reason that is this edition's own and not a weakness: there is no container image anywhere in the tree, and `VERSIONS.md` says so in prose, so the image obligations are `latent` rather than `patterned`. The row stays `owed` on the single obligation neither edition has built, which needs a registry lookup no tool here makes. The window-number obligation is `proven` on the strongest evidence available: it caught a LIVE defect in this edition's own ledger on its first run (E-76). |
| DEP-2 standing advisory sweep with a remediation clock | not built | owed; trigger: the next edition build pass, moved there from the first armed CI loop by adjudication ruling 9c on 2026-07-26, because the sweep is buildable in the shared tier today with no instantiation and no third-party action. the publish-date check DEP-1 names as its upgrade path rides the same job. |
| TEST-1 tier strategy | not built | owed; trigger: the first store, which is what gives the tiers something to be real about. |
| TEST-2 e2E harness | `scripts/e2e.ts` migrates a throwaway database, boots the real entrypoint on an unwritten port, mints two bearers whose permissions are READ from `POLICIES`, and drives both tools against it; `client-web/tools/harness/main.ts` drives the real `src/data/notesRepo.ts` and `src/api/client.ts` and emits NDJSON; `tools/smoke/composedApp.smoke.ts` boots the composed entrypoint against the same server; `redact.ts` scrubs the output; an `e2e` job in `ci.yml` runs the orchestrator | owed (the weakest of 5 obligations); **an out-of-process harness drives the real, unmodified client service layer** `proven`: Lifted from `latent`, which meant built and started by nothing. `scripts/e2e.ts` now starts it, an `e2e` job in `ci.yml` and a `node-e2e` job in `kernel.yml` start that, and the run has been executed against a booted server many times on 2026-07-27: **7 of 7 scenarios, exit 0**, at 494e323 plus this change. The harness imports the real repository and client modules rather than reimplementing them, and that property is guarded rather than merely true: the `service-method-coverage` scenario fails the run if the real `NotesRepo` methods are not the ones driven, which was watched happening in both directions today (all 4 driven when green, `uncovered service methods: list, get, remove` under a plant). The reach of that audit is the next obligation's business, not this one's; **the floor is complete: every public method of every client data service has a scenario** `owed`: The previous text read `unbuilt. The scenario list is hand-written`, and that was false about a file this edition ships: `main.ts` enumerates `Object.getOwnPropertyNames(NotesRepo.prototype)`, wraps every method to record what actually ran, and emits `service-method-coverage` as its own failing scenario line. It was watched doing exactly that on 2026-07-27, reporting `uncovered service methods: list, get, remove`. The status was right and the reason was wrong, which is E-87. Still `owed`, for the reason the sibling's row already carried and this one did not: the audit reads ONE class's prototype, so a second data service is invisible and a method declared as a class-field arrow function never enters the diffed set (E-67). Trigger: enumerate the class exports under `src/data/` rather than naming one, and enumerate instance members as well as prototype members; **the deterministic tier runs in CI** `patterned`: Lifted from `owed`. Two jobs invoke the orchestrator: `e2e` in this edition's `ci.yml`, which gates a seeded project, and `node-e2e` in the repository's own `kernel.yml`, which gates this repository. Both were added in the same change for the reason E-95 cost eight commits: a mechanism whose only runner is the template runs nowhere here. It needs no engine service container, because the store is `node:sqlite` and that is the runtime the job already installs. `patterned` and not `proven` for the same reason TEST-3's first obligation is: a job exists per tier because somebody wrote one, and nothing compares the set of mechanisms this edition ships against the set a job invokes. trigger: a check that enumerates this edition's mechanisms and asserts each is invoked by a job, which is the enumeration E-80 and E-95 are both instances of missing; **harness output redacts secrets** `patterned`: `redact.ts`, three arms, red-correct in this tree when the JWT arm is narrowed to match nothing (F2). Heuristic by shape, so each new secret shape owes its own arm; **the gated harness profile ships as the gate plus its refusal tests** `owed`: unbuilt, and unrecorded until now, which is the finding rather than the gap (E-98). The claim asks for a non-production server profile that re-binds provider ports only, refuses to boot outside Development or Testing, and changes no production code path, and its weakening note tells the kernel it owes the gate plus its refusal tests with no re-bindings. This edition has neither: no harness profile flag, no environment refusal, and no probe or test-only surface for the same gate to govern (PC-17). The row carried four obligations and none of them was this one, so an absence read exactly like the presence in the sibling, which ships the gate in `Program.cs` and its refusal tests in `HarnessProfileTests.cs` and records neither. trigger: it is buildable now and needs nothing else first, which is the phrasing E-84 warned about, so stated as an outcome instead: green means a boot outside the allowed environments REFUSES with the profile flag set, and a boot inside them succeeds, both asserted; Re-measured 2026-07-27 after the orchestrator landed. The row's first and third obligations both moved, and the row stays `owed` on the second, which is the roll-up rule doing its job rather than an oversight: the completeness audit reads one class's prototype and that is untouched by this pass. What moved: E-80 recorded that this edition shipped the harness and the smoke as npm scripts and nothing started either of them, and E-84 corrected its trigger to name a GREEN run rather than an artifact. The run is green, 7 of 7, exit 0, measured repeatedly against a booted server. E-91's disagreement about permission strings is closed at the source: the orchestrator reads them from `POLICIES` rather than carrying them, and planting the sibling's dot form back in reproduces the original 403 on every gated route. E-96 records the control that had to be repaired before this row could be believed. Findings: E-80, E-84, E-87, E-91, E-96, E-98. The kernel repository's own workflow runs the same orchestrator in a `node-e2e` job, which is a fact about where this edition is checked today rather than part of what it ships, so it is recorded here and not in the mechanism (E-105). |
| TEST-3 CI loop | `.github/workflows/ci.yml` (server, client, docs-lint and secret-scan jobs) plus `tools/gate-check.mjs`, which derives the required check names from the workflow and reads them back against the forge's branch protection | latent (the weakest of 2 obligations); **every mechanism the catalog names executes automatically in CI on every push and pull request** `patterned`: Five jobs run: the server suite, the client suite, the e2e tier, docs-lint and secret-scan, plus the gate-check self-test. **The residual this obligation carried is now a mechanism rather than a sentence.** Its previous text said nothing compares the set of mechanisms an edition ships against the set a job invokes, and named that as why E-80 and E-95 both happened. `kernel/tools/loop-check.mjs` is that comparison: for every executable under an edition's `tools/` and `scripts/`, it asks whether the edition's own `ci.yml` runs it and whether the repository's `kernel.yml` runs it, and it reports each answer separately because the same file can be right in one register and missing from the other. On its first live run it found `gate-check.mjs` in exactly that state in BOTH editions (E-103), which is the third instance of one shape and the reason the check exists rather than a fourth reminder. `patterned` and not `proven` because the enumeration is over shipped EXECUTABLES: a claim guard that is a test inside a suite, or an eslint rule inside a config, is covered only by whatever runs that suite or that config, and nothing compares the catalog's named mechanisms against either. **Both halves of that trigger are now mechanisms and the obligation is still `patterned`, for a residual that is now measured rather than imagined.** `docs-lint.mjs` reads this record's own `mechanism` field and refuses a file reference that resolves to nothing inside the edition (E-105, found by breaking the rule an hour after writing the check). `server/src/architecture/reachability.ts` answers the other half: from the roots this tier actually executes, derived from `package.json`'s own scripts plus the files vitest collects, every module must be reachable, and one that is not is reported by name. It found itself on its first run, before its test existed, which is the state it exists to detect. Planted twice at e6eda6a against the shipped tree: an orphan module is reported by name, and removing EVERY script that names `src/main.ts` reports the entrypoint (removing only one of the two does not, because the derivation is over all scripts and the property was redundantly satisfied, which is E-112). Nine refusal and permission tests besides, including a module reached only by a test, only through a dynamic import, and only through the extensionless form. Server tier 283 to 292.

**What is left, named with live instances rather than as a worry.** Reachability here is at MODULE granularity, so an unused EXPORT inside a reached module is invisible, and there are four of them in this tier right now: `assertImportGraph`, `assertStatementSurface`, `assertConfigurationSurface` and `assertReachability` are exported, read as the enforcement path, and are called by nothing (E-113). The scans they wrap do bind, because each test asserts the scan directly. No conformance row names any of the four, which was checked rather than assumed. The client tiers have their own package, runner and roots and are not scanned; a mechanism that is an eslint rule inside a config is covered only by whatever runs that config; and the sibling needs a different instrument entirely, because its modules are C# and its roots are test assemblies. trigger for `proven`: export-level reachability, plus the same instrument pointed at the client tiers; **the loop gates merges, armed at instantiation, with the arming step carried and verified** `latent`: `gate-check.mjs` derives the required check names from the workflow itself, reads both rulesets and classic protection, catches ruleset evaluate mode (which reports rules and blocks nothing) and requires a pull request. Its `--self-test` runs in this edition's CI. The readback itself has never executed against a real forge, so the half that would prove the gate is ARMED is built and unexercised; Measured in this edition's own tree 2026-07-27 (node Phase A, F3). Lifted from `owed`, where the row read `trigger: the first instantiation` and recorded no mechanism, while `ci.yml` and `gate-check.mjs` were both present and the workflow already runs `gate-check --self-test` (6 caught, 7 ignored). `latent` and not higher for the same reason as the sibling, established here rather than inherited: the readback has never run against a real forge, which needs a token carrying repository administration read. Findings: E-24, E-65. Re-measured 2026-07-27: `gate-check --self-test` now runs in this repository's own workflow, which it never had (E-103), and the loop check that found it runs on every push. Findings: E-103. The check that asks it, `loop-check.mjs`, lives in the kernel repository rather than in this edition, and so does one of the two registers it reads, so both are recorded here rather than in the mechanism: a seeded project has neither file and its TEST-3 mechanism is its own workflow (E-105). |
| TEST-4 real-runtime boot proof | not built | owed; trigger: the next edition build pass. UI-5's composed smoke already proves entrypoint wiring in-process; the real-boot script assertion and built-form pinning are the promotion set that completes it. |
| HUM-1 irreversible surfaces get a human turn, unconditionally | `edition.json`'s `irreversibleSurfaces` names each surface's path in this edition; `.github/CODEOWNERS` names a human for each; docs-lint fails when a declared surface has no owned CODEOWNERS entry; `gate-check.mjs` reads back whether the forge requires code-owner review | latent (the weakest of 2 obligations); **every irreversible surface is enumerated and declared to require a named human review** `proven`: The surface set is a closed registry in the tool, not a list in the edition, so an edition cannot quietly drop one: docs-lint iterates the three surfaces it knows and fails on any the edition does not declare or CODEOWNERS does not cover. Red-correct in this tree for a removed owner line; **the merge gate requires that review, armed at instantiation** `latent`: `gate-check.mjs` is built and reads code-owner review back from the forge. It has never run against a real forge, so the arming half is unexercised. The claim itself assigns arming to the human and verification to the kernel, which is why this is `latent` rather than `owed`; Measured in this edition's own tree 2026-07-27 (node Phase A, F3). Lifted from `owed`, where the row read `trigger: the first instantiation` and recorded no mechanism, while all three parts were present. Red-correct here: removing the contracts owner from CODEOWNERS fails docs-lint naming the surface and the claim. Two of the three declared directories do not exist in this edition yet, and that is argued rather than accidental: naming the owner before the first migration lands is the point, and a CODEOWNERS pattern matching nothing today matches the day it appears. The residue is recorded in E-81 rather than left implicit. Findings: E-24, E-65, E-81. |
| AI-1 server-injected identity | not built | owed; trigger: the first tool-execution seam. |
| AI-2 untrusted content, no authority | not built | owed; trigger: the first tool. CHECKED 2026-07-27 rather than assumed: `server/src` and `client-web/src` contain no tool-execution surface, no agent loop and no model call, so there is no untrusted-content boundary for this claim to bind to. This is the one row in the F3 batch whose trigger has genuinely not fired; the other four recorded no mechanism while their mechanisms were binding. |
| AI-3 prompt architecture | not built | owed; trigger: the first model-backed feature in an edition project. |
| OBS-1 external-effect seam observability | not built | owed; trigger: the first external side-effect port in an edition project. This claim opens the observability family the v1 cut deliberately deferred. |
| OBS-2 refusals and privileged actions are security events | not built | owed; trigger: the first deployed edition host (alongside SEC-9; the refusal seams it instruments are already built and tested, so the events attach to existing tested paths). |
<!-- conformance:end -->

## Known limitations

- **The exemplar surface is minimal and its store is a placeholder.** Four routes exist (`/health` anonymous,
  `/notes` read and write, `/notes/:noteId`) so the scans have something real to be green about; a scan whose
  only subject is one route it was never going to flag proves close to nothing. `/notes` is backed by a
  module-level Map with no tenancy, no concurrency token and no bounded read. No DATA claim is realized by it and
  choosing a real store under DEP-1 is its own pass. TEST-2 and TEST-4 stay `owed` and the CI loop carries no e2e
  job, deliberately, rather than an e2e job that asserts nothing.
- **No credential can be minted, so every gated route answers 401.** SEC-4 and TEN-6 are owed in both editions,
  so this server has no way to produce a credential and the authorization seam is wired to a function that
  always returns none. That is the fail-closed direction and it is asserted as a test rather than left implicit:
  if a mint ever lands without being wired, the test goes red instead of the server quietly opening.
- **The client is the shared one, unmodified.** `client-web/` is composed from the shared tier and still points at
  the sibling edition's API shape. Wiring it to this server is part of the build pass, not the scaffold.
- **The CI loop is built, not armed.** Branch protection is set at instantiation; until then the loop blocks
  nothing, exactly as in the sibling edition.
- **The client's pins carry known advisories.** `client-web/` reports 14 advisories, 1 critical and 13 high,
  across the eslint, vitest, vite and postcss chains. Every one now has a fix that also clears the cooling-off
  window, following the window pass of 2026-07-26; at 90 days the `vite` chain had none, which is the evidence
  that prompted the ruling. This edition's server tree installs with zero advisories. The client's pins are the
  shared tier's and are not this edition's to change unilaterally, so the bump is queued rather than applied:
  it re-composes into the shipped .NET edition and owes that edition its own verification run. `VERSIONS.md`
  and the findings register (E-3) carry the detail.
