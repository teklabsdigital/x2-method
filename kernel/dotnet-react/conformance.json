{
  "edition": "dotnet-react",
  "catalogPassDate": "2026-07-26",
  "claims": {
    "TEN-1": {
      "title": "Tenant comes from the credential only",
      "status": "proven",
      "mechanism": "`TenantScopeMiddleware` reads `tenant_id` from the JWT only; `EndpointSpineTests` scans route and query parameters enumerated BY EXCLUSION and DECLARED tenant/PII headers via the framework's own `IFromHeaderMetadata` with vendor-prefix and separator normalization, matching both with `NameComparison`; `HostSecurityTests` proves at run time that a forged tenant header in five spellings does not move a request into another tenant; `ContractShapeTests` bans `TenantId` on requests",
      "note": "TEN-1 names four surfaces and this edition reaches all four, unevenly. ROUTE is total, because the pattern loop reads parameter names without consulting a type. HEADER is covered from both sides (A-2): a declared binding is a scan violation, and a header arriving undeclared is proven not to influence tenant resolution. QUERY was type-dependent and is not any more (E-2, repaired 2026-07-27): nothing asks what binds from a URL, so a strong-typed tenant id is inside the surface by default, proven by planting `TenantKeyProbe : IParsable` as a query parameter named `tenantKey` on a real route (red now, green under the old mechanism). Matching was equality and is now a comparison (E-9, repaired), which matters most on the header surface, where normalization removes every boundary a token matcher would need. Findings: A-2 (repaired), E-2 (repaired), E-9 (repaired), E-6 (contracts half open). Request-contract surface raised 2026-07-27, together with E-22's repair of the tenant predicate on body members.",
      "obligations": [
        {
          "name": "the tenant is resolved solely from the validated credential",
          "status": "proven",
          "text": "`TenantScopeMiddleware` reads `tenant_id` from the token and from nothing else, which is the half of this claim the second edition cannot reach at all because it can mint no credential."
        },
        {
          "name": "the route surface is enumerated completely",
          "status": "proven",
          "text": "the pattern loop reads parameter names without consulting a type, so no route parameter is invisible to it; what the predicate then does with the name is the comparison obligation below."
        },
        {
          "name": "the query surface is enumerated completely",
          "status": "proven",
          "text": "the hand-written type list is gone: parameters are enumerated by exclusion from the composed route table, so a value object implementing the framework's parse interface, a `TimeSpan` or a type nobody has thought of is inside the surface rather than outside it, and the enumeration is held to agree with the real binder over E-2's measured corpus rather than restating the binder's rules in an assertion (E-2)."
        },
        {
          "name": "the header surface has a mechanism of its own",
          "status": "proven",
          "text": "a header surface cannot be enumerated, so this is the non-scan mechanism ruling 5 requires, and it is covered from both sides: a DECLARED header binding is a scan violation through the framework's own `IFromHeaderMetadata`, with vendor-prefix and separator normalization, and a header arriving UNDECLARED is proven at run time not to move a request into another tenant, in five spellings (A-2). The normalization compacts `X-Tenant-Id` to `tenantid`, which destroys every boundary; the comparison resolves the concatenation, so the base entry covers it without a list of pre-glued spellings (E-9)."
        },
        {
          "name": "the request-contract surface is enumerated completely",
          "status": "proven",
          "text": "repaired 2026-07-27, and raising it exposed a gap no pass had named. The contracts guard now walks every member at any depth through the shared `BodyMemberWalk`, which closes the enumeration half. The predicate half was missing outright: the tenant registry was applied to URL parameters and to nothing else, so a request contract carrying `WorkspaceId` and `OrganisationId` passed all 100 tests while TEN-1's statement says no request contract carries a tenant identifier field. Only `tenantId` was caught, and only because SEC-2's separate registry happens to list it. Both surfaces now apply the tenant registry to body members, proven red on an unrouted contract and on the routed `CreateNoteRequest`, green when reverted. Recorded as E-22."
        },
        {
          "name": "the comparison resolves compounds, concatenations and decompositions of a listed word",
          "status": "proven",
          "text": "matching is `NameComparison`: an entry `tenant` covers `tenantId`, `tenantid`, `tenant_id` and `tenantSlug`, an entry `org` covers `orgId` and `orgid` through a declared affix, and the flow-back pass's widened list of compounds is gone because they are derived, each asserted to still match. The prefix rule that would let `org` reach `organisationid` directly is rejected and asserted against, because it also matches `origin`, an ordinary request header (E-9)."
        }
      ]
    },
    "TEN-2": {
      "title": "Ambient scope, fail-closed",
      "status": "patterned",
      "mechanism": "`AmbientTenantScope` (AsyncLocal, throws on unset); `AmbientTenantScopeTests`",
      "note": "the non-HTTP job base class is owed (trigger: first background job)",
      "obligations": [
        {
          "name": "scope established at every entry",
          "status": "patterned",
          "text": "`TenantScopeMiddleware` stamps the scope at HTTP ingress and the middleware test proves it; each non-HTTP entry owes its own `Begin`, and the job base class is owed (trigger: first background job)."
        },
        {
          "name": "unset scope fails before I/O",
          "status": "proven",
          "text": "`AmbientTenantScope` throws when read unset, so an unstamped path is an error rather than an unfiltered query; `AmbientTenantScopeTests` asserts the throw rather than the accessor's presence."
        },
        {
          "name": "cross-tenant probes assert uniform not-found",
          "status": "patterned",
          "text": "`One_tenant_cannot_read_another_tenants_note`, `One_tenant_cannot_delete_another_tenants_note` and `Another_tenants_note_is_absent_from_the_list` cover the read, mutating and list verbs, each asserting the same absent answer. Before E-50 only the GET existed, and removing the tenant filter from `EfNoteStore.DeleteAsync` left every runnable test green while the path answered 500 rather than 404, which is an existence oracle. The delete probe now turns red on exactly that plant. trigger: each new mutating endpoint owes its own cross-tenant probe."
        }
      ]
    },
    "TEN-3": {
      "title": "Tenant-leading composite keys",
      "status": "proven",
      "mechanism": "`NoteConfiguration.HasKey(TenantId, Id)`; `TenantKeyTests` over the EF model, reading `TenantColumn.IsTenantShaped`, which is TEN-1's `ForbiddenTenantParams` registry through `NameComparison`; `KeyShapeExemption` for sanctioned exceptions",
      "note": "",
      "obligations": [
        {
          "name": "tenant-owned entities lead their key with the tenant id",
          "status": "proven",
          "text": "`Every_tenant_owned_entity_leads_its_key_with_TenantId` reads the EF model and binds: flipping `HasKey` to lead with `Id` turns it red and names TEN-3. It rides on the `ITenantOwned` marker, and `The_sets_these_assertions_iterate_are_not_empty` now fails if that marked set is empty, so the assertion cannot pass over nothing (E-43 repaired)."
        },
        {
          "name": "no unmarked tenant data",
          "status": "proven",
          "text": "`Every_entity_with_a_tenant_column_is_marked_tenant_owned` binds: stripping `ITenantOwned` from `Note` turns it red. It reads TEN-1's `ForbiddenTenantParams` through `NameComparison`, not a list of its own; the four-name equality list it carried until E-51 let six of thirteen tenant-shaped spellings past, `tenant_id` and `TenantKey` among them. The extent is asserted against an independently written floor of thirteen spellings plus six ordinary names that must not match. This obligation now rests on exactly the registry and comparison TEN-1 rests on, so the two rows stand or fall together."
        },
        {
          "name": "a sanctioned exception carries a named justification and its own guard",
          "status": "proven",
          "text": "`KeyShapeExemption` is a register separate from the TEN-5 access ledger, as the claim requires, following `AnonymousCarveOut`. Four rules, each proven by fixture because the shipped register is empty and proving them from an empty register would prove nothing: a stale entry fails, an unjustified entry fails, an unnecessary entry fails, a duplicate fails. End-to-end control: a violating key plus a justified exemption is green, and the same exemption with its justification reduced to a label is red (E-44 repaired)."
        }
      ]
    },
    "TEN-4": {
      "title": "Save-time guard and write provenance",
      "status": "owed",
      "mechanism": "`KernelDbContext.GuardTenancy` on both SaveChanges variants; `TenantGuardTests`; integration cross-tenant modify; `BulkWriteBanTests` bans the write paths that bypass the save pipeline (EF set-based, raw SQL, raw ADO)",
      "note": "the tenancy guard is proven; the write-provenance half has no mechanism (E-52)",
      "obligations": [
        {
          "name": "foreign-tenant writes are refused at save",
          "status": "proven",
          "text": "`KernelDbContext.GuardTenancy` runs on both SaveChanges variants; `TenantGuardTests` covers stamp-on-create, throw-on-foreign-row and throw-on-unset-scope, with an integration cross-tenant modify behind it."
        },
        {
          "name": "no write path bypasses the save pipeline",
          "status": "proven",
          "text": "`BulkWriteBanTests` bans the EF set-based, raw SQL and raw ADO operators, which is what makes the guard above total rather than typical."
        },
        {
          "name": "the pipeline stamps write provenance on create and modify",
          "status": "owed",
          "text": "Four of the seven unit tests TEN-4's mechanism class names are provenance, and none exists or can: `Note` carries no actor field and `GuardTenancy` stamps `TenantId` only. `CreatedAtUtc` is stamped by `NoteService`, not by the pipeline, which is the opposite of the claim's \"assigned by the pipeline unconditionally, values already present are overwritten\". This was recorded in the row's free-text note and not as an obligation, so the roll-up read `proven` (E-52). trigger: the actor field and a pipeline stamp, or the next edition build pass."
        }
      ]
    },
    "TEN-5": {
      "title": "Sanctioned-bypass ledger",
      "status": "owed",
      "mechanism": "`docs/claims/tenant-bypass-ledger.md` (empty) + docs-lint rule that a row without a sole-reader test fails",
      "note": "planted 2026-07-27, six plants, two red naming TEN-5; three more obligations planted 2026-07-28 and two of the three moved off `owed`. The parse is keyed on column name rather than position and refuses a ledger it cannot read as a table (E-35 repaired), the named sole-reader test is resolved against the tree and scored in both directions (E-34 repaired), and a second bypass ledger fails (E-34 repaired). The row stays `owed` on one obligation and the reason is a measurement, not a backlog: this edition enforces tenancy per query rather than by a global filter, so a sanctioned bypass has no lexical signature to scan for (E-77).",
      "obligations": [
        {
          "name": "a committed ledger exists and is where the claim lives",
          "status": "proven",
          "text": "removing docs/claims/tenant-bypass-ledger.md fails docs-lint with a sentence naming TEN-5, not a stack trace."
        },
        {
          "name": "a ledger row that names no sole-reader test fails the build",
          "status": "proven",
          "text": "red-correct for an empty third cell. The parse is keyed on the COLUMN NAME now, not on position, which is E-35 repaired in both of its halves: inserting an Owner column used to move the cell out of reach and pass every row unread, and a ledger reformatted as a bullet list used to yield no rows at all and pass for being empty. A parse that silently finds nothing is the worst failure available to a guard whose subject is empty by design, so a ledger this parser cannot read as a table is now a failure, and so is a table with no sole-reader column."
        },
        {
          "name": "the named sole-reader test exists, and is the remedy",
          "status": "patterned",
          "text": "The name is resolved against the tree: a row naming an identifier that appears in no source file fails, red-correct for a planted row naming `NightlyBillingSweepIsSoleReader`. Scored in BOTH directions, which is what distinguishes a resolver from a guard that always refuses: adding a declaration of that identifier turns the same row green. Deliberately a whole-word search over source rather than a test-framework lookup, because knowing what counts as a test in xunit and in vitest is the stack-specific knowledge a shared tool must not hold (E-34 repaired). `patterned`, and the residue is the obligation's second clause: it proves the name resolves to SOMETHING, not that the something is a test, and nothing at all checks that the test is the sole reader. Trigger for the rest: the first sanctioned bypass, which would give the clause a subject."
        },
        {
          "name": "every sanctioned cross-tenant path appears in the ledger",
          "status": "owed",
          "text": "unbuilt, and the reason is worth recording rather than restating the trigger. The scan this obligation's trigger asks for presumes a bypass has a lexical signature, which presumes tenancy is enforced by something that can be bypassed. Measured 2026-07-27: this edition has NO global query filter. Tenancy is an explicit `.Where(n => n.TenantId == tenantId)` per query, so a cross-tenant read is the ABSENCE of a predicate, which no regex can see and which a scan for `IgnoreQueryFilters` would report as clean forever. Building that scan here would be a guard checking for a bypass shape this design cannot produce (E-34, E-77). Trigger: a global query filter or an equivalent chokepoint that a bypass has to name, plus the first sanctioned bypass."
        },
        {
          "name": "one ledger, not several",
          "status": "patterned",
          "text": "A second document whose name carries both `bypass` and `ledger` fails, red-correct for a planted `docs/claims/billing-bypass-ledger.md` (E-34 repaired). `patterned` because the rule is on the NAME: a second ledger called `cross-tenant-register.md` escapes it. Each new spelling owes its own entry, which is the ordinary cost of a name-shaped rule and is why the canonical path is named in the message."
        }
      ]
    },
    "TEN-6": {
      "title": "Tenant is minted from membership, never asserted",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: first identity slice (shared with SEC-4's version store and SEC-10)"
    },
    "SEC-1": {
      "title": "Every endpoint gated",
      "status": "proven",
      "mechanism": "`EndpointSpineTests` over the composed host's route table: every endpoint names a non-empty perm policy or matches a justified, method-specific anonymous carve-out; the fallback policy is resolved the way the middleware resolves it and EVALUATED; and the enumeration itself is asserted to be every route the host maps",
      "note": "four obligations, all proven, and the fourth was added because a narrowed enumeration left the other three green",
      "obligations": [
        {
          "name": "every endpoint names a permission or is a reviewed anonymous carve-out",
          "status": "proven",
          "text": "The scan walks the composed host's own route table and requires a `perm:`-prefixed, non-empty policy. Bare authentication is refused at the endpoint, which is what the claim asks for and what the realization had accepted as its floor until E-7."
        },
        {
          "name": "the anonymous allowlist is reviewed rather than accumulated",
          "status": "proven",
          "text": "`AnonymousCarveOut` carries method, pattern and a justification with a 40-character floor, so an entry is scoped to one verb on one URL rather than pre-authorizing every method on it (E-8). A carve-out matching no endpoint fails as stale, which is what stops the register becoming a hole waiting for a URL. An endpoint with no method metadata answers every method and is reported as `*`, so it can never match a specific entry."
        },
        {
          "name": "the fallback denies by default, evaluated rather than inspected",
          "status": "proven",
          "text": "Resolved from `IAuthorizationPolicyProvider`, which is what `AuthorizationMiddleware` consults, and then run against two principals: an anonymous one and an authenticated one carrying tenant, session version and both perms. The earlier version read `IOptions<AuthorizationOptions>` and asserted a requirement was present, which is a different object and could not see that returning null from the provider opened every metadata-free endpoint (E-7 round 4). A `DenyAnonymousAuthorizationRequirement` is satisfied by any authenticated caller, so the authenticated case is the one the old assertion could never have expressed."
        },
        {
          "name": "the enumeration reaches every route the host maps",
          "status": "proven",
          "text": "Added 2026-07-27. Every assertion above is a loop over one shared `RouteEndpoints()`, and a loop over a narrowed set passes while saying nothing. Measured: narrowing it to routes whose pattern contains `health` left all three assertions above green, and the only test that noticed was SEC-2's body scan, whose message names neither the narrowing nor SEC-1 (E-72, and E-22's third instance here). The assertion compares the scanned count against the framework's own unfiltered table and then checks a named floor of this edition's three routes, because non-emptiness alone would not have caught a plant that left one route standing. TEN-1's route, query and header scans read the same method, so they inherit it."
        }
      ]
    },
    "SEC-2": {
      "title": "Anti mass-assignment",
      "status": "proven",
      "mechanism": "`EndpointSpineTests` takes each endpoint's body type from the framework's own `IAcceptsMetadata` and walks it RECURSIVELY through public properties, constructor parameters, arrays and generic arguments, with cycle protection and no depth cap, matched by `NameComparison` against the shared registry, with a parameter walk as a second net and a non-vacuity assertion; `ContractShapeTests` is a third net over the Contracts assembly, FLAT and filtered to types whose name ends in `Request`, matching by equality; App/Api declare no request/response types",
      "note": "The guards are not equivalent and the difference is the point. The HOST scan recurses (E-6, 2026-07-26) and as of 2026-07-27 it no longer decides for itself what a body is: the enumeration comes from the framework's `IAcceptsMetadata`, so the two exclusions that used to hide a type from it are gone. Both were proven by planting the violation on a real route: a public DTO declared in the entry-point file, which has no namespace and was skipped outright, is now caught, and the old mechanism with the same DTO planted is green. The comparison is no longer equality (E-9). The residual it leaves was measured rather than argued: a `*Request` contract carrying a forbidden field one level down, bound to no route, is invisible to both guards; the moment it is bound to a route the host scan catches it at depth. So the uncovered set is exactly the contract types nothing can post to. Findings: E-2 (repaired), E-6 (host half repaired, contracts half blocked on MOD-2), E-9 (repaired), E-20. Enumeration raised 2026-07-27: the contracts guard now shares the endpoint scan's walk and comparison, and the tenant registry reaches contract members for the first time (E-22).",
      "obligations": [
        {
          "name": "the predicate rejects a registry member at any depth of an enumerated type",
          "status": "proven",
          "text": "`EndpointSpineTests` walks every body-bound DTO recursively through public properties, constructor parameters, arrays and generic arguments, with cycle protection and no depth cap, proven both ways against a nested `AuthorDto` carrying `CreatedBy` (E-6), and the walk's depth is asserted directly because the composed host binds only flat records today."
        },
        {
          "name": "the enumeration reaches every body-bound type",
          "status": "proven",
          "text": "both nets are now complete and they share one walk. The host half takes the body type from the framework's own `IAcceptsMetadata.RequestType` rather than from a predicate of this test's own, so E-6's null-namespace exclusion and E-2's hand-written type list are both gone. The second net, `ContractShapeTests`, was the last flat and equality-matching guard in the tree and is repaired: it walks the SAME `BodyMemberWalk.MemberNames` at any depth and compares through the SAME `NameComparison`, so the two guards cannot drift apart about what a caller can populate. Proven with a control: with the pre-repair guard restored and an unrouted `OrphanRequest` carrying `AuthorInfo.CreatedBy` planted, all 100 tests passed; with the repair, the nested field and a `CreatedByUser` compound at depth zero each fail separately. The residual, stated: a contract type in the Contracts assembly that is neither routed nor named `*Request` is enumerated by neither guard, and nothing can post to it, so it enters the surface only by gaining a route, at which point the host scan sees it. It was repaired here after three passes recorded it as blocked on MOD-2's delta; that premise was measured and is false (S-12)."
        },
        {
          "name": "the comparison resolves compounds, concatenations and decompositions of a listed word",
          "status": "proven",
          "text": "matching is `NameComparison`, not a hash-set lookup: `status` covers `noteStatus` and `notestatus`, `createdAt` covers `createdAtUtc`, `role` covers `roles`, and the registry lost the spellings it used to enumerate because they are derived, each one asserted to still match. The flat second net still compares by equality and reads the same registry, and a test holds every name in the flat list to be matched by the rules, so the weaker net can never become a different one (E-9)."
        }
      ]
    },
    "SEC-3": {
      "title": "No PII in URLs",
      "status": "owed",
      "mechanism": "`EndpointSpineTests` route + query scan over the composed route table, enumerated BY EXCLUSION (the framework's own declared body type, registered services and the pipeline's own types are set aside and every other parameter is a value a caller can spell), with [AsParameters] wrapper properties walked; matched by `NameComparison`, which resolves compounds, concatenations, decompositions and plurals",
      "note": "Lowered from `proven` to `owed` by the adjudication pass of 2026-07-26 (ruling 6), and **both named gaps were closed on 2026-07-27**: the enumeration no longer asks which types bind from a URL (E-2) and the comparison is no longer equality (E-9). Each was proven by planting the violation on a real route in the composed host, confirming red, then restoring the old mechanism with the violation still planted and confirming green, which reproduces the hole rather than taking the record's word for it. The row does not return to `proven`, and the reason is a duty the row never carried: SEC-3's completeness obligation ends \"a query parameter the surface does not declare is refused or removed before the handler by a ruled decision, never bound silently\", and nothing here does that. Measured, not inferred: a handler that takes HttpContext and reads `Request.Query[\"emailAddress\"]` leaves the scan green. That obligation is written into the row below rather than left in prose, so the row reads `owed` for something real (E-20).",
      "obligations": [
        {
          "name": "the predicate rejects a registry match on a parameter it can see",
          "status": "proven",
          "text": "`EndpointSpineTests` scans route and query parameters, covering `[AsParameters]` wrappers, collections and date parameter types that a naive scan misses."
        },
        {
          "name": "the enumeration reaches every declared route and query parameter",
          "status": "proven",
          "text": "the hand-written type list is gone. Nothing asks what binds from a URL any more: the body type comes from the framework's own `IAcceptsMetadata` on the composed endpoint, services come from `IServiceProviderIsService`, the pipeline's own types are a closed list of things no caller can spell a value for, and every other parameter is compared. A type nobody has thought of is inside the surface by default instead of outside it. `Both_enumerations_agree_with_the_real_binder_over_a_corpus_of_parameter_types` holds that against the binder that actually runs, over E-2's measured corpus (`TenantKey : IParsable`, `TimeSpan`, `Uri`, `IPAddress`, `Int128`, a legacy `TryParse` type), so the two can no longer drift apart silently (E-2)."
        },
        {
          "name": "the comparison resolves compounds, concatenations, decompositions and plurals of a listed word",
          "status": "proven",
          "text": "`NameComparison` tokenizes on camel case, separators and letter/digit boundaries, re-glues every contiguous run of tokens, matches an entry that begins or ends a glued word, folds plurals, and derives an entry's tail past a one-letter particle, so `email` covers `emailAddress`, `user_email`, `emailaddress`, `e_mail`, `e-mail`, `EMailAddress`, `mail`, `emails` and `email1`. It beats both matchers this repo has built: the six inputs the round 4 audit measured escaping BOTH equality and token matching are asserted, and the registry got SHORTER because `firstname`, `lastname` and `dateofbirth` are derived rather than listed, with every dropped spelling asserted to still match. Its false positives (`fileName`, `voicemail`) and its residuals (a synonym, a reordering, an unnamed parameter) are passing tests, so a later narrowing goes red (E-9)."
        },
        {
          "name": "a parameter that binds or is read without being declared is inside the surface",
          "status": "owed",
          "text": "SEC-3's remedy sentence requires an undeclared query parameter to be \"refused or removed before the handler by a ruled decision, never bound silently\", and this edition has no such mechanism: measured, a handler taking HttpContext and reading `Request.Query[\"emailAddress\"]` leaves the whole suite green. TEN-1 met the same shape on the header surface with a runtime mechanism rather than a scan, which is the available answer here, and refuse-versus-remove is exactly the choice the claim says must be RULED per surface (trigger: an adjudication ruling on the refuse-or-remove decision, in the class of E-8's HUM-1 half; the row is `owed` on this and on nothing else)."
        }
      ]
    },
    "SEC-4": {
      "title": "JWT hardening and revocation",
      "status": "proven",
      "mechanism": "pinned HS256, `RequireSignedTokens`, `SessionVersionMiddleware`; `HostSecurityTests` asserts both token-validation properties off every registered bearer scheme and covers a tampered signature, an HS384 token, an unsigned alg:none token, a missing `sv` and a bumped `sv`",
      "note": "both halves of the token-validation sentence now read off the host, over every bearer scheme, not one by name",
      "obligations": [
        {
          "name": "the signing algorithm is pinned to exactly one",
          "status": "proven",
          "text": "`Token_validation_pins_exactly_one_algorithm` reads `ValidAlgorithms` off every scheme whose handler is `JwtBearerHandler` and asserts it equals `[HS256]`. The behavioural HS384 test proved HS384 was excluded and nothing about what else the list admitted: adding `RsaSha256` left all twelve host tests green (E-54). Controls on 2026-07-27: widening the list goes red, registering a second looser bearer scheme goes red, and narrowing the scan to no scheme goes red on its own non-empty assertion."
        },
        {
          "name": "tokens must be signed",
          "status": "proven",
          "text": "`Token_validation_requires_a_signature` reads the flag off every bearer scheme, and `An_unsigned_token_is_rejected` sends a hand-assembled alg:none token. `RequireSignedTokens = false` had left all twelve host tests green (E-55); it now turns both red. The control also corrected a prediction: with the flag off and the algorithm still pinned to HS256, an unsigned token AUTHENTICATES, because it never reaches signature validation, so the pin does not shadow this flag."
        },
        {
          "name": "revocation is immediate through a server-side session version",
          "status": "proven",
          "text": "`SessionVersionMiddleware` plus the bumped-`sv` and missing-`sv` tests; removing the middleware from the pipeline turns both red. The v1 store is process-local, so an EF-backed store is this mechanism's named upgrade rather than a second obligation. No TTL cache exists, so the claim's cache-window obligation has no surface here."
        }
      ]
    },
    "SEC-5": {
      "title": "No secrets in config",
      "status": "proven",
      "mechanism": "`UserSecretsId` + `SecretConfigShapeTests` over every committed JSON config under `src/`, matching secret-shaped keys by TOKEN (camel/Pascal/snake/kebab split) rather than by containment, with a `[Theory]` asserting the predicate's extent + `tools/secret-scan.mjs`, a composed shared-tier scan over every tracked text surface with a `--self-test` mode CI runs before the scan",
      "note": "Both mechanisms were blind to the edition's own `Jwt:Key` until 2026-07-26 and both are repaired (E-11). The arch test matched `leafKey.Contains(term)`, which could never fire on a leaf shorter than the term describing it; the CI half was a grep in YAML that was additionally blind to every QUOTED key, meaning every key in every JSON file in the edition (E-16). Four justified exceptions are declared in `secret-scan.allow.json`, each a (file, key) pair with a written reason and a staleness check; one of them is a committed test signing key the repaired scan found on its first run (E-17). Token matching costs a false positive on `TokenCount`, asserted as a passing case rather than hidden (E-9). Still open: `.env` lives inside the repository tree while the claim's statement says development secrets live outside it (E-12), which the scan's tracked-file reading does NOT address. Vault-port interface owed (trigger: first runtime credential).",
      "obligations": [
        {
          "name": "no secret-shaped value in committed configuration",
          "status": "proven",
          "text": "`SecretConfigShapeTests` over every committed JSON config under `src/`, matching secret-shaped keys by token split rather than by containment, with a `[Theory]` asserting the predicate's extent."
        },
        {
          "name": "no secret-shaped literal on any tracked text surface",
          "status": "proven",
          "text": "`tools/secret-scan.mjs`, whose `--self-test` asserts eleven positive and thirteen negative controls before the scan runs; four justified exceptions are declared as (file, key) pairs with reasons and a staleness check."
        }
      ]
    },
    "SEC-6": {
      "title": "Log safety",
      "status": "patterned",
      "mechanism": "`client-web/tools/harness/redact.ts` scrubs Authorization headers and JWT-shaped strings (`redact.test.ts`); `LogSafetyTests` reads the server's real sink through a capturing `ILoggerProvider` while a request carrying a bearer token and a body with content in it goes through the real `UseExceptionHandler`",
      "note": "both surfaces this edition has are covered; there is no hub, so realtime logging has no surface here yet",
      "obligations": [
        {
          "name": "secret-shaped values are scrubbed by one redactor",
          "status": "patterned",
          "text": "Two surfaces, two mechanisms, because they are different sinks. Harness output: `redact.ts` plus `redact.test.ts`, on the client. Server: `LogSafetyTests` asserts the bearer token, a JWT-shaped string and the request body's content are all absent from what the sink actually received. It found the redactor's JWT pattern over-matching: the unanchored three-dot form matches `Microsoft.EntityFrameworkCore.Database.Command` and every other dotted identifier (E-58). trigger: each new logging surface owes its own, which is what keeps this `patterned`."
        },
        {
          "name": "each logging surface asserts its own redaction",
          "status": "patterned",
          "text": "The claim names three surfaces: hub/realtime logging, harness output, error handlers. Harness output is covered on the client; the error handler is now covered on the server, where `ILogger`, `Serilog` and every redaction helper had returned zero occurrences across `server/src` while `app.UseExceptionHandler()` was wired and nothing asserted what those entries contained (E-56). This edition has no hub, so the third surface does not exist to test. Controls on 2026-07-27: logging request headers in the pipeline turns the token and JWT assertions red, `EnableSensitiveDataLogging()` on the DbContext turns the content assertion red, and unwiring the capture turns the arrangement's own non-empty guard red while the other three pass on an empty log. trigger: the first slice that adds a hub or any other logging surface owes its named test."
        }
      ]
    },
    "SEC-7": {
      "title": "Opaque public identifiers",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first anonymous resource surface in an edition project (the kernel exemplar has none)"
    },
    "SEC-8": {
      "title": "Abuse posture",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first publicly exposed edition host (the launch pre-flight names it)"
    },
    "SEC-9": {
      "title": "Deployment-edge hardening",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first deployed edition host (arrives with the launch pre-flight)"
    },
    "SEC-10": {
      "title": "Authentication strength asserted at mint, verified at the gate",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: first identity slice (shared with SEC-4's version store and TEN-6)"
    },
    "SEC-11": {
      "title": "The runtime credential cannot touch the schema",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the next edition build pass (rides DATA-6: the migrate mode and the role split are one provisioning story; the integration tier's provisioning path is where both roles are created)"
    },
    "TIME-1": {
      "title": "UTC, offset-aware, and nothing else",
      "status": "owed",
      "mechanism": "`TimeTypeTests` over every layer project under server/src, DERIVED from the filesystem rather than listed (recurses Nullable, arrays, generics; the global namespace is app code, not framework); a layer that exists and whose assembly the scan cannot reach fails a test of its own",
      "note": "DB UTC column defaults not used: the service sets the value. **Lowered from `proven` to `owed` by the adjudication pass of 2026-07-26 (rulings 2 and 3).** E-15 measured the omitted layer with a control and the flow-back pass deliberately did not repair it, on the grounds that lowering the row belonged to the owner. **The layer half was repaired on 2026-07-27**, and not by adding the missing assembly to the list: the list is gone and the layer set is derived from the projects under server/src, which is what TIME-1 requires (\"a rule the test can check rather than a hand-written list\"). Proven three ways, each red before the repair and after the violation was planted: a naive `DateTime` on a public type in the API layer, the same type declared in the entry-point file where it has no namespace at all (the file E-6 and E-15 both exempt), and a layer added under src/ that the tests cannot reach, which fails without carrying any time type. The monotonic-durations half has never had a realization here and is why the row still reads `owed`, which is the S-11 shape: two obligations that are built, gating and red-green proven, rolled up with one deferred extension, and the vocabulary's weakest word means \"not built\".",
      "obligations": [
        {
          "name": "the predicate rejects a naive instant in the layers it reaches",
          "status": "proven",
          "text": "`TimeTypeTests` recurses Nullable, arrays and generics over the layers it is given, and rejects naive `DateTime` in all of them. As of 2026-07-27 it also walks types with no namespace, which the hierarchy walk previously stopped at as though the global namespace were the framework."
        },
        {
          "name": "the enumeration reaches every layer that declares a time value",
          "status": "proven",
          "text": "the layer set is derived from the projects under server/src, so a layer added to the server is scanned with no edit to the test, and a layer that exists and whose assembly the scan cannot reach fails `Every_declared_layer_is_reachable_by_the_scan` rather than being skipped, which is the remedy half TIME-1 asks for (\"a layer that exists and is not in the enumeration fails the build too\"). The derivation is cross-checked against the composed host's own assembly graph so an empty walk cannot read green. E-15's control is red both ways now, and so is the one that composes with E-6 on the entry-point file."
        },
        {
          "name": "durations come from the monotonic source through one clock seam",
          "status": "owed",
          "text": "the wall-clock-delta lint the 2026-07-24 extension adds has no realization here, and nothing confines a now-reading to a seam (trigger: the next edition build pass)."
        }
      ]
    },
    "DATA-1": {
      "title": "Stores, records, downward deps",
      "status": "proven",
      "mechanism": "`DependencyDirectionTests`: seven assertions over the App, Contracts, Persistence and Api assemblies, with the service and store registries discovered by reflection and every selection asserted non-empty",
      "note": "",
      "obligations": [
        {
          "name": "no endpoint or host type references a store implementation or DbContext",
          "status": "proven",
          "text": "`No_host_type_outside_the_composition_root_depends_on_Persistence` covers all of `Kernel.Api.*`, not just the endpoints namespace, and `Program` is excluded by construction because top-level statements put the composition root in the global namespace. Planted in both namespaces: red in each, naming DATA-1 (E-40 repaired)."
        },
        {
          "name": "no persistence type references application services",
          "status": "proven",
          "text": "`Persistence_does_not_depend_on_an_application_service` bans a registry of `Kernel.App` public classes named `*Service`, discovered by reflection and asserted non-empty. A persistence type taking `NoteService` turns it red (E-41 repaired)."
        },
        {
          "name": "store implementations are reachable only via their interfaces",
          "status": "proven",
          "text": "`Store_implementations_are_named_only_by_the_composition_root` bans a registry of persistence classes realizing a `Kernel.App` `*Store` interface, discovered by reflection and asserted non-empty. A host type taking `EfNoteStore` rather than `INoteStore` turns it red (E-41 repaired). The App half of that assertion is unfalsifiable by project-reference direction and is carried as defense in depth, not as the proof."
        }
      ]
    },
    "DATA-2": {
      "title": "Bounded reads",
      "status": "patterned",
      "mechanism": "`EfNoteStore` AsNoTracking + `(CreatedAtUtc, Id)` keyset + a store-side clamp; `EfNoteStoreTests` asserts all three against the real store over SQLite; `NoteServiceTests` the service clamp; integration `KeysetPagingTests` the same-timestamp collision page, which needs Docker",
      "note": "per-seam: each new list endpoint and each new store read owes its own assertion",
      "obligations": [
        {
          "name": "every list endpoint has a maximum page size",
          "status": "patterned",
          "text": "Bounded at the service (`NoteServiceTests.List_clamps_the_page_size`, over a fake store) and again at the store (`A_direct_caller_cannot_read_past_the_maximum_page_size`, over the real one). Removing the store clamp turns five assertions red, as does removing `.Take(limit)` outright; both were green everywhere before E-48 was repaired. trigger: each new list endpoint owes its own bound test."
        },
        {
          "name": "reads run without change tracking",
          "status": "patterned",
          "text": "Asserted on both read paths against the real store. Removing `.AsNoTracking()` from `GetAsync` turns `A_single_read_leaves_nothing_tracked` red; before the repair it was green everywhere, and removing it from `ListAsync` turned only AI-2's tool test red with a message naming a tool, which is E-22's shape rather than coverage (E-48 repaired). trigger: each new store read method owes its own assertion."
        },
        {
          "name": "list reads are paginated by keyset, not offset",
          "status": "patterned",
          "text": "`Paging_by_cursor_visits_every_row_exactly_once` pages the whole table and asserts no row is dropped or repeated, which distinguishes keyset from offset without a concurrent writer, and runs on SQLite with no Docker daemon. Removing the cursor filter turns it red. The same-timestamp collision page stays with `KeysetPagingTests`, which needs a real engine and stays in the edition template's CI rather than this repository's, by the deliberate scope decision `kernel.yml` now states (E-49, E-39 repaired). trigger: a provider whose id ordering differs from the one under test owes the collision page on that engine."
        }
      ]
    },
    "DATA-3": {
      "title": "Two-layer at-most-once",
      "status": "owed",
      "mechanism": "claim + proven exemplar",
      "note": "trigger: first idempotent external side effect"
    },
    "DATA-4": {
      "title": "Cross-store reconcile",
      "status": "owed",
      "mechanism": "claim + proven exemplar",
      "note": "trigger: first cross-store sequence"
    },
    "DATA-5": {
      "title": "Fail-fast mandatory config",
      "status": "owed",
      "mechanism": "`Required()` startup reads feeding the JWT options and the DbContext; `StartupConfigTests` blanks each of the four keys in turn",
      "note": "the `when` obligation holds; the two `closure` obligations and the `remedy` obligation do not (E-61)",
      "obligations": [
        {
          "name": "validation runs at startup, before the process accepts work, structurally",
          "status": "proven",
          "text": "`Required()` runs at configure time and its results are the JWT signing key, issuer, audience and the connection string, so the serving surface cannot be constructed without them; this is the structural ordering the claim asks for rather than a conventional early call. Each of the four keys is planted individually and each turns the host's startup red naming itself."
        },
        {
          "name": "closure over every key the application declares mandatory",
          "status": "owed",
          "text": "The theory's key list is four `[InlineData]` entries. Adding a fifth `Required()` call and satisfying it in both factories leaves all 157 tests green, so a new mandatory key arrives with nothing proving it is load-bearing (E-61). trigger: drive the theory from a source scan of the `Required()` call sites, not a hand-written list."
        },
        {
          "name": "closure over every key a committed configuration document declares that the application does not",
          "status": "owed",
          "text": "`Webhook:Secretz` added to `appsettings.json` is ignored, green (E-61). The claim calls this the direction that is easy to miss and says an undeclared key is refused rather than ignored, because a typo, a stale key and an abandoned key look identical to a validator that walks only its own list. trigger: walk the committed documents and refuse a key the application never reads."
        },
        {
          "name": "the remedy names every failing key, not the first",
          "status": "owed",
          "text": "Three keys blanked at once, and startup names one: `Jwt:Issuer` (E-61, measured; E-14 recorded it by reading). `Required()` throws on first miss, so a three-key misconfiguration is three failed deploys, which is the claim's own sentence. trigger: collect the misses and throw once naming all of them."
        }
      ]
    },
    "DATA-6": {
      "title": "Migrate and exit",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the next edition build pass (a small host change; the mechanism is two facts on the composed host)"
    },
    "DATA-7": {
      "title": "Ruled retention and disposal",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first PII-bearing entity in an edition project, or the first contractual deletion clause, whichever lands first"
    },
    "DATA-8": {
      "title": "A state change and its event commit together or not at all",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first event publish in an edition project (arrives with the first broker, alongside RES-5)"
    },
    "DATA-9": {
      "title": "A stale write is refused, never silently applied",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the next edition build pass (rides TEN-4's interceptor work; the token and the refusal live in the same save pipeline the tenancy guard already owns)"
    },
    "DATA-10": {
      "title": "Everything that grows has a registered sweep",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first append-only operational table in an edition project (DATA-8's outbox is the likely first, with RES-5's dead-letter quarantine close behind)"
    },
    "DATA-11": {
      "title": "A migration never breaks the release running beside it",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first production deployment with rolling replacement; the lint half can land at the next edition build pass, ahead of the compatibility job"
    },
    "CFG-1": {
      "title": "Operational settings are configuration, not code",
      "status": "patterned",
      "mechanism": "`OperationalSettingsTests` scans `server/src` and `scripts/` for model-id and provider-endpoint literals, and compares every leaf value in the committed appsettings against every literal in the shipped scripts and CI workflows",
      "note": "planted 2026-07-27, six plants, three red naming OperationalSettingsTests. Both regexes could once be narrowed to match nothing with all 100 tests green; the registry's extent is now a Theory (E-29 repaired). The duplication half, which the row's own mechanism text used to describe as though it were a mechanism, was built on 2026-07-27 and immediately found four live duplications of the committed JWT identity in the CI workflow, all repaired (E-13, E-36 repaired; E-75). Both obligations are `patterned` for different reasons: the literal registry is heuristic in its PREDICATE, so a novel operational-setting shape escapes it, and the duplication scan is total in its predicate and heuristic in its SURFACE, which is two globs rather than the rule the claim's completeness obligation asks for. Two live defects remain recorded with triggers rather than repaired: the shared harness token minter carries `?? 'kernel'` as a silent default, and it is a composed shared file so the fix is a cross-edition decision.",
      "obligations": [
        {
          "name": "a scan over host source and shipped scripts fails the build on an operational-setting literal",
          "status": "patterned",
          "text": "red-correct three ways: a model id in scripts/dev-setup.sh, a model id in Kernel.Api/Program.cs, and https://api.anthropic.com in Kernel.App/Agents/ToolExecutor.cs. Two literal shapes are covered; each new shape owes its own entry. The registry's extent is now asserted as a Theory (13 cases, 8 positive and 5 negative), so narrowing a pattern fails its own test: measured red on the neutered registry with the assertion in place, green on the shipped one (E-29)."
        },
        {
          "name": "shipped scripts read committed values rather than duplicating them",
          "status": "patterned",
          "text": "`No_shipped_script_duplicates_a_committed_configuration_value` reads every leaf value out of the committed appsettings and looks for THOSE in the shipped scripts and CI workflows, which is the opposite direction from the literal registry above and needs no entry when a project adds a setting. Red-correct for the E-36 recurrence, literal issuer and audience in scripts/e2e.sh, in both the quoted and bare assignment forms. It found four LIVE duplications in the CI workflow, all repaired (E-75). CFG-1's sanctioned fourth home is honoured and bounded: an environment assignment under a declared key is an override, but only where its value DIFFERS from the committed one, because an override set to the value already committed is a copy that drifts. `patterned` for the surface, not the predicate: the scan covers `scripts/*.sh` and `.github/workflows/*.yml`, which is the maintained list the claim's completeness obligation says is not enough. Widening was measured, not assumed, and reports two false positives (E-75), so it needs a carve-out register rather than a wider glob. Each shipped file kind outside those two globs owes its own entry."
        }
      ]
    },
    "CON-1": {
      "title": "One wire dialect",
      "status": "patterned",
      "mechanism": "one host configuration point (`AddProblemDetails`, `UseStatusCodePages`, a single camelCase `JsonStringEnumConverter`); `WireConventionTests` asserts naming, an enum round-trip in both directions, problem+json on two routes, every mapped endpoint's declared result union against an allowlist of dialect-conforming result types, and every identifier-shaped member in `Kernel.Contracts` against the types that cross the wire as strings",
      "note": "all four statement clauses now have a mechanism, and two of the four are heuristic in a direction the claim admits. Built 2026-07-27: the error-shape scan walks the framework's own `EndpointDataSource` rather than two hand-picked routes (E-46 repaired), and the identifier scan reads member TYPES, which no guard in the suite had ever done (E-47 repaired). Both plants red-correct at c5a7fb7, and neither was noticed by any other test, which is what unguarded meant. Two holes recorded rather than hidden: the error-shape predicate reads only `IResult`-rooted return types, so an MVC controller action is enumerated and never questioned (E-73), and the identifier registry is two entries compared heuristically (E-74). Both scans carry an extent assertion, because a walk over three routes and one identifier reads identically working and empty.",
      "obligations": [
        {
          "name": "one host-level serialization and error configuration point",
          "status": "proven",
          "text": "Both halves bind: removing the enum converter turns `Json_naming_is_camelCase_with_an_enum_converter` and `Enums_round_trip_as_camelCase_strings` red, and removing `AddProblemDetails()` turns `Unknown_route_returns_problem_json` red."
        },
        {
          "name": "no endpoint invents its own error shape",
          "status": "patterned",
          "text": "`No_endpoint_declares_a_result_type_that_invents_an_error_shape` walks every route in the host's `EndpointDataSource` and reads each handler's declared result union against an allowlist of dialect-conforming result types. An allowlist, not a ban list: any `Xxx<TBody>` renders TBody raw at whatever status it carries, so a ban list reads green for the result types it has not heard of. Red-correct for `GetAsync` returning `NotFound<string>` in place of `NotFound`, naming the route; 158 green before, and no other test noticed (E-46 repaired). The extent of the predicate is a 14-case Theory written from CON-1's statement, not derived from the shipped handlers. `patterned` and not `proven` because the predicate reads only `IResult`-rooted return types, so an MVC controller action returning a bespoke body is enumerated and never questioned (E-73). Each new endpoint style owes its own entry."
        },
        {
          "name": "enums cross the wire as closed string sets",
          "status": "proven",
          "text": "The host converter is constructed with `allowIntegerValues: false`, so the wire set is closed on the read side, and `WireConventionTests` plants both shapes through the host's own options: an undeclared string and the documents `7`, `0` and `-1`. Before E-45 the string case was rejected and every integer was accepted into an undeclared value, which is why the gap read as fine. Control: restoring the old registration with these tests in place turns all three integer cases red (E-45 repaired)."
        },
        {
          "name": "identifiers are opaque strings",
          "status": "patterned",
          "text": "`Contract_identifiers_cross_the_wire_as_opaque_strings` walks every type in `Kernel.Contracts` through the shared `BodyMemberWalk`, at any depth, and asserts every identifier-shaped member is declared as a type that crosses the wire as a string. Red-correct for a planted `NoteRevisionResponse(int RevisionId, string Summary)`, naming the contract, the member and the type; 158 green before. `ContractParityTests` cannot cover this by construction: it compares property NAMES against a fixture, so an `Id` changing from `Guid` to `int` matches the fixture exactly (E-47 repaired). `patterned` because the registry is `id` and `key` compared heuristically, so a resource address under another spelling owes its own entry (E-74); both directions carry a floor, including six ordinary field names that must not match. The assembly holds exactly one identifier, so the scan asserts it found at least one."
        }
      ]
    },
    "CON-2": {
      "title": "Contract parity fixture",
      "status": "owed",
      "mechanism": "shared `note-contract.fixture.json`, one physical file linked into the C# test project (`None Include` + `Link`) and read natively by the TS test; `ContractParityTests`, `noteContract.test.ts`",
      "note": "one corpus, no copy to rot, and three hand-written lists deciding what it covers (E-60)",
      "obligations": [
        {
          "name": "exactly one corpus, physically one file",
          "status": "proven",
          "text": "The fixture lives once, under `client-web/src/data/__fixtures__/`, and reaches the server test project as a linked item rather than a copy. This is the mechanism class's actual sentence and it holds; the claim's precondition (both consumers under one root that travels together) also holds here, so the sanctioned materializer alternative is not needed."
        },
        {
          "name": "every hand-mirrored contract is pinned by the corpus",
          "status": "owed",
          "text": "Adding a fourth contract type with no fixture entry leaves all 157 tests green, and adding a fixture key nothing consumes is green on both sides (E-60). The C# consumer names three types in `[InlineData]`, the client names three in three `it` blocks, and the fixture holds three keys; no side enumerates anything. trigger: enumerate the public types under `Kernel.Contracts` and assert each has a key, and assert every key has a consumer."
        },
        {
          "name": "drift on either side fails that side's build",
          "status": "patterned",
          "text": "Renaming `NextCursor` on `NoteListResponse` turns `ContractParityTests` red, so the server half binds. The client half reads `Object.keys` off a literal typed as the contract type, so it sees what the literal sets: an optional field added to the interface leaves it green (E-60). A required field would fail compilation, which is why the gap is easy to miss. trigger: derive the client key set from the type rather than a literal."
        }
      ]
    },
    "CON-3": {
      "title": "Read completeness for action-bearing surfaces",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first edition project with a restorable session (the kernel exemplar has none)"
    },
    "CON-4": {
      "title": "A breaking change is detected by machine, approved by human",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first published external contract (a versioned API consumed outside the repo, a partner surface, or the first schema registry; the composed client in the same repo is CON-2's territory, not a published contract)"
    },
    "RT-1": {
      "title": "Realtime discipline",
      "status": "owed",
      "mechanism": "client is REST-only in v1",
      "note": "trigger: first realtime slice; the promotion set is the hub arch test, MaximumReceiveMessageSize, the createHub client seam, and the socket harness scenario"
    },
    "SRV-1": {
      "title": "One deployable unit",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the next edition build pass. Applies to web products; an API-only project re-rules it in its deltas file at adoption"
    },
    "RES-1": {
      "title": "Every outbound call is bounded, and retries are earned",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first external service dependency in an edition project. Database session bounds are RES-6's, carved out so this claim stays about the chokepoint shape"
    },
    "RES-2": {
      "title": "No unbounded queue, pool, or backlog in the process",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first background worker or in-process queue in an edition project"
    },
    "RES-3": {
      "title": "The host dies cleanly, and there is a test that kills it",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first deployed edition host (rides SEC-9's deployment-edge work; the drain budget is a CFG-1 operational setting ruled against the platform's grace period)"
    },
    "RES-4": {
      "title": "Liveness answers alone; readiness answers for its dependencies",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first deployed edition host (lands with RES-3 and SEC-9; the health root SEC-1 already allowlists is the surface this claim splits in two)"
    },
    "RES-5": {
      "title": "Poison is quarantined at a counted limit, never retried forever, never dropped",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first message broker in an edition project (the transactional outbox relay, DATA-8, is the likely first consumer)"
    },
    "RES-6": {
      "title": "Every database session carries four finite bounds, each proven where it lives",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the next edition build pass. Everything here runs on the existing integration tier against the real engine, so proven is reachable immediately; nothing waits on a new dependency"
    },
    "PERF-1": {
      "title": "A hot operation's IO is counted, budgeted, and gated",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the next edition build pass (the v1 exemplar's list endpoint is the first designated seam; the interceptor rides the same seam TEN-4's stamps use)"
    },
    "PERF-2": {
      "title": "A designated hot path's allocations are budgeted per operation",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first designated hot path in an edition project; designation before need fails the YAGNI gate, so the family expects most projects to hold zero designations for a long time, honestly"
    },
    "PERF-3": {
      "title": "A designated algorithmic seam proves its scaling with a counted ratio",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first hand-written algorithm over unbounded input in an edition project; library calls and database queries are not designations (the library's complexity is its documentation's promise, and query shape is PERF-1's and DATA-2's)"
    },
    "PERF-4": {
      "title": "No sync IO and no whole-payload buffering on the serving path",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the next edition build pass (a BannedApiAnalyzers configuration plus one host assertion; the client side already carries the idiom under UI-2's lint discipline)"
    },
    "PERF-5": {
      "title": "Every cache is bounded and every entry expires",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first cache in an edition project; the claim deliberately does not mandate caching anywhere, it prices caching where chosen"
    },
    "PERF-6": {
      "title": "Wall time is never asserted raw",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first wall-time benchmark in an edition project; the lint half can land at the next edition build pass, ahead of any benchmark existing, since its job is to keep the first timing assert out"
    },
    "MOD-1": {
      "title": "Module co-location",
      "status": "owed",
      "mechanism": "the Notes module is co-located by construction; no cross-module rule exists on either side",
      "note": "not built rather than not executed, and the cited client lint belongs to UI-5 (E-62)",
      "obligations": [
        {
          "name": "a module's code lives together on each side of the wire",
          "status": "latent",
          "text": "The skeleton instantiates the layout and Notes is co-located, which is the mechanism class's first part and is real. Nothing asserts it, and with one module nothing could distinguish co-located from coincidental. trigger: the second module."
        },
        {
          "name": "modules meet only through interfaces registered at the composition root",
          "status": "owed",
          "text": "Planted a second module taking a direct constructor dependency on another module's service: green, 157 of 157. The string `module` appears in no test; `DependencyDirectionTests` partitions layers, not modules (E-62). trigger: the second module, and a rule that partitions by module folder with the platform layer whitelisted, which the claim's weakening note requires."
        },
        {
          "name": "client-side deep-path import bans force modules through their public surface",
          "status": "owed",
          "text": "The lint this row cited is `serviceBan`, whose own message names UI-5: it stops screens reaching the transport, not modules reaching each other (E-62). trigger: the second client module."
        }
      ]
    },
    "MOD-2": {
      "title": "Deterministic naming and placement",
      "status": "owed",
      "mechanism": "`NamingPlacementTests` (server source scan) + `namingPlacement.test.ts` (client)",
      "note": "naming and routes bind; the artifact-kind registry is closed over six suffixes and silent about a seventh (E-63)",
      "obligations": [
        {
          "name": "one public type per file, named for the file",
          "status": "proven",
          "text": "A second public type added to `NoteCursor.cs` turns the scan red. The scan walks every non-generated file under `src`, counts delegates separately so a delegate cannot be the smuggled second type, and carries two declared exceptions (`Program.cs`, and `*Contracts.cs` under `Kernel.Contracts` only)."
        },
        {
          "name": "routes originate only from endpoint files",
          "status": "proven",
          "text": "A `MapGet` planted in `SessionVersionMiddleware.cs` turns `Routes_are_declared_only_in_endpoint_files` red. The pattern covers every framework `Map*` binder including `MapHub<T>` and bare `Map(`, and deliberately does not match a custom `Map{Resource}Endpoints` aggregator."
        },
        {
          "name": "artifact kinds form a closed registry, and a new kind is a deliberate kernel edit",
          "status": "owed",
          "text": "The check knows six suffixes and constrains where those six live. `NoteRepository.cs`, a seventh kind placed in `Kernel.Api/Platform/`, is green at 157 of 157 (E-63). The claim's sentence is about the registry being closed, and nothing notices a kind that is not in it. trigger: fail on a file whose suffix the registry does not declare."
        }
      ]
    },
    "DOC-1": {
      "title": "Documentation lifecycle",
      "status": "owed",
      "mechanism": "`docs-lint.mjs`: front matter, placement, work slice ids, the narrow design/ governance, the MET-08 dash check (exclusions declared above)",
      "note": "planted 2026-07-27, eleven plants, five red naming DOC-1 and six green. The stateless halves the previous note admitted were never built while the row read `proven`; they are per-obligation now (E-33). The tree walk skips any directory named bin, obj, dist or node_modules at any depth, inside docs/ included (E-32). The tool is fixture-free and the documented shared-tier edit path disables it with every gate green (E-28). The tool gained `--self-test` on 2026-07-27, so a narrowing of these predicates now fails a control instead of passing silently (E-28 repaired).",
      "obligations": [
        {
          "name": "every markdown declares a kind, and the kind matches the single legal root it lives in",
          "status": "patterned",
          "text": "red-correct on a wrong kind in docs/claims/ and in docs/decisions/. Enumeration is not total: markdown under any bin/obj/dist/node_modules directory is never walked (E-32)."
        },
        {
          "name": "no markdown outside the legal roots, closure over the edition tree",
          "status": "patterned",
          "text": "red-correct for server/handover.md. Same walk, same basename hole (E-32)."
        },
        {
          "name": "working documents carry a slice id",
          "status": "patterned",
          "text": "red-correct for a work doc with no slice key. The value is never validated (`slice: whenever` passes) and the check keys only on the folder name work, so a document declaring itself working in any other root owes nothing (E-33)."
        },
        {
          "name": "status transitions move only forward",
          "status": "owed",
          "text": "unbuilt. Forward and backward transitions on the same file produce byte-identical clean output; the guard is a snapshot lint that holds no prior value (E-33). The previous note admitted this while the row read `proven`. Trigger: a docs-lint reader that compares a declared status against its previous committed value."
        },
        {
          "name": "byproducts die at slice completion, and archived documents are not cited as authority",
          "status": "patterned",
          "text": "`citationFindings` resolves every markdown link in every governed document, relative or root-anchored, and fails a LIVE document that cites an ARCHIVED one. Asymmetric on the citer by design: an archived document may cite anything, because history refers to history; a live one handing a reader a superseded answer is the failure. Red-correct for the edition's one real inter-document citation, README.md to VERIFICATION.md, with the target archived (E-33 repaired). `patterned` because it reads MARKDOWN LINKS: the edition README also names the runbook in backticks as prose, which is a citation a reader follows and this predicate cannot see. The first clause, that a byproduct dies at slice completion, has no mechanism either; nothing compares a slice's completion against the documents it produced. Trigger for both: a citation form register, and a slice-completion signal the tool can read."
        },
        {
          "name": "the docs-lint gate runs in the CI loop",
          "status": "owed",
          "text": "the edition's own ci.yml is a template; deleting its only invocation of the guard is reported by nothing. What runs docs-lint in this repo is kernel.yml, which the row did not name (E-39). Trigger: instantiation arming the loop, which E-24 established ships no mechanism."
        }
      ]
    },
    "DEC-1": {
      "title": "A module's behaviour traces to a decision",
      "status": "owed",
      "mechanism": "docs-lint fails a `docs/decisions/*.md` without a `provenance` field (the upward link); the decision template models the field",
      "note": "the downward link is owed (trigger: first regeneration of a module from its decisions); the artifact-order check rides here too (trigger: next acceptance test); the upward provenance lint is built and proven"
    },
    "UI-1": {
      "title": "Token source lockstep",
      "status": "latent",
      "mechanism": "`tokenCoverage.test.ts` reads the export at its canonical home `design/prototype/_ds/colors_and_type.css` (every CSS var has a token, count > 50)",
      "note": "Lowered from `proven` to `latent` on 2026-07-27 by the acceptance test. The mechanism binds and is not in question; its SUBJECT is the kernel`s own placeholder. `design/prototype/_ds/colors_and_type.css` ships 58 variables, deliberately over the floor of 50, and its own header says to replace it with a real export at instantiation, so `tokenCoverage.test.ts` goes green in a seeded project whose design system is the kernel`s filler and whose product design does not exist. That is `latent` by the vocabulary`s own definition: the mechanism exists and has had no real subject yet. Compounding it, manifest step 6 demands the complete design export at seed time, which the method`s own skill order makes impossible (seed runs immediately after gate 1; the export is two skills away, behind decompose and design), and it duplicates x2:lock. The manifest offers no fallback and part C admits no blocked state (E-27). TEST-3 and HUM-1 both carry an instantiation asterisk; this row needed one and did not have it. Finding: E-27."
    },
    "UI-2": {
      "title": "Literal visual values are lint errors",
      "status": "patterned",
      "mechanism": "`eslint.config.js` bans visual literals in styles: colors, font props, radii, hairlines, and spacing/dimension literals (numerics matched on `raw`), with the structural allowlist",
      "note": "the ban is not total: bare negative numerics escape every dimension selector, because the numeric selectors use a direct-child combinator and a negative parses as Property > UnaryExpression > Literal, and the per-side axes (borderTopWidth, outline) are in no alternation (E-31). Planted 2026-07-27, nine plants, seven red naming UI-2. The config itself is fixture-free and can be narrowed to reach nothing with `npm run verify` green (E-30). The config gained an executable extent assertion on 2026-07-27 (E-30 repaired): 49 assertions over 19 caught and 22 ignored controls plus an independent 54-colour floor and 21 gated axes, red under all five sabotage probes including a drop to `warn` severity, while `eslint .` stayed green under every one.",
      "obligations": [
        {
          "name": "error-tier rules ban literal visual values and gate the client verification chain",
          "status": "proven",
          "text": "planted 2026-07-27 across seven categories (hex, named colour, bare dimension, px string, radius, border shorthand, fontWeight); each failed `eslint .` with the message naming UI-2, and `verify` short-circuits on it. The reach is now asserted executably rather than by plant alone (E-30)."
        },
        {
          "name": "coverage of every axis and every syntactic form a literal can take",
          "status": "patterned",
          "text": "`marginTop: -8` and `borderTopWidth: 1` both pass (E-31). Each newly gated axis and each newly reachable syntactic form owes its own selector, which is the standing debt this tag names. The fourteen measured holes are carried as passing controls in the extent test, so closing one turns that test red and has to be argued (E-31)."
        }
      ]
    },
    "UI-3": {
      "title": "Screens compose primitives only",
      "status": "owed",
      "mechanism": "`themeBan` in eslint plus the naming scan restrict theme imports to `src/components/**` and `src/theme/**`",
      "note": "the token half is enforced; the other two things the statement bans in screens are not (E-69)",
      "obligations": [
        {
          "name": "the primitive component layer is the sole consumer of theme tokens",
          "status": "proven",
          "text": "Two independent mechanisms, the eslint `themeBan` over `**/theme` and `**/theme/**` and a source scan in `namingPlacement.test.ts`, and both restrict the importer set to `components/` and `theme/`. That matches the claim, which speaks of the primitive LAYER. The conformance row previously read `Text`/`Button` are the only token importers, which is a rule neither mechanism has: a third primitive importing tokens is legal and correct (E-69)."
        },
        {
          "name": "screens create no style sheets and use no raw platform text elements",
          "status": "owed",
          "text": "A bare `<p>` planted in `NotesScreen` is clean on tsc, eslint and 65 of 65 tests (E-69). UI-2's literal bans fire on visual VALUES, so an element carrying no inline style passes them, and nothing else looks. trigger: a screens-scoped lint on raw text elements and on style-sheet creation, which is the same kind of rule as the one already there."
        },
        {
          "name": "client business logic lives in separately testable services, hooks and reducers",
          "status": "latent",
          "text": "The claim's own weakening note says this half is structural rather than lint-provable: the skeleton's shape is the mechanism and the fidelity suites plus off-framework unit tests are the evidence. With one screen and one repo the shape holds and nothing has tested that it stays true. trigger: the second module."
        }
      ]
    },
    "UI-4": {
      "title": "Fidelity ledger",
      "status": "owed",
      "mechanism": "`NotesScreen.fidelity.test.tsx` renders every branch and asserts the `data-atom` set against a hand-transcribed ledger, in both directions",
      "note": "both directions bind for atoms that opt in; fabricated UI that carries no marker is invisible to both (E-68)",
      "obligations": [
        {
          "name": "every ledger atom renders (exhaustiveness)",
          "status": "patterned",
          "text": "Renaming a ledger atom so it stops rendering turns the test red, and both branches (populated and empty) are rendered so a conditional atom cannot hide. Per-screen by construction: this is one screen's suite and each new screen owes its own. trigger: the second screen."
        },
        {
          "name": "nothing renders beyond the ledger (de-fabrication)",
          "status": "owed",
          "text": "The assertion iterates `querySelectorAll('[data-atom]')`, so its subject is the set of elements that already declare themselves part of the ledger's vocabulary. A fabricated `<div data-atom=\"promo-banner\">` is caught; a fabricated `<p>` with no marker is green at 65 of 65 (E-68). This is the direction the claim exists for, and it can only police code that is cooperating. trigger: assert over the rendered element tree with a declared allowlist of invisible wrappers, so an unmarked element fails rather than being absent."
        },
        {
          "name": "the ledger is exported from the locked prototype, not hand-maintained",
          "status": "owed",
          "text": "The ledger is a hand-written const in the test file. The claim names this itself: \"The v1 exemplar ledger is hand-authored: the prototype-to-ledger exporter is owed, and until it exists the ledger is a second hand-maintained copy of the prototype, carrying exactly the UI-1 divergence risk this claim's export mechanism is meant to remove.\" trigger: the first slice with a design tool that emits a machine-readable prototype."
        }
      ]
    },
    "UI-5": {
      "title": "Thin UI over tested services, composed entrypoint exercised",
      "status": "owed",
      "mechanism": "eslint `serviceBan` on `api/**` and `data/**` outside the composition root; `npm run smoke` boots the actual `src/main.tsx` in jsdom and asserts a real request per primary flow",
      "note": "the import ban binds; transport logic that imports nothing walks past it, and the smoke has not been run here (E-70)",
      "obligations": [
        {
          "name": "modules outside the composition root import neither the transport nor the data services",
          "status": "proven",
          "text": "Importing `../../data/notesRepo.ts` into `NotesScreen` fails eslint with UI-5's own message. Centralized: a violating import cannot merge. Type-only imports stay legal, which the claim requires explicitly. This closes the route the acceptance-test pilot's failure actually took."
        },
        {
          "name": "the client UI holds no business or transport logic of its own",
          "status": "owed",
          "text": "A screen-local function calling `fetch` with the API base URL and a bearer token from the environment imports nothing, so the ban cannot see it: clean tsc, clean eslint, 65 of 65 tests (E-70). The realistic arrival is a screen needing one endpoint the repo does not expose yet. trigger: a screens-scoped ban on `fetch`, `XMLHttpRequest`, `EventSource` and `WebSocket`."
        },
        {
          "name": "the composed entrypoint is exercised against a running server",
          "status": "latent",
          "text": "`composedApp.smoke.ts` imports the real `src/main.tsx`, wraps `globalThis.fetch` to record what leaves, and waits for a real GET and a real POST per primary flow. It needs the server up, which needs the engine, so it was read and not run here. Per-seam by the claim's own note: each new primary flow owes its smoke line. trigger: a run with the stack up."
        }
      ]
    },
    "DEP-1": {
      "title": "Dependency quarantine",
      "status": "owed",
      "mechanism": "CPM exact pins + lockfiles + locked-mode restore (`--locked-mode` / `npm ci`) + `VERSIONS.md` (cooling-off window header, container-images section, kernel-provenance section) + docs-lint ledger, override, image, cross-surface image agreement, window-number and kernel-provenance checks",
      "note": "Planted 2026-07-27, seventeen plants: four red-correct, twelve green, one red-different. Four of the twelve greens were repaired on 2026-07-28 and three obligations moved from `owed` to `proven`: cross-surface image agreement, package.json overrides, and the cooling-off window number. Two of the three found a live defect on their first run against a real tree (E-76): node-react's own VERSIONS.md asserted the superseded window as a live one under a header declaring the current number, and the E-38 image widening read two English sentences as floating container images while its self-test stayed green. The row stays `owed` on its weakest obligation and only that one: publish dates are shape-tested, never parsed against today, which needs a registry lookup this tool does not make. Still carried and not repaired: the CI toolchain is unpinned and unledgered (E-1); the lockfile-drift remedy fails the build but npm's EUSAGE names npm rather than DEP-1, in a different job from every other check, which is a third-outcome result and not a pass; source mapping is deletable unreported (E-37). The `postcss` advisory-rule pin (ruling 9a, 2026-07-26) is now checked like any other pin rather than parsed and ignored, and leaves the advisory section on 2026-08-11 (E-18).",
      "obligations": [
        {
          "name": "every direct dependency is pinned exactly, dated, and restored in locked mode",
          "status": "patterned",
          "text": "the ledger check is keyed on name AND version and is red-correct for a floating range and for an unledgered direct dependency. Source mapping is deletable unreported, package.json overrides are never read, and publish dates are shape-tested only, never parsed against today (E-37)."
        },
        {
          "name": "every container image is pinned tag-plus-digest and ledgered",
          "status": "patterned",
          "text": "red-correct for an unpinned mcr.microsoft.com image in a shell script. E-38 is half closed: a Dockerfile `FROM` line is now read whatever the registry host, so Docker Hub shorthand (the obligation's own stated violation shape) is caught, and an extensionless `Dockerfile` is now a scanned surface. The closure is gated on the file being a Dockerfile, because the ungated version read two English sentences in VERIFICATION.md beginning a line with the word `from` as floating images while its self-test stayed green (E-76). Still `patterned` for the residue that remains a carried control: a bare `name:tag` outside a FROM line, in a compose file for instance, is invisible, and widening to it would match every `key: value` in the tree."
        },
        {
          "name": "one image value across every surface naming an image",
          "status": "proven",
          "text": "`imageAgreementFindings` collects every image reference in the tree into a map keyed by repository and fails when one repository is named with more than one value. The ledger's own row is in the comparison, reconstructed from its two cells, so ledger-versus-code drift is visible and not only code-versus-code. Red-correct: changing the digest in `scripts/db-up.sh` alone reports the repository, both values, the four surfaces holding one and the one holding the other. `proven` rather than `patterned` because it needs no registry and no entry per surface: it compares whatever it finds against whatever else it finds, so a sixth surface is covered the day it is written (E-37, E-64 repaired)."
        },
        {
          "name": "a transitive pin taken under the advisory rule carries its own ledger row",
          "status": "proven",
          "text": "The npm reader now reads `overrides` alongside dependencies and devDependencies, including npm's nested per-parent form, so an override is checked for exactness and for a dated ledger row exactly as a direct pin is. That is what DEP-1's advisory rule asks for in so many words, a row \"whether the dependency is direct or transitive\", and an override is the most deliberate pin in the file because it overrules what a dependency asked for. Red-correct: moving the shared tier's postcss override off its ledgered version reports it by name and version (E-37 repaired)."
        },
        {
          "name": "the cooling-off window number lives in exactly one place",
          "status": "proven",
          "text": "The number is parsed from the VERSIONS.md header and every other assertion of it in the tree is compared against it. It does not ban restating the number, which would falsify dated records naming the window in force at the time; it makes restating it unable to drift, so a cut turns every stale statement red and each one is then decided as history or as a live rule. Red-correct for a second number planted in BUILD-BRIEF.md. It found a live instance on its first run: node-react's own VERSIONS.md stated the superseded window as a live one while its header declared the current number (E-76, repaired). Residue, stated rather than carved out: a future dated record naming a superseded window fails this check and must cite the header instead of restating the number."
        },
        {
          "name": "kernel provenance is ledgered and moves only with the mechanisms",
          "status": "latent",
          "text": "the section's absence fails docs-lint, red-correct. No real pin exists until the next instantiation fills one, so the comparison half has never run."
        },
        {
          "name": "publish date is checked against the cooling-off window",
          "status": "owed",
          "text": "unbuilt, and the row has said so in prose since before this round. Trigger: the publish-date CI check DEP-1 already names as its upgrade path."
        }
      ]
    },
    "DEP-2": {
      "title": "Standing advisory sweep with a remediation clock",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the next edition build pass, moved there from the first armed CI loop by adjudication ruling 9c on 2026-07-26. The mechanism is an advisory sweep over the pinned set, which is buildable in the shared tier today with no instantiation and no third-party action, and the old trigger deferred the only mechanism that would report an advisory the kernel already ships with. The publish-date check DEP-1 names as its upgrade path rides the same job"
    },
    "TEST-1": {
      "title": "Tier strategy",
      "status": "owed",
      "mechanism": "SQLite unit/arch tier, Testcontainers integration tier with a unique database and the real migration, e2e through the client service layer; EF InMemory banned by `NamingPlacementTests`; the edition's `ci.yml` runs all three",
      "note": "the ban binds and the integration fixture is correctly shaped by reading; it has never run here, and the engine pin is duplicated four times (E-64, E-49) 2026-07-27: E-39 is repaired. `kernel.yml` gained a `dotnet-server` job running `restore --locked-mode`, `build -warnaserror` and the architecture and unit tiers by name, so the 203 guards this edition ships are now protected by a continuous process rather than by whoever remembers. That was the reason a neutered guard survived twice (E-29, E-30).",
      "obligations": [
        {
          "name": "no fake in-memory database provider in any test project",
          "status": "proven",
          "text": "Adding `Microsoft.EntityFrameworkCore.InMemory` to the unit project turns `Test_projects_do_not_reference_ef_in_memory` red. The scan is case-insensitive and covers `Directory.Build.props` and `Directory.Packages.props` as well as every test csproj, so neither a casing trick nor a shared props file evades it."
        },
        {
          "name": "the integration tier uses the real engine, disposable, unique per run, with real migrations",
          "status": "latent",
          "text": "`SqlServerFixture` starts one ephemeral container, points it at `Kernel_{guid}` and runs `MigrateAsync()` rather than `EnsureCreated`, which is exactly the claim's shape. It has not been executed here: no container runtime is available. Until 2026-07-27 that produced four RED tests whose messages were container startup errors, in a suite where red means a conformance defect (E-49); they now skip, naming the claim and naming what goes untested, which is engine-specific by construction (`DateTimeOffset` ordering under the keyset cursor, composite-key behaviour). Controlled: forcing the runtime probe true turns all four red again, so the attribute is not skipping unconditionally. `latent` is what a tier that skips everywhere is worth, and CI is the gate against it staying that way. trigger: a run with a container runtime available."
        },
        {
          "name": "exactly one provisioning path",
          "status": "owed",
          "text": "Three: Testcontainers for the integration tier, `scripts/db-up.sh` for development, and a workflow `services:` container plus `dotnet ef database update` for e2e. The claim's harm paragraph names this shape by name. The pinned image digest is written out four times and changing one copy alone leaves everything green (E-64). trigger: one committed machine-readable pin that the script, the fixture and the workflow all read."
        },
        {
          "name": "all three tiers run in the CI loop",
          "status": "latent",
          "text": "The edition's `.github/workflows/ci.yml` restores locked, builds with `-warnaserror` and runs all three tiers. It is a template that becomes real CI only at instantiation, and instantiation ships no arming mechanism (E-24), so it has never run. This repository's own CI now runs the two engine-free tiers, 203 architecture and 58 unit, on every push and pull request (E-39 repaired); the engine tier stays in the edition template where the engine is. So the obligation is split by fact rather than by wish: two of the three tiers are continuously run against this edition's code, and the third runs only where an engine exists. trigger: E-24, for the tier that needs the instantiated loop."
        }
      ]
    },
    "TEST-2": {
      "title": "E2E harness",
      "status": "owed",
      "mechanism": "`tools/harness/main.ts` drives the real client services out of process (NDJSON + redaction, fails loudly not vacuously); a completeness self-audit wraps the enumerated methods and diffs them against what ran",
      "note": "the harness is real and the audit's shape is right; its reach is one class, prototype members only (E-67)",
      "obligations": [
        {
          "name": "an out-of-process harness drives the real, unmodified client service layer",
          "status": "proven",
          "text": "`main.ts` imports `KernelClient` and `NotesRepo` themselves, not a parallel HTTP client and not a browser puppet, and runs as a node process against a running server. This is the mechanism class's own sentence and the harness satisfies it by construction."
        },
        {
          "name": "the floor is complete: every public method of every client data service has a scenario",
          "status": "owed",
          "text": "The audit reads `Object.getOwnPropertyNames(NotesRepo.prototype)`. A second data service is invisible: `TagsRepo` with two public methods and no scenario leaves the coverage line byte-identical, tsc and eslint clean, 65 of 65 tests green. A method declared as a class-field arrow function is invisible too, because it is an instance property and never enters the set that gets diffed (E-67). trigger: enumerate the class exports under `src/data/` rather than naming one, and enumerate instance members as well as prototype members."
        },
        {
          "name": "the deterministic tier runs in CI",
          "status": "latent",
          "text": "The edition's `ci.yml` carries the `e2e-wire` job which starts the API and runs the harness. It has never executed: the workflow is a template until instantiation (E-24). This repository's own CI now runs the edition's engine-free tiers (E-39 repaired) and deliberately does not run `e2e-wire`, which needs a live engine and a booted server. Locally it needs the stack up, which needs a container runtime. trigger: the first instantiation, or a local run with the engine available."
        },
        {
          "name": "harness output redacts secrets",
          "status": "patterned",
          "text": "`redact.ts` scrubs Authorization headers and JWT-shaped strings and every emitted line goes through it; `redact.test.ts` proves the true positives. It asserts nothing about what survives, and its JWT pattern matches any dotted identifier (E-58). trigger: anchor the pattern and add a passing case for an identifier that must survive."
        }
      ]
    },
    "TEST-3": {
      "title": "CI loop",
      "status": "latent",
      "mechanism": "`ci.yml`: server, client, docs-lint, secret-scan, e2e-wire; `tools/gate-check.mjs` reads the armed state back off the forge and `--self-test` drives its predicate against six violations and seven controls",
      "note": "the loop runs everything; arming stays a human step, and skipping it is now loud rather than invisible",
      "obligations": [
        {
          "name": "every mechanism the catalog names executes automatically in CI on every push and pull request",
          "status": "patterned",
          "text": "`ci.yml` declares server, client, docs-lint, secret-scan and e2e-wire. The workflow has never executed on a forge, and one of the five jobs is red on day one in a seeded project for a missing allowlist the file set does not carry (E-25)."
        },
        {
          "name": "the loop gates merges, armed at instantiation, with the arming step carried and verified",
          "status": "latent",
          "text": "Arming is an instantiation step and both TEST-3 and HUM-1 word it that way: the manifest carries the step and the kernel acceptance test verifies it. Until 2026-07-27 the kernel shipped the step as one imperative sentence and nothing that could see whether it had been done, so an armed instantiation and an unarmed one were indistinguishable by every means the kernel shipped (E-24). `tools/gate-check.mjs` closes the verification half: it derives the required check names from the workflow rather than listing them, requires a pull request and code-owner review, catches a ruleset in evaluate mode, and fails rather than passes when there is no remote, no token, or no readable workflow. Red-green against its own controls, and the three unreachable paths each exit non-zero, measured in a seeded-shape tree. `latent` because it has never been run against a real forge: that needs a token this repository does not have. trigger: the first instantiation, which is also E-24's own trigger."
        }
      ]
    },
    "TEST-4": {
      "title": "Real-runtime boot proof",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the next edition build pass. UI-5's composed smoke already proves entrypoint wiring in-process; the real-boot script assertion and built-form pinning are the promotion set that completes it"
    },
    "HUM-1": {
      "title": "Irreversible surfaces get a human turn, unconditionally",
      "status": "latent",
      "mechanism": "`.github/CODEOWNERS` covers migrations, contracts and `docs/contracts/`, reconciled against `edition.json``s `irreversibleSurfaces` so renaming one and forgetting the other is loud; docs-lint fails if any of the three loses its owned entry, and fails outside the kernel repo if an entry names only the `@OWNER` placeholder; branch protection requires code-owner review; `tools/gate-check.mjs` reads back whether code-owner review is actually required",
      "note": "the declaration half is proven; the merge-gate half is now readable and has not yet been read against a real forge",
      "obligations": [
        {
          "name": "every irreversible surface is enumerated and declared to require a named human review",
          "status": "proven",
          "text": "the three surfaces are declared in `edition.json` and reconciled against `.github/CODEOWNERS`, and an entry naming only the shipped placeholder is now refused outside the kernel repo. Red-green proven in a seeded tree: placeholder gives three failures, one per surface, each naming the path and the claim; a real handle gives ok; the kernel itself stays green because its placeholders are legal there; and the pre-repair predicate with the placeholder still in place goes green again, which reproduces the hole (E-23)."
        },
        {
          "name": "the merge gate requires that review, armed at instantiation",
          "status": "latent",
          "text": "Measured absent 2026-07-27: no forge call, no ruleset, no token, no readback, and every hit for branch protection was prose (E-24). `tools/gate-check.mjs` now reads whether code-owner review is required on the default branch, across both the ruleset and classic branch-protection shapes, because reading only one would report a gated repository as ungated. The self-test carries this as its own violation case, separate from TEST-3's, so a change that stopped checking code-owner review would be caught without the status checks changing. `latent`: never run against a real forge, which needs a token this repository does not have. trigger: the first instantiation."
        }
      ]
    },
    "AI-1": {
      "title": "Server-injected identity",
      "status": "patterned",
      "mechanism": "`ToolExecutor` reject-then-inject + throw-on-unset, rejecting server-owned keys recursively through nested objects and lists, matched through the shared `NameComparison` over `TenantNames` plus the OIDC claim and delegation vocabulary; `ToolExecutorTests` incl. a nested-payload case and a thirty-case extent theory",
      "note": "the chokepoint, its recursion and its reach all hold; what remains is the JsonElement subtree the claim itself carves out",
      "obligations": [
        {
          "name": "actor-supplied identity, tenant and scope are rejected before any merge",
          "status": "patterned",
          "text": "Rejection is case-insensitive and recurses nested dictionaries and lists, both covered by `ToolExecutorTests`. The reach was `ServerOwnedKeys`, twenty-one entries matched by equality: against a twenty-name floor written independently of it, six escaped, `workspaceId`, `accountId`, `workspaceSlug`, `organisation_id`, `tenantIdentifier` and `actorId`, while the registry's own comment claimed parity with the URL and EF-model guards (E-53). `NameComparison` and the canonical tenant registry now live in the application assembly, so this chokepoint reads the same comparison and the same list as those guards; the floor and a ten-name cost side are both asserted, and restoring the equality set turns six of them red. Still `patterned` rather than `proven`, now for the reason the claim names rather than a defect: a `JsonElement`-typed value is a subtree the executor does not walk, so every tool accepting a raw JSON payload owes its own review. trigger: the per-tool parameter allowlist the claim names as the upgrade, or the first tool that takes a raw JSON payload."
        },
        {
          "name": "server context is injected unconditionally",
          "status": "proven",
          "text": "`arguments[\"tenantId\"] = tenantId` runs after sanitizing, on every call, with no branch that can skip it."
        },
        {
          "name": "an unset context throws rather than proceeding unscoped",
          "status": "proven",
          "text": "`tenantScope.Current` is read before the tool is reached and throws when unset; `ToolExecutorTests.Invocation_without_a_scope_throws` covers it and turns red when the accessor is made to fail open."
        }
      ]
    },
    "AI-2": {
      "title": "Untrusted content, no authority",
      "status": "patterned",
      "mechanism": "`ListNotesTool` read-only + `ListNotesToolReadOnlyTests`; `docs/decisions/ai-trust-tiers.md`",
      "note": "the v1 tool has its guard test, each new tool owes its own; full taint tracking and the SSRF egress guard are owed (trigger: outbound agentic requests)",
      "obligations": [
        {
          "name": "capability derives from server configuration and the principal, never from content",
          "status": "patterned",
          "text": "the trust tiers are named in `docs/decisions/ai-trust-tiers.md` and AI-1's chokepoint is where the derivation actually happens; full taint tracking is out of v1 by the claim's own phasing, not deferred by this edition."
        },
        {
          "name": "a tool declared read-only is proven read-only by test",
          "status": "patterned",
          "text": "`ListNotesToolReadOnlyTests` covers the v1 tool; each new tool owes its own guard test."
        }
      ]
    },
    "AI-3": {
      "title": "Prompt architecture",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first model-backed feature in an edition project"
    },
    "OBS-1": {
      "title": "External-effect seam observability",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first external side-effect port in an edition project. This claim opens the observability family the v1 cut deliberately deferred"
    },
    "OBS-2": {
      "title": "Refusals and privileged actions are security events",
      "status": "owed",
      "mechanism": "",
      "note": "trigger: the first deployed edition host (alongside SEC-9; the refusal seams it instruments are already built and tested, so the events attach to existing tested paths)"
    }
  }
}
