# kernel/dotnet-react

The .NET 9 + React edition of the X2 kernel. It realizes the portable claims in `../claims/` as running,
enforced mechanisms: architecture tests that fail the build when an invariant is broken, a fail-closed tenancy
composite, a deny-by-default HTTP host, a bounded-read data layer, a token-locked client, an out-of-process e2e
harness, and a CI loop that runs all of it on every push. The product placeholder name is `Kernel`.

The claims catalog holds 69 claims and this edition declares a status for every one of them in
[`conformance.json`](conformance.json), the single machine-readable record the table below is generated from:
37 realized (25 `proven`, 6 `patterned`, 2 `latent`) and 32 `owed`, each owed claim carrying the named trigger
that promotes it. The next edition build pass picks up the subset whose trigger is that pass (RES-6, DATA-9,
SEC-11, PERF-1, PERF-4, DATA-6, SRV-1, TEST-4, the DATA-11 and PERF-6 lint halves, and the TIME-1 and TEN-4
extensions); the rest wait on their named triggers (first external dependency, first broker, first deployed
host, first cache, first published contract, first identity slice).

Every guard in this edition carries a red-green proof: the violation it exists to catch was deliberately
planted, the guard confirmed red, the violation reverted. Five verification rounds have run (the first build, an
adversarial review that closed real evasion paths, a full code review that closed runtime and guard-binding
defects the guard mutations could not reach, the invariants pass, and the versioning pass that landed the
kernel-provenance check), and each round's mechanisms were proven as they landed, not in a batch at the end. The
dated history and the proof tables are in [`VERIFICATION.md`](VERIFICATION.md).

## What is here

```
BUILD-BRIEF.md   the handover this edition was built from (kept for provenance)
VERIFICATION.md  the dated verification history and the four red-green proof tables
VERSIONS.md      the DEP-1 ledger: kernel pin, packages + container images, publish dates; the 30-day window header
conformance.json the per-claim status record: one row per catalog claim, the README table generated from it
edition.json     this edition's stack-specific declarations for the shared linter: which files hold its direct
                 dependencies (DEP-1 ledger check) and where its irreversible surfaces live (HUM-1 owner check)
.github/workflows/ci.yml   the TEST-3 loop (template; moves to repo root at instantiation)
.github/CODEOWNERS         the HUM-1 gate on migrations and published contracts (armed at instantiation)
.gitattributes   eol=lf everywhere (INV-03: no CRLF churn across the Windows/Mac/Docker boundary)
.vscode/tasks.json         thin editor tasks; each just calls a script or CLI below (INV-03)
scripts/         db-up (pinned image, named volume, arm64 precheck), db-down, db-migrate, dev-setup, e2e
server/          .NET 9 solution: Contracts, App, Persistence, Api + three test projects
docs/            the DOC-1 tree (claims, decisions, contracts, runbooks, work); the templates are shared
```

Composed from the shared tier (`kernel/shared/`, one home for what is not stack-specific; see its README):

```
client-web/      Vite + React 19: tokens, primitives, notes module, scans, e2e harness + composed smoke
design/          the INV-01 design home: prototype/ (provenance README + imported artifact + _ds) and ledger/
docs/*/_template.md        the DOC-1 templates, one per registry kind
tools/docs-lint.mjs        the DOC-1 / TEN-5 / DEP-1 / DEC-1 / HUM-1 / conformance / MET-08 gate (plain node)
tools/conformance.mjs      validates conformance.json and generates the table below
```

**Never hand-edit a composed path.** Edit `kernel/shared/` and run `node kernel/tools/compose.mjs`;
`--check` fails the build on any divergence, so a local edit to a composed copy cannot merge. Composed copies
are committed rather than referenced in place because an edition has to be a self-contained file set that
instantiation copies into a new repo: the committed tree is the instantiated shape, so the loop tests exactly
what seeding produces. The reasoning is in `kernel/tools/compose.mjs`.

**Declared docs-lint exclusions (INV-08).** docs-lint governs authored files and skips imported ones, and the
exclusions are declared here so they are a visible decision, never a silent patch: (1) any path registered in
`.gitmodules` (vendored submodules are third-party by definition); (2) everything under `design/` EXCEPT the two
authored shapes it governs, `design/prototype/README.md` (the lock record, kind `provenance`) and
`design/ledger/*.md` (kind `ledger`, slice-stamped). Lockfiles are exempt from the dash check (generated
third-party metadata); every other authored file in the tree is scanned.

## Running it

See `docs/runbooks/local-development.md`. In short: `scripts/dev-setup.sh`, `scripts/db-up.sh`,
`scripts/db-migrate.sh`, `dotnet test`; `npm ci && npm run verify` in `client-web/`; and `scripts/e2e.sh` for the
harness plus the composed-entrypoint smoke (or drive everything from the `.vscode` tasks).

## Architecture spine

The server layers run downward only (DATA-1): Contracts references nothing; App references Contracts; Persistence
implements App's store interfaces; Api is the composition root over App + Persistence. `DependencyDirectionTests`
enforces the direction and that endpoints never touch Persistence directly.

**Provider ports (CONF-02).** Every external side effect (message delivery, a model call, clock-like effects)
sits behind a provider interface bound at the composition root. This is not style: it is what makes the e2e floor
closable. The gated harness profile (TEST-2) can only re-bind an effect that is behind a port; an effect
instantiated inline is unreachable by the profile, and the harness completeness rule breaks there. The
acceptance-test pilot proved the payoff concretely: when full e2e collided with two deliberate design choices
(out-of-band OTP, a live paid model call), both effects were already behind ports, so the resolution was pure
composition-root re-binding, a ruling instead of a redesign. Review each new external effect at its slice for
exactly this property.

## Instantiation manifest

The instantiation layer is where the acceptance test found the kernel weakest (a quarter of the pilot's human
turns were bootstrap the handover failed to ship), so instantiation is a MANIFEST: a concrete file set plus setup
steps plus a verification set, verified AS A SET, not one by one from memory. x2:seed runs it; the kernel
acceptance test verifies that instantiation ships and arms the whole set, extending TEST-3's keystone from
"instantiation arms branch protection" to "instantiation ships and arms everything below". A gap found while
seeding is a finding for the invariants pass, logged as a turn.

**A. The file set** (copy into the new repo root): `server/`, `client-web/`, `docs/`, `tools/`, `scripts/`,
`design/`, `.github/` (workflow AND CODEOWNERS), `.vscode/`, `.claude/` (the prompt-submit hook, PC-10),
`.gitignore`, `.gitattributes`, `VERSIONS.md`, `conformance.json`, `edition.json`, `secret-scan.allow.json`.

This enumeration IS the file set. It is not a straight copy of the edition directory, and the difference runs
both ways (E-25). A copy takes what is gitignored: `.env`, which holds a live development SA password, plus
`node_modules/`, every `bin/` and `obj/`, and any scratch project left under `server/src/`. The enumeration used
to drop two tracked files the edition needs: `.claude/settings.json`, which `skills/seed/SKILL.md` already
asserted was part of this set, and `secret-scan.allow.json`, without which the `secret-scan` job that step 4
tells you to arm cannot pass on day one. The edition tree is already composed, so seeding never reaches into
`kernel/shared/`. BUILD-BRIEF.md and VERIFICATION.md stay behind (kernel provenance,
not project material), and so does THIS README: the seeded project writes its own.

`conformance.json` travels, because it is the project's own conformance statement from that point on, pinned to
the catalog date in its `catalogPassDate` field and moved only by a deliberate upgrade. Two of docs-lint's
conformance checks are conditional in the seeded shape, and it prints a `note:` line for each rather than
passing silently:

- **Catalog completeness is not checked**, because a seeded project carries the edition and not the catalog.
  The record is still validated (statuses, mechanisms, triggers).
- **The README table is not checked**, because the marker pair lives in this file, which stays behind. The
  seeded project's README belongs to the product, and nothing here forces a conformance table into it. A
  project that wants one adds the `conformance:begin` and `conformance:end` HTML comment markers, each on
  its own line, to any README it owns, and gets the identical drift gate.

**B. Setup steps, in order:**

1. **Rename the product.** Replace `Kernel` with the product name across project file names, namespaces, the
   `.sln`, the `UserSecretsId` in `Kernel.Api.csproj`, the connection-string key `ConnectionStrings:Kernel`, the
   scripts' container/volume names, `.vscode/tasks.json` labels, CODEOWNERS paths, and the `file` paths in
   `secret-scan.allow.json`, whose own header says an entry matching nothing fails the scan, so carrying it
   unrenamed turns one defect into another. Then the CONFIG VALUES the
   pilot missed (INV-06): `Jwt:Issuer` and `Jwt:Audience` in `appsettings.json` are `kernel` and rename with the
   product. The HUM-1 paths are renamed in TWO places, `.github/CODEOWNERS` and `edition.json`'s
   `irreversibleSurfaces`, and docs-lint fails if they disagree, so renaming one and forgetting the other is
   loud rather than a silent loss of coverage.
2. **Git setup.** `git init`; the canonical `.gitignore` and `.gitattributes` (`eol=lf`) are in the file set;
   first commit only when directed (the never-commit constraint is in the project CLAUDE.md that x2:seed writes).
3. **Write the kernel pin (DEP-1).** Fill `VERSIONS.md`'s Kernel provenance section mechanically from the kernel
   checkout the file set was copied from: remote (`git remote get-url origin`), commit (`git rev-parse HEAD`),
   and the catalog pass date (the date of the latest pass paragraph in the claims catalog README); the edition
   field is prefilled. Mechanical means the seed runs the commands; a human is never asked to transcribe a hash.
   If the kernel arrived without git metadata (an archive), record the archive's stated source and the catalog
   pass date, note the missing commit in D-000, and treat the pass date as the load-bearing value.
4. **Arm the gates (TEST-3 + HUM-1, the step that gets skipped).** Move `.github/workflows/ci.yml` to the repo
   root; branch protection requires all five jobs AND code-owner review; replace `@OWNER` in CODEOWNERS with the
   product owner. Until this step the loop runs but blocks nothing. Arming stays a human step, which is how both
   claims word it; what the kernel owes is the readback, so finish with `node tools/gate-check.mjs` and do not
   treat this step as done until it exits ok. It names exactly what is missing, and it fails rather than passes
   when it cannot reach the forge.
5. **Dev environment.** `scripts/dev-setup.sh` (SA password to gitignored `.env`; `Jwt:Key` and connection string
   to user-secrets), then `scripts/db-up.sh` (pinned image, named volume, arm64 precheck) and
   `scripts/db-migrate.sh`. Ports 5080/5173/1433; Node 24+ for the harness and smoke. If the product vendors a
   submodule: `git submodule update --init` joins this step (docs-lint auto-skips `.gitmodules` paths). If the
   product needs a provider key (the pilot's Anthropic key): it goes to user-secrets here, and the bootstrap line
   is added to `dev-setup.sh`, never a manual copy no script covers.
6. **Design home (INV-01), carried forward deliberately.** The Claude Design export cannot exist when this step
   runs. x2:seed's Next is decompose, decompose produces D-000, and x2:design runs only once D-000 exists, so the
   export this step used to demand is two skills downstream of the skill executing it, with no fallback branch and
   no deferred state, while part C says the seed is not done until all of it passes (E-27). Seeding therefore
   ships the kernel's placeholder `design/prototype/_ds/` and records the debt instead of pretending: delete
   nothing, and write into `design/prototype/README.md` the single line `PLACEHOLDER: kernel filler, no product
   design yet; replaced at the first x2:lock`, so the lock record is false until it is true. The real import (the
   `.dc.html` plus the full `_ds/`, both themes, more than fifty variables, never values scraped from the
   `.dc.html`), re-pointing `tokenCoverage.test.ts` at the real export path, and re-transcribing
   `src/theme/tokens.ts` until it is green all belong to x2:lock, whose done-checks are that same list; the
   manifest does not duplicate them. Until the first lock, UI-1 in this project's `conformance.json` reads
   `latent` and not `proven`, for the reason its own note gives. Each slice lock adds its
   `design/ledger/slice-NNN.md`.

**C. Verify as a set** (the seed is not done until all pass):

- `dotnet build -warnaserror` and `dotnet test` (all tiers; Docker running)
- `npm ci && npm run verify` in `client-web/`
- `scripts/e2e.sh` (harness with completeness self-audit, then the composed-entrypoint smoke)
- `node tools/docs-lint.mjs` (DOC-1 placement, DEC-1 provenance, DEP-1 image pins and the filled kernel-provenance
  pin, HUM-1 CODEOWNERS coverage, MET-08 dashes)
- `node tools/secret-scan.mjs --self-test`, then `node tools/secret-scan.mjs` (SEC-5, over every tracked text
  surface). Part C used to omit this while step 4 told you to require the `secret-scan` job, so the one list that
  exists to catch an unarmable gate could not see it (E-25).
- The dash check standalone, with the literal-byte pattern (BSD grep false-negatives on a BRE class):
  `grep -rn "$(printf '\342\200\224')" <authored paths>` and `grep -rn "$(printf '\342\200\223')" <authored paths>`
- `node tools/gate-check.mjs --self-test`, then `node tools/gate-check.mjs`: the merge gate read back off the
  forge and required to be armed. It requires every job the CI workflow declares (derived from the workflow, not
  listed), a pull request, and code-owner review, and it fails rather than passes when it cannot reach the forge
  at all. It needs `GH_TOKEN` (or an authenticated `gh`) with repository administration read. This line replaces
  "a test PR touching `Migrations/` must show the required code-owner review", which named no command, needed a
  second identity, and left no artifact behind saying it had ever passed (E-24).

Every project you add lands one D-000 (the first project decision record) plus deltas from this edition. That is
the methodology metric: human turns per shipped slice.

### A named adoption delta: the database engine

SQL Server is this edition's ruled default, not a claim: no file in the claims catalog names an engine, and the
catalog's re-rule mechanism (record the decision at adoption, swap the enforcement) applies. The engine is a
project choice and it is **declared in `edition.json`**, which is the one place it lives.

Swapping to PostgreSQL or MySQL is a supported delta, and this is the whole swap set:

| # | change | what checks it |
|---|--------|----------------|
| 1 | the `engine` block in `edition.json`: name, provider, image, container, volume, port | `EngineChoiceTests` reads it; docs-lint puts the image in the cross-surface agreement check |
| 2 | the EF provider package in `server/Directory.Packages.props`, plus a dated `VERSIONS.md` row | docs-lint's DEP-1 ledger check; `The_declared_provider_package_is_referenced_by_the_build` |
| 3 | `UseSqlServer(...)` in `Kernel.Api/Program.cs` and `KernelDbContextFactory` | `The_host_registers_the_engine_the_edition_declares`, which reads `Database.ProviderName` off the PRODUCTION composition |
| 4 | the Testcontainers module in `SqlServerFixture`, and the image row in `VERSIONS.md` | the agreement check, then the integration tier itself |
| 5 | the `services:` image in `.github/workflows/ci.yml` | the agreement check (a workflow `services:` image cannot be computed, so it stays a literal and is compared instead) |
| 6 | regenerate the migrations: they carry provider-specific model annotations | the integration tier |

`scripts/db-up.sh` is deliberately absent from that list: it reads the declaration, so it needs no edit. That is
the difference DB2 made. The image was named on five surfaces that agreed only by hand (E-64); it is now named on
four that are compared to each other plus one declaration they are all compared against, and the script that used
to be the fifth copy is a reader.

Re-verify the two engine-sensitive seams the tiers already pin: `DateTimeOffset` ordering under the keyset cursor
(`KeysetPagingTests`) and composite-key behavior (`TenantPersistenceTests`). The integration tier going green on
the new engine is the proof the swap holds. SQLite as the production engine is a larger re-rule, not a delta:
TEST-1's tier shape presumes a client-server engine in containers, and the SQLite `DateTimeOffset` converter
exists for the fast tier's semantics, not production's; record that as a D-000 that re-rules TEST-1, or pick a
client-server engine.

### Upgrading a seeded project

A seeded project is a pinned copy, not a live dependency: it keeps exactly the invariants of the kernel named in
its Kernel provenance row, enforced by its own loop, indefinitely; the kernel moving does not weaken it. An
upgrade is a deliberate act with the catalog pass as its unit, never a raw file diff:

1. Read the pass paragraphs in the claims catalog between the pinned catalog date and head, then land on head
   directly (claim files are current state, not diffs; the pass paragraphs are the cumulative changelog).
2. Claims minted since the pin arrive as owed rows with their named triggers; they cost nothing until a trigger
   fires.
3. Claims extended in place arrive as mechanism changes and may demand immediate work (new red tests, sometimes
   a migration). A project may instead adopt an extension as a ledgered deferral: a VERSIONS.md waiver row with
   a named trigger, never a locally edited test. Deferral is an honest state; a silently un-upgraded mechanism
   is not.
4. Re-copy the kernel-owned paths at the new pin: the architecture test project, the platform layer, `tools/`,
   the client lint config, and `scripts/`. Known gap, until registry externalization lands (owed below): several
   scans carry per-project registries inside those files (SEC-2 forbidden fields, SEC-3 PII names, CFG-1 literal
   shapes), so re-copy is a guided merge for the registry-bearing files, re-applying the project's registry
   additions after the copy.
5. Re-apply the project's recorded deltas. Re-rules are decisions: they replay as deliberate edits verified by
   the loop, not as mechanical patches.
6. Run the loop to green. A schema-affecting extension (TEN-4's write-provenance stamps) lands with its
   migration and takes the HUM-1 human turn like any other migration.
7. Update the Kernel provenance row to the new pin in the same change.

Rollback is re-copying at the old pin and reverting the row; a migration that rode the upgrade does not reverse
by re-copy (HUM-1 territory, a human decision).

A project seeded before the pin existed reconstructs it once: date the project's initial commit against the
catalog's pass dates, corroborate by diffing its kernel-owned paths against kernel history, record the
reconstructed pin and its stated uncertainty in a decision, adopt the current docs-lint, and fill the row. From
then on the mechanism holds.

## Decisions and deviations from the brief

The brief hands the claim file authority on conflict ("if this brief and a claim file disagree, the claim file
wins"). Three claim-mandated obligations were absent from the brief and are built here, confirmed with the owner:

- CON-2 contract-parity fixture (the Notes contract is hand-mirrored in C# and TypeScript, so the claim applies in
  v1): one shared `note-contract.fixture.json`, linked into the C# test project and read by the client test.
- AI-1 / AI-2 agent-trust exemplar: the `ToolExecutor` reject-then-inject chokepoint and one read-only tool with
  its no-writes guard test. Nothing in v1 invokes them; they ship as the proven pattern the claims place in v1.
- SEC-5 config-shape test and CI secret-scan (the brief asserted only that user-secrets was wired, not the actual
  no-secret-in-committed-config invariant). The vault-port interface is recorded owed (no runtime credential yet).

Two resolved potholes:

- Dev database and secrets: the brief named a committed LocalDB connection string. X-6 forbids OS-installed engines
  and SEC-5 forbids committed secrets, so dev `Jwt:Key` and the containerized `ConnectionStrings:Kernel` live in
  user-secrets, and `appsettings.Development.json` holds no secret. The dev engine is a container (see the runbook).
- SQLite cannot order or compare `DateTimeOffset`, which the keyset list requires. `KernelDbContext` applies a
  `DateTimeOffset` to UtcTicks value converter (also matching `DateTimeOffset?`) only when the provider is SQLite
  (the arch and unit tiers), so those tiers exercise the real query; production and the integration tier keep native
  `datetimeoffset` on SQL Server.
- Keyset cursor: the cursor is `(CreatedAtUtc, Id)`, not `CreatedAtUtc` alone, and the supporting index is not
  unique. Id is the tiebreaker, so two notes created in the same clock tick can coexist and still page without a
  dropped or duplicated row; the earlier unique `(TenantId, CreatedAtUtc)` index would have crashed the second
  write. An integration test creates five notes at one instant and pages all five.

## Conformance table

**The record is `conformance.json`, not this table.** Per-claim status is edition property, not catalog property
(the catalog says so), so it lives in one machine-readable file per edition, keyed by claim id. The table below is
generated from it by `tools/conformance.mjs`, and docs-lint fails the build on three things: a catalog claim with
no row, a row naming no claim, and a table that has drifted from the record. Edit the JSON and run
`node tools/conformance.mjs --write`; never edit the table.

Status uses the catalog's four states (defined in `kernel/claims/README.md`): `proven` (one scan covers the whole
surface), `patterned` (mechanism plus the v1 seams tested; each new seam owes its test), `latent` (built, never
yet exercised by a real instance), `owed` (recorded, with the named trigger that promotes it). Every gating status
is conditional on TEST-3's loop being armed at instantiation. The record is honest by construction in two further
ways the linter enforces: a claim that is not `owed` must name the mechanism that proves it, and a claim that is
`owed` must name the trigger that promotes it. Scan-coverage specifics behind these summaries are in
`VERIFICATION.md` under "Coverage notes by claim".

<!-- conformance:begin -->
Generated from `conformance.json` by `tools/conformance.mjs`; edit the JSON, not the table.
69 claims at catalog pass 2026-07-26: 7 `proven`, 8 `patterned`, 3 `latent`, 51 `owed`.

32 of those rows state a status per obligation; the row's own status is the weakest of them, so the tally above reads a split row at its weakest half and never at its strongest.

| Claim | Edition mechanism | Status |
|-------|-------------------|--------|
| TEN-1 tenant comes from the credential only | `TenantScopeMiddleware` reads `tenant_id` from the JWT only; `EndpointSpineTests` scans route and query parameters enumerated BY EXCLUSION and DECLARED tenant/PII headers via the framework's own `IFromHeaderMetadata` with vendor-prefix and separator normalization, matching both with `NameComparison`; `HostSecurityTests` proves at run time that a forged tenant header in five spellings does not move a request into another tenant; `ContractShapeTests` bans `TenantId` on requests | proven (the weakest of 6 obligations); **the tenant is resolved solely from the validated credential** `proven`: `TenantScopeMiddleware` reads `tenant_id` from the token and from nothing else, which is the half of this claim the second edition cannot reach at all because it can mint no credential; **the route surface is enumerated completely** `proven`: the pattern loop reads parameter names without consulting a type, so no route parameter is invisible to it; what the predicate then does with the name is the comparison obligation below; **the query surface is enumerated completely** `proven`: the hand-written type list is gone: parameters are enumerated by exclusion from the composed route table, so a value object implementing the framework's parse interface, a `TimeSpan` or a type nobody has thought of is inside the surface rather than outside it, and the enumeration is held to agree with the real binder over E-2's measured corpus rather than restating the binder's rules in an assertion (E-2); **the header surface has a mechanism of its own** `proven`: a header surface cannot be enumerated, so this is the non-scan mechanism ruling 5 requires, and it is covered from both sides: a DECLARED header binding is a scan violation through the framework's own `IFromHeaderMetadata`, with vendor-prefix and separator normalization, and a header arriving UNDECLARED is proven at run time not to move a request into another tenant, in five spellings (A-2). The normalization compacts `X-Tenant-Id` to `tenantid`, which destroys every boundary; the comparison resolves the concatenation, so the base entry covers it without a list of pre-glued spellings (E-9); **the request-contract surface is enumerated completely** `proven`: repaired 2026-07-27, and raising it exposed a gap no pass had named. The contracts guard now walks every member at any depth through the shared `BodyMemberWalk`, which closes the enumeration half. The predicate half was missing outright: the tenant registry was applied to URL parameters and to nothing else, so a request contract carrying `WorkspaceId` and `OrganisationId` passed all 100 tests while TEN-1's statement says no request contract carries a tenant identifier field. Only `tenantId` was caught, and only because SEC-2's separate registry happens to list it. Both surfaces now apply the tenant registry to body members, proven red on an unrouted contract and on the routed `CreateNoteRequest`, green when reverted. Recorded as E-22; **the comparison resolves compounds, concatenations and decompositions of a listed word** `proven`: matching is `NameComparison`: an entry `tenant` covers `tenantId`, `tenantid`, `tenant_id` and `tenantSlug`, an entry `org` covers `orgId` and `orgid` through a declared affix, and the flow-back pass's widened list of compounds is gone because they are derived, each asserted to still match. The prefix rule that would let `org` reach `organisationid` directly is rejected and asserted against, because it also matches `origin`, an ordinary request header (E-9); TEN-1 names four surfaces and this edition reaches all four, unevenly. ROUTE is total, because the pattern loop reads parameter names without consulting a type. HEADER is covered from both sides (A-2): a declared binding is a scan violation, and a header arriving undeclared is proven not to influence tenant resolution. QUERY was type-dependent and is not any more (E-2, repaired 2026-07-27): nothing asks what binds from a URL, so a strong-typed tenant id is inside the surface by default, proven by planting `TenantKeyProbe : IParsable` as a query parameter named `tenantKey` on a real route (red now, green under the old mechanism). Matching was equality and is now a comparison (E-9, repaired), which matters most on the header surface, where normalization removes every boundary a token matcher would need. Findings: A-2 (repaired), E-2 (repaired), E-9 (repaired), E-6 (contracts half open). Request-contract surface raised 2026-07-27, together with E-22's repair of the tenant predicate on body members. |
| TEN-2 ambient scope, fail-closed | `AmbientTenantScope` (AsyncLocal, throws on unset); `AmbientTenantScopeTests` | patterned (the weakest of 3 obligations); **scope established at every entry** `patterned`: `TenantScopeMiddleware` stamps the scope at HTTP ingress and the middleware test proves it; each non-HTTP entry owes its own `Begin`, and the job base class is owed (trigger: first background job); **unset scope fails before I/O** `proven`: `AmbientTenantScope` throws when read unset, so an unstamped path is an error rather than an unfiltered query; `AmbientTenantScopeTests` asserts the throw rather than the accessor's presence; **cross-tenant probes assert uniform not-found** `patterned`: `One_tenant_cannot_read_another_tenants_note`, `One_tenant_cannot_delete_another_tenants_note` and `Another_tenants_note_is_absent_from_the_list` cover the read, mutating and list verbs, each asserting the same absent answer. Before E-50 only the GET existed, and removing the tenant filter from `EfNoteStore.DeleteAsync` left every runnable test green while the path answered 500 rather than 404, which is an existence oracle. The delete probe now turns red on exactly that plant. trigger: each new mutating endpoint owes its own cross-tenant probe; the non-HTTP job base class is owed (trigger: first background job) |
| TEN-3 tenant-leading composite keys | `NoteConfiguration.HasKey(TenantId, Id)`; `TenantKeyTests` over the EF model, reading `TenantColumn.IsTenantShaped`, which is TEN-1's `ForbiddenTenantParams` registry through `NameComparison`; `KeyShapeExemption` for sanctioned exceptions | proven (the weakest of 3 obligations); **tenant-owned entities lead their key with the tenant id** `proven`: `Every_tenant_owned_entity_leads_its_key_with_TenantId` reads the EF model and binds: flipping `HasKey` to lead with `Id` turns it red and names TEN-3. It rides on the `ITenantOwned` marker, and `The_sets_these_assertions_iterate_are_not_empty` now fails if that marked set is empty, so the assertion cannot pass over nothing (E-43 repaired); **no unmarked tenant data** `proven`: `Every_entity_with_a_tenant_column_is_marked_tenant_owned` binds: stripping `ITenantOwned` from `Note` turns it red. It reads TEN-1's `ForbiddenTenantParams` through `NameComparison`, not a list of its own; the four-name equality list it carried until E-51 let six of thirteen tenant-shaped spellings past, `tenant_id` and `TenantKey` among them. The extent is asserted against an independently written floor of thirteen spellings plus six ordinary names that must not match. This obligation now rests on exactly the registry and comparison TEN-1 rests on, so the two rows stand or fall together; **a sanctioned exception carries a named justification and its own guard** `proven`: `KeyShapeExemption` is a register separate from the TEN-5 access ledger, as the claim requires, following `AnonymousCarveOut`. Four rules, each proven by fixture because the shipped register is empty and proving them from an empty register would prove nothing: a stale entry fails, an unjustified entry fails, an unnecessary entry fails, a duplicate fails. End-to-end control: a violating key plus a justified exemption is green, and the same exemption with its justification reduced to a label is red (E-44 repaired). |
| TEN-4 save-time guard and write provenance | `KernelDbContext.GuardTenancy` on both SaveChanges variants; `TenantGuardTests`; integration cross-tenant modify; `BulkWriteBanTests` bans the write paths that bypass the save pipeline (EF set-based, raw SQL, raw ADO) | owed (the weakest of 3 obligations); **foreign-tenant writes are refused at save** `proven`: `KernelDbContext.GuardTenancy` runs on both SaveChanges variants; `TenantGuardTests` covers stamp-on-create, throw-on-foreign-row and throw-on-unset-scope, with an integration cross-tenant modify behind it; **no write path bypasses the save pipeline** `proven`: `BulkWriteBanTests` bans the EF set-based, raw SQL and raw ADO operators, which is what makes the guard above total rather than typical; **the pipeline stamps write provenance on create and modify** `owed`: Four of the seven unit tests TEN-4's mechanism class names are provenance, and none exists or can: `Note` carries no actor field and `GuardTenancy` stamps `TenantId` only. `CreatedAtUtc` is stamped by `NoteService`, not by the pipeline, which is the opposite of the claim's "assigned by the pipeline unconditionally, values already present are overwritten". This was recorded in the row's free-text note and not as an obligation, so the roll-up read `proven` (E-52). trigger: the actor field and a pipeline stamp, or the next edition build pass; the tenancy guard is proven; the write-provenance half has no mechanism (E-52) |
| TEN-5 sanctioned-bypass ledger | `docs/claims/tenant-bypass-ledger.md` (empty) + docs-lint rule that a row without a sole-reader test fails | owed (the weakest of 5 obligations); **a committed ledger exists and is where the claim lives** `proven`: removing docs/claims/tenant-bypass-ledger.md fails docs-lint with a sentence naming TEN-5, not a stack trace; **a ledger row that names no sole-reader test fails the build** `proven`: red-correct for an empty third cell. The parse is keyed on the COLUMN NAME now, not on position, which is E-35 repaired in both of its halves: inserting an Owner column used to move the cell out of reach and pass every row unread, and a ledger reformatted as a bullet list used to yield no rows at all and pass for being empty. A parse that silently finds nothing is the worst failure available to a guard whose subject is empty by design, so a ledger this parser cannot read as a table is now a failure, and so is a table with no sole-reader column; **the named sole-reader test exists, and is the remedy** `patterned`: The name is resolved against the tree: a row naming an identifier that appears in no source file fails, red-correct for a planted row naming `NightlyBillingSweepIsSoleReader`. Scored in BOTH directions, which is what distinguishes a resolver from a guard that always refuses: adding a declaration of that identifier turns the same row green. Deliberately a whole-word search over source rather than a test-framework lookup, because knowing what counts as a test in xunit and in vitest is the stack-specific knowledge a shared tool must not hold (E-34 repaired). `patterned`, and the residue is the obligation's second clause: it proves the name resolves to SOMETHING, not that the something is a test, and nothing at all checks that the test is the sole reader. Trigger for the rest: the first sanctioned bypass, which would give the clause a subject; **every sanctioned cross-tenant path appears in the ledger** `owed`: unbuilt, and the reason is worth recording rather than restating the trigger. The scan this obligation's trigger asks for presumes a bypass has a lexical signature, which presumes tenancy is enforced by something that can be bypassed. Measured 2026-07-27: this edition has NO global query filter. Tenancy is an explicit `.Where(n => n.TenantId == tenantId)` per query, so a cross-tenant read is the ABSENCE of a predicate, which no regex can see and which a scan for `IgnoreQueryFilters` would report as clean forever. Building that scan here would be a guard checking for a bypass shape this design cannot produce (E-34, E-77). Trigger: a global query filter or an equivalent chokepoint that a bypass has to name, plus the first sanctioned bypass; **one ledger, not several** `patterned`: A second document whose name carries both `bypass` and `ledger` fails, red-correct for a planted `docs/claims/billing-bypass-ledger.md` (E-34 repaired). `patterned` because the rule is on the NAME: a second ledger called `cross-tenant-register.md` escapes it. Each new spelling owes its own entry, which is the ordinary cost of a name-shaped rule and is why the canonical path is named in the message; planted 2026-07-27, six plants, two red naming TEN-5; three more obligations planted 2026-07-28 and two of the three moved off `owed`. The parse is keyed on column name rather than position and refuses a ledger it cannot read as a table (E-35 repaired), the named sole-reader test is resolved against the tree and scored in both directions (E-34 repaired), and a second bypass ledger fails (E-34 repaired). The row stays `owed` on one obligation and the reason is a measurement, not a backlog: this edition enforces tenancy per query rather than by a global filter, so a sanctioned bypass has no lexical signature to scan for (E-77). |
| TEN-6 tenant is minted from membership, never asserted | not built | owed; trigger: first identity slice (shared with SEC-4's version store and SEC-10) |
| SEC-1 every endpoint gated | `EndpointSpineTests` over the composed host's route table: every endpoint names a non-empty perm policy or matches a justified, method-specific anonymous carve-out; the fallback policy is resolved the way the middleware resolves it and EVALUATED; and the enumeration itself is asserted to be every route the host maps | proven (the weakest of 4 obligations); **every endpoint names a permission or is a reviewed anonymous carve-out** `proven`: The scan walks the composed host's own route table and requires a `perm:`-prefixed, non-empty policy. Bare authentication is refused at the endpoint, which is what the claim asks for and what the realization had accepted as its floor until E-7; **the anonymous allowlist is reviewed rather than accumulated** `proven`: `AnonymousCarveOut` carries method, pattern and a justification with a 40-character floor, so an entry is scoped to one verb on one URL rather than pre-authorizing every method on it (E-8). A carve-out matching no endpoint fails as stale, which is what stops the register becoming a hole waiting for a URL. An endpoint with no method metadata answers every method and is reported as `*`, so it can never match a specific entry; **the fallback denies by default, evaluated rather than inspected** `proven`: Resolved from `IAuthorizationPolicyProvider`, which is what `AuthorizationMiddleware` consults, and then run against two principals: an anonymous one and an authenticated one carrying tenant, session version and both perms. The earlier version read `IOptions<AuthorizationOptions>` and asserted a requirement was present, which is a different object and could not see that returning null from the provider opened every metadata-free endpoint (E-7 round 4). A `DenyAnonymousAuthorizationRequirement` is satisfied by any authenticated caller, so the authenticated case is the one the old assertion could never have expressed; **the enumeration reaches every route the host maps** `proven`: Added 2026-07-27. Every assertion above is a loop over one shared `RouteEndpoints()`, and a loop over a narrowed set passes while saying nothing. Measured: narrowing it to routes whose pattern contains `health` left all three assertions above green, and the only test that noticed was SEC-2's body scan, whose message names neither the narrowing nor SEC-1 (E-72, and E-22's third instance here). The assertion compares the scanned count against the framework's own unfiltered table and then checks a named floor of this edition's three routes, because non-emptiness alone would not have caught a plant that left one route standing. TEN-1's route, query and header scans read the same method, so they inherit it; four obligations, all proven, and the fourth was added because a narrowed enumeration left the other three green |
| SEC-2 anti mass-assignment | `EndpointSpineTests` takes each endpoint's body type from the framework's own `IAcceptsMetadata` and walks it RECURSIVELY through public properties, constructor parameters, arrays and generic arguments, with cycle protection and no depth cap, matched by `NameComparison` against the shared registry, with a parameter walk as a second net and a non-vacuity assertion; `ContractShapeTests` is a third net over the Contracts assembly, FLAT and filtered to types whose name ends in `Request`, matching by equality; App/Api declare no request/response types | proven (the weakest of 3 obligations); **the predicate rejects a registry member at any depth of an enumerated type** `proven`: `EndpointSpineTests` walks every body-bound DTO recursively through public properties, constructor parameters, arrays and generic arguments, with cycle protection and no depth cap, proven both ways against a nested `AuthorDto` carrying `CreatedBy` (E-6), and the walk's depth is asserted directly because the composed host binds only flat records today; **the enumeration reaches every body-bound type** `proven`: both nets are now complete and they share one walk. The host half takes the body type from the framework's own `IAcceptsMetadata.RequestType` rather than from a predicate of this test's own, so E-6's null-namespace exclusion and E-2's hand-written type list are both gone. The second net, `ContractShapeTests`, was the last flat and equality-matching guard in the tree and is repaired: it walks the SAME `BodyMemberWalk.MemberNames` at any depth and compares through the SAME `NameComparison`, so the two guards cannot drift apart about what a caller can populate. Proven with a control: with the pre-repair guard restored and an unrouted `OrphanRequest` carrying `AuthorInfo.CreatedBy` planted, all 100 tests passed; with the repair, the nested field and a `CreatedByUser` compound at depth zero each fail separately. The residual, stated: a contract type in the Contracts assembly that is neither routed nor named `*Request` is enumerated by neither guard, and nothing can post to it, so it enters the surface only by gaining a route, at which point the host scan sees it. It was repaired here after three passes recorded it as blocked on MOD-2's delta; that premise was measured and is false (S-12); **the comparison resolves compounds, concatenations and decompositions of a listed word** `proven`: matching is `NameComparison`, not a hash-set lookup: `status` covers `noteStatus` and `notestatus`, `createdAt` covers `createdAtUtc`, `role` covers `roles`, and the registry lost the spellings it used to enumerate because they are derived, each one asserted to still match. The flat second net still compares by equality and reads the same registry, and a test holds every name in the flat list to be matched by the rules, so the weaker net can never become a different one (E-9); The guards are not equivalent and the difference is the point. The HOST scan recurses (E-6, 2026-07-26) and as of 2026-07-27 it no longer decides for itself what a body is: the enumeration comes from the framework's `IAcceptsMetadata`, so the two exclusions that used to hide a type from it are gone. Both were proven by planting the violation on a real route: a public DTO declared in the entry-point file, which has no namespace and was skipped outright, is now caught, and the old mechanism with the same DTO planted is green. The comparison is no longer equality (E-9). The residual it leaves was measured rather than argued: a `*Request` contract carrying a forbidden field one level down, bound to no route, is invisible to both guards; the moment it is bound to a route the host scan catches it at depth. So the uncovered set is exactly the contract types nothing can post to. Findings: E-2 (repaired), E-6 (host half repaired, contracts half blocked on MOD-2), E-9 (repaired), E-20. Enumeration raised 2026-07-27: the contracts guard now shares the endpoint scan's walk and comparison, and the tenant registry reaches contract members for the first time (E-22). |
| SEC-3 no PII in URLs | `EndpointSpineTests` route + query scan over the composed route table, enumerated BY EXCLUSION (the framework's own declared body type, registered services and the pipeline's own types are set aside and every other parameter is a value a caller can spell), with [AsParameters] wrapper properties walked; matched by `NameComparison`, which resolves compounds, concatenations, decompositions and plurals | owed (the weakest of 4 obligations); **the predicate rejects a registry match on a parameter it can see** `proven`: `EndpointSpineTests` scans route and query parameters, covering `[AsParameters]` wrappers, collections and date parameter types that a naive scan misses; **the enumeration reaches every declared route and query parameter** `proven`: the hand-written type list is gone. Nothing asks what binds from a URL any more: the body type comes from the framework's own `IAcceptsMetadata` on the composed endpoint, services come from `IServiceProviderIsService`, the pipeline's own types are a closed list of things no caller can spell a value for, and every other parameter is compared. A type nobody has thought of is inside the surface by default instead of outside it. `Both_enumerations_agree_with_the_real_binder_over_a_corpus_of_parameter_types` holds that against the binder that actually runs, over E-2's measured corpus (`TenantKey : IParsable`, `TimeSpan`, `Uri`, `IPAddress`, `Int128`, a legacy `TryParse` type), so the two can no longer drift apart silently (E-2); **the comparison resolves compounds, concatenations, decompositions and plurals of a listed word** `proven`: `NameComparison` tokenizes on camel case, separators and letter/digit boundaries, re-glues every contiguous run of tokens, matches an entry that begins or ends a glued word, folds plurals, and derives an entry's tail past a one-letter particle, so `email` covers `emailAddress`, `user_email`, `emailaddress`, `e_mail`, `e-mail`, `EMailAddress`, `mail`, `emails` and `email1`. It beats both matchers this repo has built: the six inputs the round 4 audit measured escaping BOTH equality and token matching are asserted, and the registry got SHORTER because `firstname`, `lastname` and `dateofbirth` are derived rather than listed, with every dropped spelling asserted to still match. Its false positives (`fileName`, `voicemail`) and its residuals (a synonym, a reordering, an unnamed parameter) are passing tests, so a later narrowing goes red (E-9); **a parameter that binds or is read without being declared is inside the surface** `owed`: SEC-3's remedy sentence requires an undeclared query parameter to be "refused or removed before the handler by a ruled decision, never bound silently", and this edition has no such mechanism: measured, a handler taking HttpContext and reading `Request.Query["emailAddress"]` leaves the whole suite green. TEN-1 met the same shape on the header surface with a runtime mechanism rather than a scan, which is the available answer here, and refuse-versus-remove is exactly the choice the claim says must be RULED per surface (trigger: an adjudication ruling on the refuse-or-remove decision, in the class of E-8's HUM-1 half; the row is `owed` on this and on nothing else); Lowered from `proven` to `owed` by the adjudication pass of 2026-07-26 (ruling 6), and **both named gaps were closed on 2026-07-27**: the enumeration no longer asks which types bind from a URL (E-2) and the comparison is no longer equality (E-9). Each was proven by planting the violation on a real route in the composed host, confirming red, then restoring the old mechanism with the violation still planted and confirming green, which reproduces the hole rather than taking the record's word for it. The row does not return to `proven`, and the reason is a duty the row never carried: SEC-3's completeness obligation ends "a query parameter the surface does not declare is refused or removed before the handler by a ruled decision, never bound silently", and nothing here does that. Measured, not inferred: a handler that takes HttpContext and reads `Request.Query["emailAddress"]` leaves the scan green. That obligation is written into the row below rather than left in prose, so the row reads `owed` for something real (E-20). |
| SEC-4 JWT hardening and revocation | pinned HS256, `RequireSignedTokens`, `SessionVersionMiddleware`; `HostSecurityTests` asserts both token-validation properties off every registered bearer scheme and covers a tampered signature, an HS384 token, an unsigned alg:none token, a missing `sv` and a bumped `sv` | proven (the weakest of 3 obligations); **the signing algorithm is pinned to exactly one** `proven`: `Token_validation_pins_exactly_one_algorithm` reads `ValidAlgorithms` off every scheme whose handler is `JwtBearerHandler` and asserts it equals `[HS256]`. The behavioural HS384 test proved HS384 was excluded and nothing about what else the list admitted: adding `RsaSha256` left all twelve host tests green (E-54). Controls on 2026-07-27: widening the list goes red, registering a second looser bearer scheme goes red, and narrowing the scan to no scheme goes red on its own non-empty assertion; **tokens must be signed** `proven`: `Token_validation_requires_a_signature` reads the flag off every bearer scheme, and `An_unsigned_token_is_rejected` sends a hand-assembled alg:none token. `RequireSignedTokens = false` had left all twelve host tests green (E-55); it now turns both red. The control also corrected a prediction: with the flag off and the algorithm still pinned to HS256, an unsigned token AUTHENTICATES, because it never reaches signature validation, so the pin does not shadow this flag; **revocation is immediate through a server-side session version** `proven`: `SessionVersionMiddleware` plus the bumped-`sv` and missing-`sv` tests; removing the middleware from the pipeline turns both red. The v1 store is process-local, so an EF-backed store is this mechanism's named upgrade rather than a second obligation. No TTL cache exists, so the claim's cache-window obligation has no surface here; both halves of the token-validation sentence now read off the host, over every bearer scheme, not one by name |
| SEC-5 no secrets in config | `UserSecretsId` + `SecretConfigShapeTests` over every committed JSON config under `src/`, matching secret-shaped keys by TOKEN (camel/Pascal/snake/kebab split) rather than by containment, with a `[Theory]` asserting the predicate's extent + `tools/secret-scan.mjs`, a composed shared-tier scan over every tracked text surface with a `--self-test` mode CI runs before the scan | proven (the weakest of 2 obligations); **no secret-shaped value in committed configuration** `proven`: `SecretConfigShapeTests` over every committed JSON config under `src/`, matching secret-shaped keys by token split rather than by containment, with a `[Theory]` asserting the predicate's extent; **no secret-shaped literal on any tracked text surface** `proven`: `tools/secret-scan.mjs`, whose `--self-test` asserts eleven positive and thirteen negative controls before the scan runs; four justified exceptions are declared as (file, key) pairs with reasons and a staleness check; Both mechanisms were blind to the edition's own `Jwt:Key` until 2026-07-26 and both are repaired (E-11). The arch test matched `leafKey.Contains(term)`, which could never fire on a leaf shorter than the term describing it; the CI half was a grep in YAML that was additionally blind to every QUOTED key, meaning every key in every JSON file in the edition (E-16). Four justified exceptions are declared in `secret-scan.allow.json`, each a (file, key) pair with a written reason and a staleness check; one of them is a committed test signing key the repaired scan found on its first run (E-17). Token matching costs a false positive on `TokenCount`, asserted as a passing case rather than hidden (E-9). Still open: `.env` lives inside the repository tree while the claim's statement says development secrets live outside it (E-12), which the scan's tracked-file reading does NOT address. Vault-port interface owed (trigger: first runtime credential). |
| SEC-6 log safety | `client-web/tools/harness/redact.ts` scrubs Authorization headers and JWT-shaped strings (`redact.test.ts`); `LogSafetyTests` reads the server's real sink through a capturing `ILoggerProvider` while a request carrying a bearer token and a body with content in it goes through the real `UseExceptionHandler` | patterned (the weakest of 2 obligations); **secret-shaped values are scrubbed by one redactor** `patterned`: Two surfaces, two mechanisms, because they are different sinks. Harness output: `redact.ts` plus `redact.test.ts`, on the client. Server: `LogSafetyTests` asserts the bearer token, a JWT-shaped string and the request body's content are all absent from what the sink actually received. It found the redactor's JWT pattern over-matching: the unanchored three-dot form matches `Microsoft.EntityFrameworkCore.Database.Command` and every other dotted identifier (E-58). trigger: each new logging surface owes its own, which is what keeps this `patterned`; **each logging surface asserts its own redaction** `patterned`: The claim names three surfaces: hub/realtime logging, harness output, error handlers. Harness output is covered on the client; the error handler is now covered on the server, where `ILogger`, `Serilog` and every redaction helper had returned zero occurrences across `server/src` while `app.UseExceptionHandler()` was wired and nothing asserted what those entries contained (E-56). This edition has no hub, so the third surface does not exist to test. Controls on 2026-07-27: logging request headers in the pipeline turns the token and JWT assertions red, `EnableSensitiveDataLogging()` on the DbContext turns the content assertion red, and unwiring the capture turns the arrangement's own non-empty guard red while the other three pass on an empty log. trigger: the first slice that adds a hub or any other logging surface owes its named test; both surfaces this edition has are covered; there is no hub, so realtime logging has no surface here yet |
| SEC-7 opaque public identifiers | not built | owed; trigger: the first anonymous resource surface in an edition project (the kernel exemplar has none) |
| SEC-8 abuse posture | not built | owed; trigger: the first publicly exposed edition host (the launch pre-flight names it) |
| SEC-9 deployment-edge hardening | not built | owed; trigger: the first deployed edition host (arrives with the launch pre-flight) |
| SEC-10 authentication strength asserted at mint, verified at the gate | not built | owed; trigger: first identity slice (shared with SEC-4's version store and TEN-6) |
| SEC-11 the runtime credential cannot touch the schema | not built | owed; trigger: the next edition build pass (rides DATA-6: the migrate mode and the role split are one provisioning story; the integration tier's provisioning path is where both roles are created) |
| TIME-1 UTC, offset-aware, and nothing else | `TimeTypeTests` over every layer project under server/src, DERIVED from the filesystem rather than listed (recurses Nullable, arrays, generics; the global namespace is app code, not framework); a layer that exists and whose assembly the scan cannot reach fails a test of its own | owed (the weakest of 3 obligations); **the predicate rejects a naive instant in the layers it reaches** `proven`: `TimeTypeTests` recurses Nullable, arrays and generics over the layers it is given, and rejects naive `DateTime` in all of them. As of 2026-07-27 it also walks types with no namespace, which the hierarchy walk previously stopped at as though the global namespace were the framework; **the enumeration reaches every layer that declares a time value** `proven`: the layer set is derived from the projects under server/src, so a layer added to the server is scanned with no edit to the test, and a layer that exists and whose assembly the scan cannot reach fails `Every_declared_layer_is_reachable_by_the_scan` rather than being skipped, which is the remedy half TIME-1 asks for ("a layer that exists and is not in the enumeration fails the build too"). The derivation is cross-checked against the composed host's own assembly graph so an empty walk cannot read green. E-15's control is red both ways now, and so is the one that composes with E-6 on the entry-point file; **durations come from the monotonic source through one clock seam** `owed`: the wall-clock-delta lint the 2026-07-24 extension adds has no realization here, and nothing confines a now-reading to a seam (trigger: the next edition build pass); DB UTC column defaults not used: the service sets the value. **Lowered from `proven` to `owed` by the adjudication pass of 2026-07-26 (rulings 2 and 3).** E-15 measured the omitted layer with a control and the flow-back pass deliberately did not repair it, on the grounds that lowering the row belonged to the owner. **The layer half was repaired on 2026-07-27**, and not by adding the missing assembly to the list: the list is gone and the layer set is derived from the projects under server/src, which is what TIME-1 requires ("a rule the test can check rather than a hand-written list"). Proven three ways, each red before the repair and after the violation was planted: a naive `DateTime` on a public type in the API layer, the same type declared in the entry-point file where it has no namespace at all (the file E-6 and E-15 both exempt), and a layer added under src/ that the tests cannot reach, which fails without carrying any time type. The monotonic-durations half has never had a realization here and is why the row still reads `owed`, which is the S-11 shape: two obligations that are built, gating and red-green proven, rolled up with one deferred extension, and the vocabulary's weakest word means "not built". |
| DATA-1 stores, records, downward deps | `DependencyDirectionTests`: seven assertions over the App, Contracts, Persistence and Api assemblies, with the service and store registries discovered by reflection and every selection asserted non-empty | proven (the weakest of 3 obligations); **no endpoint or host type references a store implementation or DbContext** `proven`: `No_host_type_outside_the_composition_root_depends_on_Persistence` covers all of `Kernel.Api.*`, not just the endpoints namespace, and `Program` is excluded by construction because top-level statements put the composition root in the global namespace. Planted in both namespaces: red in each, naming DATA-1 (E-40 repaired); **no persistence type references application services** `proven`: `Persistence_does_not_depend_on_an_application_service` bans a registry of `Kernel.App` public classes named `*Service`, discovered by reflection and asserted non-empty. A persistence type taking `NoteService` turns it red (E-41 repaired); **store implementations are reachable only via their interfaces** `proven`: `Store_implementations_are_named_only_by_the_composition_root` bans a registry of persistence classes realizing a `Kernel.App` `*Store` interface, discovered by reflection and asserted non-empty. A host type taking `EfNoteStore` rather than `INoteStore` turns it red (E-41 repaired). The App half of that assertion is unfalsifiable by project-reference direction and is carried as defense in depth, not as the proof. |
| DATA-2 bounded reads | `EfNoteStore` AsNoTracking + `(CreatedAtUtc, Id)` keyset + a store-side clamp; `EfNoteStoreTests` asserts all three against the real store over SQLite; `NoteServiceTests` the service clamp; integration `KeysetPagingTests` the same-timestamp collision page, which needs Docker | patterned (the weakest of 3 obligations); **every list endpoint has a maximum page size** `patterned`: Bounded at the service (`NoteServiceTests.List_clamps_the_page_size`, over a fake store) and again at the store (`A_direct_caller_cannot_read_past_the_maximum_page_size`, over the real one). Removing the store clamp turns five assertions red, as does removing `.Take(limit)` outright; both were green everywhere before E-48 was repaired. trigger: each new list endpoint owes its own bound test; **reads run without change tracking** `patterned`: Asserted on both read paths against the real store. Removing `.AsNoTracking()` from `GetAsync` turns `A_single_read_leaves_nothing_tracked` red; before the repair it was green everywhere, and removing it from `ListAsync` turned only AI-2's tool test red with a message naming a tool, which is E-22's shape rather than coverage (E-48 repaired). trigger: each new store read method owes its own assertion; **list reads are paginated by keyset, not offset** `patterned`: `Paging_by_cursor_visits_every_row_exactly_once` pages the whole table and asserts no row is dropped or repeated, which distinguishes keyset from offset without a concurrent writer, and runs on SQLite with no Docker daemon. Removing the cursor filter turns it red. The same-timestamp collision page stays with `KeysetPagingTests`, which needs a real engine and stays in the edition template's CI rather than this repository's, by the deliberate scope decision `kernel.yml` now states (E-49, E-39 repaired). trigger: a provider whose id ordering differs from the one under test owes the collision page on that engine; per-seam: each new list endpoint and each new store read owes its own assertion |
| DATA-3 two-layer at-most-once | claim + proven exemplar | owed; trigger: first idempotent external side effect |
| DATA-4 cross-store reconcile | claim + proven exemplar | owed; trigger: first cross-store sequence |
| DATA-5 fail-fast mandatory config | `Required()` startup reads feeding the JWT options and the DbContext; `StartupConfigTests` blanks each of the four keys in turn | owed (the weakest of 4 obligations); **validation runs at startup, before the process accepts work, structurally** `proven`: `Required()` runs at configure time and its results are the JWT signing key, issuer, audience and the connection string, so the serving surface cannot be constructed without them; this is the structural ordering the claim asks for rather than a conventional early call. Each of the four keys is planted individually and each turns the host's startup red naming itself; **closure over every key the application declares mandatory** `owed`: The theory's key list is four `[InlineData]` entries. Adding a fifth `Required()` call and satisfying it in both factories leaves all 157 tests green, so a new mandatory key arrives with nothing proving it is load-bearing (E-61). trigger: drive the theory from a source scan of the `Required()` call sites, not a hand-written list; **closure over every key a committed configuration document declares that the application does not** `owed`: `Webhook:Secretz` added to `appsettings.json` is ignored, green (E-61). The claim calls this the direction that is easy to miss and says an undeclared key is refused rather than ignored, because a typo, a stale key and an abandoned key look identical to a validator that walks only its own list. trigger: walk the committed documents and refuse a key the application never reads; **the remedy names every failing key, not the first** `owed`: Three keys blanked at once, and startup names one: `Jwt:Issuer` (E-61, measured; E-14 recorded it by reading). `Required()` throws on first miss, so a three-key misconfiguration is three failed deploys, which is the claim's own sentence. trigger: collect the misses and throw once naming all of them; the `when` obligation holds; the two `closure` obligations and the `remedy` obligation do not (E-61) |
| DATA-6 migrate and exit | not built | owed; trigger: the next edition build pass (a small host change; the mechanism is two facts on the composed host) |
| DATA-7 ruled retention and disposal | not built | owed; trigger: the first PII-bearing entity in an edition project, or the first contractual deletion clause, whichever lands first |
| DATA-8 a state change and its event commit together or not at all | not built | owed; trigger: the first event publish in an edition project (arrives with the first broker, alongside RES-5) |
| DATA-9 a stale write is refused, never silently applied | not built | owed; trigger: the next edition build pass (rides TEN-4's interceptor work; the token and the refusal live in the same save pipeline the tenancy guard already owns) |
| DATA-10 everything that grows has a registered sweep | not built | owed; trigger: the first append-only operational table in an edition project (DATA-8's outbox is the likely first, with RES-5's dead-letter quarantine close behind) |
| DATA-11 a migration never breaks the release running beside it | not built | owed; trigger: the first production deployment with rolling replacement; the lint half can land at the next edition build pass, ahead of the compatibility job |
| CFG-1 operational settings are configuration, not code | `OperationalSettingsTests` scans `server/src` and `scripts/` for model-id and provider-endpoint literals, and compares every leaf value in the committed appsettings against every literal in the shipped scripts and CI workflows | patterned (the weakest of 2 obligations); **a scan over host source and shipped scripts fails the build on an operational-setting literal** `patterned`: red-correct three ways: a model id in scripts/dev-setup.sh, a model id in Kernel.Api/Program.cs, and https://api.anthropic.com in Kernel.App/Agents/ToolExecutor.cs. Two literal shapes are covered; each new shape owes its own entry. The registry's extent is now asserted as a Theory (13 cases, 8 positive and 5 negative), so narrowing a pattern fails its own test: measured red on the neutered registry with the assertion in place, green on the shipped one (E-29); **shipped scripts read committed values rather than duplicating them** `patterned`: `No_shipped_script_duplicates_a_committed_configuration_value` reads every leaf value out of the committed appsettings and looks for THOSE in the shipped scripts and CI workflows, which is the opposite direction from the literal registry above and needs no entry when a project adds a setting. Red-correct for the E-36 recurrence, literal issuer and audience in scripts/e2e.sh, in both the quoted and bare assignment forms. It found four LIVE duplications in the CI workflow, all repaired (E-75). CFG-1's sanctioned fourth home is honoured and bounded: an environment assignment under a declared key is an override, but only where its value DIFFERS from the committed one, because an override set to the value already committed is a copy that drifts. `patterned` for the surface, not the predicate: the scan covers `scripts/*.sh` and `.github/workflows/*.yml`, which is the maintained list the claim's completeness obligation says is not enough. Widening was measured, not assumed, and reports two false positives (E-75), so it needs a carve-out register rather than a wider glob. Each shipped file kind outside those two globs owes its own entry; planted 2026-07-27, six plants, three red naming OperationalSettingsTests. Both regexes could once be narrowed to match nothing with all 100 tests green; the registry's extent is now a Theory (E-29 repaired). The duplication half, which the row's own mechanism text used to describe as though it were a mechanism, was built on 2026-07-27 and immediately found four live duplications of the committed JWT identity in the CI workflow, all repaired (E-13, E-36 repaired; E-75). Both obligations are `patterned` for different reasons: the literal registry is heuristic in its PREDICATE, so a novel operational-setting shape escapes it, and the duplication scan is total in its predicate and heuristic in its SURFACE, which is two globs rather than the rule the claim's completeness obligation asks for. Two live defects remain recorded with triggers rather than repaired: the shared harness token minter carries `?? 'kernel'` as a silent default, and it is a composed shared file so the fix is a cross-edition decision. |
| CON-1 one wire dialect | one host configuration point (`AddProblemDetails`, `UseStatusCodePages`, a single camelCase `JsonStringEnumConverter`); `WireConventionTests` asserts naming, an enum round-trip in both directions, problem+json on two routes, every mapped endpoint's declared result union against an allowlist of dialect-conforming result types, and every identifier-shaped member in `Kernel.Contracts` against the types that cross the wire as strings | patterned (the weakest of 4 obligations); **one host-level serialization and error configuration point** `proven`: Both halves bind: removing the enum converter turns `Json_naming_is_camelCase_with_an_enum_converter` and `Enums_round_trip_as_camelCase_strings` red, and removing `AddProblemDetails()` turns `Unknown_route_returns_problem_json` red; **no endpoint invents its own error shape** `patterned`: `No_endpoint_declares_a_result_type_that_invents_an_error_shape` walks every route in the host's `EndpointDataSource` and reads each handler's declared result union against an allowlist of dialect-conforming result types. An allowlist, not a ban list: any `Xxx<TBody>` renders TBody raw at whatever status it carries, so a ban list reads green for the result types it has not heard of. Red-correct for `GetAsync` returning `NotFound<string>` in place of `NotFound`, naming the route; 158 green before, and no other test noticed (E-46 repaired). The extent of the predicate is a 14-case Theory written from CON-1's statement, not derived from the shipped handlers. `patterned` and not `proven` because the predicate reads only `IResult`-rooted return types, so an MVC controller action returning a bespoke body is enumerated and never questioned (E-73). Each new endpoint style owes its own entry; **enums cross the wire as closed string sets** `proven`: The host converter is constructed with `allowIntegerValues: false`, so the wire set is closed on the read side, and `WireConventionTests` plants both shapes through the host's own options: an undeclared string and the documents `7`, `0` and `-1`. Before E-45 the string case was rejected and every integer was accepted into an undeclared value, which is why the gap read as fine. Control: restoring the old registration with these tests in place turns all three integer cases red (E-45 repaired); **identifiers are opaque strings** `patterned`: `Contract_identifiers_cross_the_wire_as_opaque_strings` walks every type in `Kernel.Contracts` through the shared `BodyMemberWalk`, at any depth, and asserts every identifier-shaped member is declared as a type that crosses the wire as a string. Red-correct for a planted `NoteRevisionResponse(int RevisionId, string Summary)`, naming the contract, the member and the type; 158 green before. `ContractParityTests` cannot cover this by construction: it compares property NAMES against a fixture, so an `Id` changing from `Guid` to `int` matches the fixture exactly (E-47 repaired). `patterned` because the registry is `id` and `key` compared heuristically, so a resource address under another spelling owes its own entry (E-74); both directions carry a floor, including six ordinary field names that must not match. The assembly holds exactly one identifier, so the scan asserts it found at least one; all four statement clauses now have a mechanism, and two of the four are heuristic in a direction the claim admits. Built 2026-07-27: the error-shape scan walks the framework's own `EndpointDataSource` rather than two hand-picked routes (E-46 repaired), and the identifier scan reads member TYPES, which no guard in the suite had ever done (E-47 repaired). Both plants red-correct at c5a7fb7, and neither was noticed by any other test, which is what unguarded meant. Two holes recorded rather than hidden: the error-shape predicate reads only `IResult`-rooted return types, so an MVC controller action is enumerated and never questioned (E-73), and the identifier registry is two entries compared heuristically (E-74). Both scans carry an extent assertion, because a walk over three routes and one identifier reads identically working and empty. |
| CON-2 contract parity fixture | shared `note-contract.fixture.json`, one physical file linked into the C# test project (`None Include` + `Link`) and read natively by the TS test; `ContractParityTests`, `noteContract.test.ts` | owed (the weakest of 3 obligations); **exactly one corpus, physically one file** `proven`: The fixture lives once, under `client-web/src/data/__fixtures__/`, and reaches the server test project as a linked item rather than a copy. This is the mechanism class's actual sentence and it holds; the claim's precondition (both consumers under one root that travels together) also holds here, so the sanctioned materializer alternative is not needed; **every hand-mirrored contract is pinned by the corpus** `owed`: Adding a fourth contract type with no fixture entry leaves all 157 tests green, and adding a fixture key nothing consumes is green on both sides (E-60). The C# consumer names three types in `[InlineData]`, the client names three in three `it` blocks, and the fixture holds three keys; no side enumerates anything. trigger: enumerate the public types under `Kernel.Contracts` and assert each has a key, and assert every key has a consumer; **drift on either side fails that side's build** `patterned`: Renaming `NextCursor` on `NoteListResponse` turns `ContractParityTests` red, so the server half binds. The client half reads `Object.keys` off a literal typed as the contract type, so it sees what the literal sets: an optional field added to the interface leaves it green (E-60). A required field would fail compilation, which is why the gap is easy to miss. trigger: derive the client key set from the type rather than a literal; one corpus, no copy to rot, and three hand-written lists deciding what it covers (E-60) |
| CON-3 read completeness for action-bearing surfaces | not built | owed; trigger: the first edition project with a restorable session (the kernel exemplar has none) |
| CON-4 a breaking change is detected by machine, approved by human | not built | owed; trigger: the first published external contract (a versioned API consumed outside the repo, a partner surface, or the first schema registry; the composed client in the same repo is CON-2's territory, not a published contract) |
| RT-1 realtime discipline | client is REST-only in v1 | owed; trigger: first realtime slice; the promotion set is the hub arch test, MaximumReceiveMessageSize, the createHub client seam, and the socket harness scenario |
| SRV-1 one deployable unit | not built | owed; trigger: the next edition build pass. Applies to web products; an API-only project re-rules it in its deltas file at adoption |
| RES-1 every outbound call is bounded, and retries are earned | not built | owed; trigger: the first external service dependency in an edition project. Database session bounds are RES-6's, carved out so this claim stays about the chokepoint shape |
| RES-2 no unbounded queue, pool, or backlog in the process | not built | owed; trigger: the first background worker or in-process queue in an edition project |
| RES-3 the host dies cleanly, and there is a test that kills it | not built | owed; trigger: the first deployed edition host (rides SEC-9's deployment-edge work; the drain budget is a CFG-1 operational setting ruled against the platform's grace period) |
| RES-4 liveness answers alone; readiness answers for its dependencies | not built | owed; trigger: the first deployed edition host (lands with RES-3 and SEC-9; the health root SEC-1 already allowlists is the surface this claim splits in two) |
| RES-5 poison is quarantined at a counted limit, never retried forever, never dropped | not built | owed; trigger: the first message broker in an edition project (the transactional outbox relay, DATA-8, is the likely first consumer) |
| RES-6 every database session carries four finite bounds, each proven where it lives | not built | owed; trigger: the next edition build pass. Everything here runs on the existing integration tier against the real engine, so proven is reachable immediately; nothing waits on a new dependency |
| PERF-1 a hot operation's IO is counted, budgeted, and gated | not built | owed; trigger: the next edition build pass (the v1 exemplar's list endpoint is the first designated seam; the interceptor rides the same seam TEN-4's stamps use) |
| PERF-2 a designated hot path's allocations are budgeted per operation | not built | owed; trigger: the first designated hot path in an edition project; designation before need fails the YAGNI gate, so the family expects most projects to hold zero designations for a long time, honestly |
| PERF-3 a designated algorithmic seam proves its scaling with a counted ratio | not built | owed; trigger: the first hand-written algorithm over unbounded input in an edition project; library calls and database queries are not designations (the library's complexity is its documentation's promise, and query shape is PERF-1's and DATA-2's) |
| PERF-4 no sync IO and no whole-payload buffering on the serving path | not built | owed; trigger: the next edition build pass (a BannedApiAnalyzers configuration plus one host assertion; the client side already carries the idiom under UI-2's lint discipline) |
| PERF-5 every cache is bounded and every entry expires | not built | owed; trigger: the first cache in an edition project; the claim deliberately does not mandate caching anywhere, it prices caching where chosen |
| PERF-6 wall time is never asserted raw | not built | owed; trigger: the first wall-time benchmark in an edition project; the lint half can land at the next edition build pass, ahead of any benchmark existing, since its job is to keep the first timing assert out |
| MOD-1 module co-location | the Notes module is co-located by construction; no cross-module rule exists on either side | owed (the weakest of 3 obligations); **a module's code lives together on each side of the wire** `latent`: The skeleton instantiates the layout and Notes is co-located, which is the mechanism class's first part and is real. Nothing asserts it, and with one module nothing could distinguish co-located from coincidental. trigger: the second module; **modules meet only through interfaces registered at the composition root** `owed`: Planted a second module taking a direct constructor dependency on another module's service: green, 157 of 157. The string `module` appears in no test; `DependencyDirectionTests` partitions layers, not modules (E-62). trigger: the second module, and a rule that partitions by module folder with the platform layer whitelisted, which the claim's weakening note requires; **client-side deep-path import bans force modules through their public surface** `owed`: The lint this row cited is `serviceBan`, whose own message names UI-5: it stops screens reaching the transport, not modules reaching each other (E-62). trigger: the second client module; not built rather than not executed, and the cited client lint belongs to UI-5 (E-62) |
| MOD-2 deterministic naming and placement | `NamingPlacementTests` (server source scan) + `namingPlacement.test.ts` (client) | owed (the weakest of 3 obligations); **one public type per file, named for the file** `proven`: A second public type added to `NoteCursor.cs` turns the scan red. The scan walks every non-generated file under `src`, counts delegates separately so a delegate cannot be the smuggled second type, and carries two declared exceptions (`Program.cs`, and `*Contracts.cs` under `Kernel.Contracts` only); **routes originate only from endpoint files** `proven`: A `MapGet` planted in `SessionVersionMiddleware.cs` turns `Routes_are_declared_only_in_endpoint_files` red. The pattern covers every framework `Map*` binder including `MapHub<T>` and bare `Map(`, and deliberately does not match a custom `Map{Resource}Endpoints` aggregator; **artifact kinds form a closed registry, and a new kind is a deliberate kernel edit** `owed`: The check knows six suffixes and constrains where those six live. `NoteRepository.cs`, a seventh kind placed in `Kernel.Api/Platform/`, is green at 157 of 157 (E-63). The claim's sentence is about the registry being closed, and nothing notices a kind that is not in it. trigger: fail on a file whose suffix the registry does not declare; naming and routes bind; the artifact-kind registry is closed over six suffixes and silent about a seventh (E-63) |
| DOC-1 documentation lifecycle | `docs-lint.mjs`: front matter, placement, work slice ids, the narrow design/ governance, the MET-08 dash check (exclusions declared above) | owed (the weakest of 6 obligations); **every markdown declares a kind, and the kind matches the single legal root it lives in** `proven`: red-correct on a wrong kind in docs/claims/ and in docs/decisions/. The walk reaches every markdown in the tree. `bin`, `obj` and `dist` are skipped only where a project manifest sits beside them, so they are build OUTPUT rather than a name (E-32 repaired): authored markdown under a `docs/bin/` is now caught, and the identical file under a real project's `bin/` is still ignored. Both directions are controls. `proven` rather than `patterned` since 2026-07-27: E-32 was this obligation's only recorded residue, and the walk is now total over authored files. `node_modules` and `.git` stay unconditional because neither is ever authored; **no markdown outside the legal roots, closure over the edition tree** `proven`: red-correct for server/handover.md. The walk reaches every markdown in the tree. `bin`, `obj` and `dist` are skipped only where a project manifest sits beside them, so they are build OUTPUT rather than a name (E-32 repaired): authored markdown under a `docs/bin/` is now caught, and the identical file under a real project's `bin/` is still ignored. Both directions are controls. `proven` rather than `patterned` since 2026-07-27: E-32 was this obligation's only recorded residue, and the walk is now total over authored files. `node_modules` and `.git` stay unconditional because neither is ever authored; **working documents carry a slice id** `patterned`: red-correct for a work doc with no slice key. The value is never validated (`slice: whenever` passes) and the check keys only on the folder name work, so a document declaring itself working in any other root owes nothing (E-33); **status transitions move only forward** `owed`: unbuilt. Forward and backward transitions on the same file produce byte-identical clean output; the guard is a snapshot lint that holds no prior value (E-33). The previous note admitted this while the row read `proven`. Trigger: a docs-lint reader that compares a declared status against its previous committed value; **byproducts die at slice completion, and archived documents are not cited as authority** `patterned`: `citationFindings` resolves every markdown link in every governed document, relative or root-anchored, and fails a LIVE document that cites an ARCHIVED one. Asymmetric on the citer by design: an archived document may cite anything, because history refers to history; a live one handing a reader a superseded answer is the failure. Red-correct for the edition's one real inter-document citation, README.md to VERIFICATION.md, with the target archived (E-33 repaired). `patterned` because it reads MARKDOWN LINKS: the edition README also names the runbook in backticks as prose, which is a citation a reader follows and this predicate cannot see. The first clause, that a byproduct dies at slice completion, has no mechanism either; nothing compares a slice's completion against the documents it produced. Trigger for both: a citation form register, and a slice-completion signal the tool can read; **the docs-lint gate runs in the CI loop** `owed`: the edition's own ci.yml is a template; deleting its only invocation of the guard is reported by nothing. What runs docs-lint in this repo is kernel.yml, which the row did not name (E-39). Trigger: instantiation arming the loop, which E-24 established ships no mechanism; planted 2026-07-27, eleven plants, five red naming DOC-1 and six green. The stateless halves the previous note admitted were never built while the row read `proven`; they are per-obligation now (E-33). The tree walk once skipped any directory named bin, obj, dist or node_modules at any depth, docs/ included; it now skips build output only where something builds (E-32 repaired). The tool is fixture-free and the documented shared-tier edit path disables it with every gate green (E-28). The tool gained `--self-test` on 2026-07-27, so a narrowing of these predicates now fails a control instead of passing silently (E-28 repaired). |
| DEC-1 a module's behaviour traces to a decision | docs-lint fails a `docs/decisions/*.md` without a `provenance` field (the upward link); the decision template models the field | owed; the downward link is owed (trigger: first regeneration of a module from its decisions); the artifact-order check rides here too (trigger: next acceptance test); the upward provenance lint is built and proven |
| UI-1 token source lockstep | `tokenCoverage.test.ts` reads the export at its canonical home `design/prototype/_ds/colors_and_type.css` (every CSS var has a token, count > 50) | latent; Lowered from `proven` to `latent` on 2026-07-27 by the acceptance test. The mechanism binds and is not in question; its SUBJECT is the kernel`s own placeholder. `design/prototype/_ds/colors_and_type.css` ships 58 variables, deliberately over the floor of 50, and its own header says to replace it with a real export at instantiation, so `tokenCoverage.test.ts` goes green in a seeded project whose design system is the kernel`s filler and whose product design does not exist. That is `latent` by the vocabulary`s own definition: the mechanism exists and has had no real subject yet. Compounding it, manifest step 6 demands the complete design export at seed time, which the method`s own skill order makes impossible (seed runs immediately after gate 1; the export is two skills away, behind decompose and design), and it duplicates x2:lock. The manifest offers no fallback and part C admits no blocked state (E-27). TEST-3 and HUM-1 both carry an instantiation asterisk; this row needed one and did not have it. Finding: E-27. |
| UI-2 literal visual values are lint errors | `eslint.config.js` bans visual literals in styles: colors, font props, radii, hairlines, and spacing/dimension literals (numerics matched on `raw`), with the structural allowlist | patterned (the weakest of 2 obligations); **error-tier rules ban literal visual values and gate the client verification chain** `proven`: planted 2026-07-27 across seven categories (hex, named colour, bare dimension, px string, radius, border shorthand, fontWeight); each failed `eslint .` with the message naming UI-2, and `verify` short-circuits on it. The reach is now asserted executably rather than by plant alone (E-30); **coverage of every axis and every syntactic form a literal can take** `patterned`: `marginTop: -8` and `borderTopWidth: 1` both pass (E-31). Each newly gated axis and each newly reachable syntactic form owes its own selector, which is the standing debt this tag names. The fourteen measured holes are carried as passing controls in the extent test, so closing one turns that test red and has to be argued (E-31); the ban is not total: bare negative numerics escape every dimension selector, because the numeric selectors use a direct-child combinator and a negative parses as Property > UnaryExpression > Literal, and the per-side axes (borderTopWidth, outline) are in no alternation (E-31). Planted 2026-07-27, nine plants, seven red naming UI-2. The config itself is fixture-free and can be narrowed to reach nothing with `npm run verify` green (E-30). The config gained an executable extent assertion on 2026-07-27 (E-30 repaired): 49 assertions over 19 caught and 22 ignored controls plus an independent 54-colour floor and 21 gated axes, red under all five sabotage probes including a drop to `warn` severity, while `eslint .` stayed green under every one. |
| UI-3 screens compose primitives only | `themeBan` in eslint plus the naming scan restrict theme imports to `src/components/**` and `src/theme/**` | owed (the weakest of 3 obligations); **the primitive component layer is the sole consumer of theme tokens** `proven`: Two independent mechanisms, the eslint `themeBan` over `**/theme` and `**/theme/**` and a source scan in `namingPlacement.test.ts`, and both restrict the importer set to `components/` and `theme/`. That matches the claim, which speaks of the primitive LAYER. The conformance row previously read `Text`/`Button` are the only token importers, which is a rule neither mechanism has: a third primitive importing tokens is legal and correct (E-69); **screens create no style sheets and use no raw platform text elements** `owed`: A bare `<p>` planted in `NotesScreen` is clean on tsc, eslint and 65 of 65 tests (E-69). UI-2's literal bans fire on visual VALUES, so an element carrying no inline style passes them, and nothing else looks. trigger: a screens-scoped lint on raw text elements and on style-sheet creation, which is the same kind of rule as the one already there; **client business logic lives in separately testable services, hooks and reducers** `latent`: The claim's own weakening note says this half is structural rather than lint-provable: the skeleton's shape is the mechanism and the fidelity suites plus off-framework unit tests are the evidence. With one screen and one repo the shape holds and nothing has tested that it stays true. trigger: the second module; the token half is enforced; the other two things the statement bans in screens are not (E-69) |
| UI-4 fidelity ledger | `NotesScreen.fidelity.test.tsx` renders every branch and asserts the `data-atom` set against a hand-transcribed ledger, in both directions | owed (the weakest of 3 obligations); **every ledger atom renders (exhaustiveness)** `patterned`: Renaming a ledger atom so it stops rendering turns the test red, and both branches (populated and empty) are rendered so a conditional atom cannot hide. Per-screen by construction: this is one screen's suite and each new screen owes its own. trigger: the second screen; **nothing renders beyond the ledger (de-fabrication)** `owed`: The assertion iterates `querySelectorAll('[data-atom]')`, so its subject is the set of elements that already declare themselves part of the ledger's vocabulary. A fabricated `<div data-atom="promo-banner">` is caught; a fabricated `<p>` with no marker is green at 65 of 65 (E-68). This is the direction the claim exists for, and it can only police code that is cooperating. trigger: assert over the rendered element tree with a declared allowlist of invisible wrappers, so an unmarked element fails rather than being absent; **the ledger is exported from the locked prototype, not hand-maintained** `owed`: The ledger is a hand-written const in the test file. The claim names this itself: "The v1 exemplar ledger is hand-authored: the prototype-to-ledger exporter is owed, and until it exists the ledger is a second hand-maintained copy of the prototype, carrying exactly the UI-1 divergence risk this claim's export mechanism is meant to remove." trigger: the first slice with a design tool that emits a machine-readable prototype; both directions bind for atoms that opt in; fabricated UI that carries no marker is invisible to both (E-68) |
| UI-5 thin UI over tested services, composed entrypoint exercised | eslint `serviceBan` on `api/**` and `data/**` outside the composition root; `npm run smoke` boots the actual `src/main.tsx` in jsdom and asserts a real request per primary flow | owed (the weakest of 3 obligations); **modules outside the composition root import neither the transport nor the data services** `proven`: Importing `../../data/notesRepo.ts` into `NotesScreen` fails eslint with UI-5's own message. Centralized: a violating import cannot merge. Type-only imports stay legal, which the claim requires explicitly. This closes the route the acceptance-test pilot's failure actually took; **the client UI holds no business or transport logic of its own** `owed`: A screen-local function calling `fetch` with the API base URL and a bearer token from the environment imports nothing, so the ban cannot see it: clean tsc, clean eslint, 65 of 65 tests (E-70). The realistic arrival is a screen needing one endpoint the repo does not expose yet. trigger: a screens-scoped ban on `fetch`, `XMLHttpRequest`, `EventSource` and `WebSocket`; **the composed entrypoint is exercised against a running server** `latent`: `composedApp.smoke.ts` imports the real `src/main.tsx`, wraps `globalThis.fetch` to record what leaves, and waits for a real GET and a real POST per primary flow. It needs the server up, which needs the engine, so it was read and not run here. Per-seam by the claim's own note: each new primary flow owes its smoke line. trigger: a run with the stack up; the import ban binds; transport logic that imports nothing walks past it, and the smoke has not been run here (E-70) |
| DEP-1 dependency quarantine | CPM exact pins + lockfiles + locked-mode restore (`--locked-mode` / `npm ci`) + `VERSIONS.md` (cooling-off window header, container-images section, kernel-provenance section) + docs-lint ledger, override, image, cross-surface image agreement, window-number and kernel-provenance checks | owed (the weakest of 7 obligations); **every direct dependency is pinned exactly, dated, and restored in locked mode** `patterned`: the ledger check is keyed on name AND version and is red-correct for a floating range and for an unledgered direct dependency. Source mapping is deletable unreported, package.json overrides are never read, and publish dates are shape-tested only, never parsed against today (E-37); **every container image is pinned tag-plus-digest and ledgered** `patterned`: red-correct for an unpinned mcr.microsoft.com image in a shell script. E-38 is half closed: a Dockerfile `FROM` line is now read whatever the registry host, so Docker Hub shorthand (the obligation's own stated violation shape) is caught, and an extensionless `Dockerfile` is now a scanned surface. The closure is gated on the file being a Dockerfile, because the ungated version read two English sentences in VERIFICATION.md beginning a line with the word `from` as floating images while its self-test stayed green (E-76). Still `patterned` for the residue that remains a carried control: a bare `name:tag` outside a FROM line, in a compose file for instance, is invisible, and widening to it would match every `key: value` in the tree; **one image value across every surface naming an image** `proven`: `imageAgreementFindings` collects every image reference in the tree into a map keyed by repository and fails when one repository is named with more than one value. The ledger's own row is in the comparison, reconstructed from its two cells, so ledger-versus-code drift is visible and not only code-versus-code. Red-correct: changing the digest in `scripts/db-up.sh` alone reports the repository, both values, the four surfaces holding one and the one holding the other. `proven` rather than `patterned` because it needs no registry and no entry per surface: it compares whatever it finds against whatever else it finds, so a sixth surface is covered the day it is written (E-37, E-64 repaired); **a transitive pin taken under the advisory rule carries its own ledger row** `proven`: The npm reader now reads `overrides` alongside dependencies and devDependencies, including npm's nested per-parent form, so an override is checked for exactness and for a dated ledger row exactly as a direct pin is. That is what DEP-1's advisory rule asks for in so many words, a row "whether the dependency is direct or transitive", and an override is the most deliberate pin in the file because it overrules what a dependency asked for. Red-correct: moving the shared tier's postcss override off its ledgered version reports it by name and version (E-37 repaired); **the cooling-off window number lives in exactly one place** `proven`: The number is parsed from the VERSIONS.md header and every other assertion of it in the tree is compared against it. It does not ban restating the number, which would falsify dated records naming the window in force at the time; it makes restating it unable to drift, so a cut turns every stale statement red and each one is then decided as history or as a live rule. Red-correct for a second number planted in BUILD-BRIEF.md. It found a live instance on its first run: node-react's own VERSIONS.md stated the superseded window as a live one while its header declared the current number (E-76, repaired). Residue, stated rather than carved out: a future dated record naming a superseded window fails this check and must cite the header instead of restating the number; **kernel provenance is ledgered and moves only with the mechanisms** `latent`: the section's absence fails docs-lint, red-correct. No real pin exists until the next instantiation fills one, so the comparison half has never run; **publish date is checked against the cooling-off window** `owed`: unbuilt, and the row has said so in prose since before this round. Trigger: the publish-date CI check DEP-1 already names as its upgrade path; Planted 2026-07-27, seventeen plants: four red-correct, twelve green, one red-different. Four of the twelve greens were repaired on 2026-07-28 and three obligations moved from `owed` to `proven`: cross-surface image agreement, package.json overrides, and the cooling-off window number. Two of the three found a live defect on their first run against a real tree (E-76): node-react's own VERSIONS.md asserted the superseded window as a live one under a header declaring the current number, and the E-38 image widening read two English sentences as floating container images while its self-test stayed green. The row stays `owed` on its weakest obligation and only that one: publish dates are shape-tested, never parsed against today, which needs a registry lookup this tool does not make. Still carried and not repaired: the CI toolchain is unpinned and unledgered (E-1); the lockfile-drift remedy fails the build but npm's EUSAGE names npm rather than DEP-1, in a different job from every other check, which is a third-outcome result and not a pass; source mapping is deletable unreported (E-37). The `postcss` advisory-rule pin (ruling 9a, 2026-07-26) is now checked like any other pin rather than parsed and ignored, and leaves the advisory section on 2026-08-11 (E-18). |
| DEP-2 standing advisory sweep with a remediation clock | not built | owed; trigger: the next edition build pass, moved there from the first armed CI loop by adjudication ruling 9c on 2026-07-26. The mechanism is an advisory sweep over the pinned set, which is buildable in the shared tier today with no instantiation and no third-party action, and the old trigger deferred the only mechanism that would report an advisory the kernel already ships with. The publish-date check DEP-1 names as its upgrade path rides the same job |
| TEST-1 tier strategy | SQLite unit/arch tier, Testcontainers integration tier with a unique database and the real migration, e2e through the client service layer; EF InMemory banned by `NamingPlacementTests`; the edition's `ci.yml` runs all three | owed (the weakest of 4 obligations); **no fake in-memory database provider in any test project** `proven`: Adding `Microsoft.EntityFrameworkCore.InMemory` to the unit project turns `Test_projects_do_not_reference_ef_in_memory` red. The scan is case-insensitive and covers `Directory.Build.props` and `Directory.Packages.props` as well as every test csproj, so neither a casing trick nor a shared props file evades it; **the integration tier uses the real engine, disposable, unique per run, with real migrations** `latent`: `SqlServerFixture` starts one ephemeral container, points it at `Kernel_{guid}` and runs `MigrateAsync()` rather than `EnsureCreated`, which is exactly the claim's shape. It has not been executed here: no container runtime is available. Until 2026-07-27 that produced four RED tests whose messages were container startup errors, in a suite where red means a conformance defect (E-49); they now skip, naming the claim and naming what goes untested, which is engine-specific by construction (`DateTimeOffset` ordering under the keyset cursor, composite-key behaviour). Controlled: forcing the runtime probe true turns all four red again, so the attribute is not skipping unconditionally. `latent` is what a tier that skips everywhere is worth, and CI is the gate against it staying that way. trigger: a run with a container runtime available; **exactly one provisioning path** `owed`: Three: Testcontainers for the integration tier, `scripts/db-up.sh` for development, and a workflow `services:` container plus `dotnet ef database update` for e2e. The claim's harm paragraph names this shape by name. The pinned image digest is written out four times and changing one copy alone leaves everything green (E-64). trigger: one committed machine-readable pin that the script, the fixture and the workflow all read; **all three tiers run in the CI loop** `latent`: The edition's `.github/workflows/ci.yml` restores locked, builds with `-warnaserror` and runs all three tiers. It is a template that becomes real CI only at instantiation, and instantiation ships no arming mechanism (E-24), so it has never run. This repository's own CI now runs the two engine-free tiers, 203 architecture and 58 unit, on every push and pull request (E-39 repaired); the engine tier stays in the edition template where the engine is. So the obligation is split by fact rather than by wish: two of the three tiers are continuously run against this edition's code, and the third runs only where an engine exists. trigger: E-24, for the tier that needs the instantiated loop; the ban binds and the integration fixture is correctly shaped by reading; it has never run here, and the engine pin is duplicated four times (E-64, E-49) 2026-07-27: E-39 is repaired. `kernel.yml` gained a `dotnet-server` job running `restore --locked-mode`, `build -warnaserror` and the architecture and unit tiers by name, so the 203 guards this edition ships are now protected by a continuous process rather than by whoever remembers. That was the reason a neutered guard survived twice (E-29, E-30). |
| TEST-2 e2E harness | `tools/harness/main.ts` drives the real client services out of process (NDJSON + redaction, fails loudly not vacuously); a completeness self-audit wraps the enumerated methods and diffs them against what ran | owed (the weakest of 4 obligations); **an out-of-process harness drives the real, unmodified client service layer** `proven`: `main.ts` imports `KernelClient` and `NotesRepo` themselves, not a parallel HTTP client and not a browser puppet, and runs as a node process against a running server. This is the mechanism class's own sentence and the harness satisfies it by construction; **the floor is complete: every public method of every client data service has a scenario** `owed`: The audit reads `Object.getOwnPropertyNames(NotesRepo.prototype)`. A second data service is invisible: `TagsRepo` with two public methods and no scenario leaves the coverage line byte-identical, tsc and eslint clean, 65 of 65 tests green. A method declared as a class-field arrow function is invisible too, because it is an instance property and never enters the set that gets diffed (E-67). trigger: enumerate the class exports under `src/data/` rather than naming one, and enumerate instance members as well as prototype members; **the deterministic tier runs in CI** `latent`: The edition's `ci.yml` carries the `e2e-wire` job which starts the API and runs the harness. It has never executed: the workflow is a template until instantiation (E-24). This repository's own CI now runs the edition's engine-free tiers (E-39 repaired) and deliberately does not run `e2e-wire`, which needs a live engine and a booted server. Locally it needs the stack up, which needs a container runtime. trigger: the first instantiation, or a local run with the engine available; **harness output redacts secrets** `patterned`: `redact.ts` scrubs Authorization headers and JWT-shaped strings and every emitted line goes through it; `redact.test.ts` proves the true positives. It asserts nothing about what survives, and its JWT pattern matches any dotted identifier (E-58). trigger: anchor the pattern and add a passing case for an identifier that must survive; the harness is real and the audit's shape is right; its reach is one class, prototype members only (E-67) |
| TEST-3 CI loop | `ci.yml`: server, client, docs-lint, secret-scan, e2e-wire; `tools/gate-check.mjs` reads the armed state back off the forge and `--self-test` drives its predicate against six violations and seven controls | latent (the weakest of 2 obligations); **every mechanism the catalog names executes automatically in CI on every push and pull request** `patterned`: `ci.yml` declares server, client, docs-lint, secret-scan and e2e-wire. The workflow has never executed on a forge, and one of the five jobs is red on day one in a seeded project for a missing allowlist the file set does not carry (E-25); **the loop gates merges, armed at instantiation, with the arming step carried and verified** `latent`: Arming is an instantiation step and both TEST-3 and HUM-1 word it that way: the manifest carries the step and the kernel acceptance test verifies it. Until 2026-07-27 the kernel shipped the step as one imperative sentence and nothing that could see whether it had been done, so an armed instantiation and an unarmed one were indistinguishable by every means the kernel shipped (E-24). `tools/gate-check.mjs` closes the verification half: it derives the required check names from the workflow rather than listing them, requires a pull request and code-owner review, catches a ruleset in evaluate mode, and fails rather than passes when there is no remote, no token, or no readable workflow. Red-green against its own controls, and the three unreachable paths each exit non-zero, measured in a seeded-shape tree. `latent` because it has never been run against a real forge: that needs a token this repository does not have. trigger: the first instantiation, which is also E-24's own trigger; the loop runs everything; arming stays a human step, and skipping it is now loud rather than invisible |
| TEST-4 real-runtime boot proof | not built | owed; trigger: the next edition build pass. UI-5's composed smoke already proves entrypoint wiring in-process; the real-boot script assertion and built-form pinning are the promotion set that completes it |
| HUM-1 irreversible surfaces get a human turn, unconditionally | `.github/CODEOWNERS` covers migrations, contracts and `docs/contracts/`, reconciled against `edition.json``s `irreversibleSurfaces` so renaming one and forgetting the other is loud; docs-lint fails if any of the three loses its owned entry, and fails outside the kernel repo if an entry names only the `@OWNER` placeholder; branch protection requires code-owner review; `tools/gate-check.mjs` reads back whether code-owner review is actually required | latent (the weakest of 2 obligations); **every irreversible surface is enumerated and declared to require a named human review** `proven`: the three surfaces are declared in `edition.json` and reconciled against `.github/CODEOWNERS`, and an entry naming only the shipped placeholder is now refused outside the kernel repo. Red-green proven in a seeded tree: placeholder gives three failures, one per surface, each naming the path and the claim; a real handle gives ok; the kernel itself stays green because its placeholders are legal there; and the pre-repair predicate with the placeholder still in place goes green again, which reproduces the hole (E-23); **the merge gate requires that review, armed at instantiation** `latent`: Measured absent 2026-07-27: no forge call, no ruleset, no token, no readback, and every hit for branch protection was prose (E-24). `tools/gate-check.mjs` now reads whether code-owner review is required on the default branch, across both the ruleset and classic branch-protection shapes, because reading only one would report a gated repository as ungated. The self-test carries this as its own violation case, separate from TEST-3's, so a change that stopped checking code-owner review would be caught without the status checks changing. `latent`: never run against a real forge, which needs a token this repository does not have. trigger: the first instantiation; the declaration half is proven; the merge-gate half is now readable and has not yet been read against a real forge |
| AI-1 server-injected identity | `ToolExecutor` reject-then-inject + throw-on-unset, rejecting server-owned keys recursively through nested objects and lists, matched through the shared `NameComparison` over `TenantNames` plus the OIDC claim and delegation vocabulary; `ToolExecutorTests` incl. a nested-payload case and a thirty-case extent theory | patterned (the weakest of 3 obligations); **actor-supplied identity, tenant and scope are rejected before any merge** `patterned`: Rejection is case-insensitive and recurses nested dictionaries and lists, both covered by `ToolExecutorTests`. The reach was `ServerOwnedKeys`, twenty-one entries matched by equality: against a twenty-name floor written independently of it, six escaped, `workspaceId`, `accountId`, `workspaceSlug`, `organisation_id`, `tenantIdentifier` and `actorId`, while the registry's own comment claimed parity with the URL and EF-model guards (E-53). `NameComparison` and the canonical tenant registry now live in the application assembly, so this chokepoint reads the same comparison and the same list as those guards; the floor and a ten-name cost side are both asserted, and restoring the equality set turns six of them red. Still `patterned` rather than `proven`, now for the reason the claim names rather than a defect: a `JsonElement`-typed value is a subtree the executor does not walk, so every tool accepting a raw JSON payload owes its own review. trigger: the per-tool parameter allowlist the claim names as the upgrade, or the first tool that takes a raw JSON payload; **server context is injected unconditionally** `proven`: `arguments["tenantId"] = tenantId` runs after sanitizing, on every call, with no branch that can skip it; **an unset context throws rather than proceeding unscoped** `proven`: `tenantScope.Current` is read before the tool is reached and throws when unset; `ToolExecutorTests.Invocation_without_a_scope_throws` covers it and turns red when the accessor is made to fail open; the chokepoint, its recursion and its reach all hold; what remains is the JsonElement subtree the claim itself carves out |
| AI-2 untrusted content, no authority | `ListNotesTool` read-only + `ListNotesToolReadOnlyTests`; `docs/decisions/ai-trust-tiers.md` | patterned (the weakest of 2 obligations); **capability derives from server configuration and the principal, never from content** `patterned`: the trust tiers are named in `docs/decisions/ai-trust-tiers.md` and AI-1's chokepoint is where the derivation actually happens; full taint tracking is out of v1 by the claim's own phasing, not deferred by this edition; **a tool declared read-only is proven read-only by test** `patterned`: `ListNotesToolReadOnlyTests` covers the v1 tool; each new tool owes its own guard test; the v1 tool has its guard test, each new tool owes its own; full taint tracking and the SSRF egress guard are owed (trigger: outbound agentic requests) |
| AI-3 prompt architecture | not built | owed; trigger: the first model-backed feature in an edition project |
| OBS-1 external-effect seam observability | not built | owed; trigger: the first external side-effect port in an edition project. This claim opens the observability family the v1 cut deliberately deferred |
| OBS-2 refusals and privileged actions are security events | not built | owed; trigger: the first deployed edition host (alongside SEC-9; the refusal seams it instruments are already built and tested, so the events attach to existing tested paths) |
<!-- conformance:end -->

Also owed at v1 (out of the cut line, recorded not lost): an identity/auth module (tests and the harness mint their
own JWTs with the dev key), the Expo/React Native client variant (a proven mobile-client pattern, lifted
when the first mobile kernel project appears), and registry externalization: the per-project name registries the
scans read (SEC-2 forbidden fields, SEC-3 PII names, CFG-1 literal shapes) move to project-owned data files so
kernel-owned test code can be re-copied at upgrade without clobbering project extensions (trigger: the first
seeded-project upgrade or the next edition build pass, whichever lands first).

## Known limitations

Honest edges where a static or runtime scan cannot be complete. Each is a slice-level review item, not silent
coverage:

- A handler that takes `HttpContext` and reads `Request.Query["email"]` at runtime is invisible to any static
  route-table scan (the key is never a declared parameter). Ad-hoc query reads are reviewed per slice; declared
  parameters (including `[AsParameters]`, collections, and DateTime) are scanned.
- `EndpointSpineTests` enumerates `RouteEndpoint`s. A route surfaced by a custom `EndpointDataSource` yielding bare
  `Endpoint`s is not scanned; all minimal-API routes are `RouteEndpoint`s, so this needs a deliberate custom source.
- `ContractParityTests` compares the camelCase field-name set of each mirrored contract, not property types or
  nullability, and only the enumerated contracts. A new hand-mirrored contract must be added to the fixture and the
  theory; a type change that preserves the name set is a review item. A generator would collapse this obligation.
- The UI-2 named-color list is broad but not the full CSS keyword set; the token system plus review are the real
  defense, the lint is the cheap net.
- `InMemorySessionVersionStore` is process-local and resets on restart or scale-out, so revocation is un-done there.
  This is the documented skeleton choice (the EF-backed store is owed at the first identity slice) and is a hard
  pre-production prerequisite for any multi-instance deployment.
- AI-1 rejects server-owned keys (the common OIDC/Azure claim names plus `org*` tenant synonyms) recursively
  through nested dictionaries and lists. A value smuggled inside a `JsonElement`-typed argument (a raw JSON subtree
  the executor does not walk) is still a per-tool schema-review item; the allowlist-per-tool upgrade would close it.
- The strengthened `sv`/tenant gates exempt an endpoint whose matched metadata carries `IAllowAnonymous`. This
  relies on `UseRouting` running before the gates so `GetEndpoint()` is populated; the pipeline sets that order
  explicitly. A gate placed before `UseRouting` would see a null endpoint and fall back to enforcing.
